# IMPLEMENTATION_PLAN

Last updated: 2026-03-25 (Asia/Manila) — iteration 170 (TECH-073 add StringComparison.Ordinal to 11 culture-dependent string operations)
Status: **All phases and spec clarifications complete.** Phase 1 complete: P0-001–P0-022; Phase 2 complete: P1-001–P1-012, P2-001–P2-005; Phase 3 complete: P3-001–P3-038 (all 38 items). Tests complete: T-001–T-008; ~2917 tests passing (2411 backend + 470 frontend + 6 parity); 0 bugs blocking, 0 tech-debt blocking. Spec clarification backlog complete: SPEC-001–SPEC-017 all patched. All 55 acceptance criteria across 11 specs marked complete. Iteration 170: TECH-073 (add `StringComparison.Ordinal` to 11 culture-dependent string operations across 7 files).

## Execution Rules
- Always implement highest-priority uncompleted item first.
- Do not skip security/tenant boundaries for feature delivery.
- Do not ship Azure resource changes without corresponding Terraform and ARM template updates.
- Keep changes small, test-backed, and vertically integrated.
- Update this file at end of each Ralph iteration.

## Global Definition of Done
- Behavior implemented and wired end-to-end for the target scope.
- Unit + integration tests added/updated and passing for changed behavior.
- Relevant security checks included (RBAC, tenant isolation, ACL trimming).
- Telemetry added (logs/metrics/traces + correlation IDs) for new critical paths.
- Docs/spec links updated where behavior changed.
- For infrastructure changes, Terraform + ARM templates updated and validated.

## Phase 1 (MVP) Priority Queue

### P0 Foundation and Security (complete)
- [x] P0-001: Establish solution skeleton and service boundaries (.NET API, ingestion workers, React app, shared contracts).
  - Specs: jtbd-01, jtbd-05, jtbd-10
  - Completed: Solution skeleton with SmartKb.Api, SmartKb.Contracts, SmartKb.Ingestion, SmartKb.Data, React frontend, 4 test projects.

- [x] P0-002: Implement Entra ID authentication + RBAC role model.
  - Specs: jtbd-10
  - Completed: Microsoft.Identity.Web JWT bearer auth; PermissionRequirement/PermissionAuthorizationHandler; role-to-permission matrix with 14 permission strings across 5 roles; fallback policy requires authentication; 20 auth tests.

- [x] P0-003: Implement tenant context propagation and hard tenant isolation.
  - Specs: jtbd-10
  - Completed: TenantContextMiddleware extracts `tid` claim; 403 + audit on missing tenant; correlation IDs on Activity tags/baggage; cross-tenant audit events; 22 tests. BUG-001 fixed (iteration 16).

- [x] P0-004: Implement secret architecture.
  - Specs: jtbd-01, jtbd-07, jtbd-10
  - Completed: ISecretProvider with KeyVaultSecretProvider (DefaultAzureCredential); OpenAiKeyProvider; SecretMaskingExtensions; diagnostic endpoint; 19 tests.

- [x] P0-005: Create initial SQL schema + migrations.
  - Specs: jtbd-01, jtbd-05, jtbd-06, jtbd-07, jtbd-10
  - Completed: SmartKb.Data with EF Core; 10 entity classes; InitialCreate migration; SqlAuditEventWriter; global query filters for soft-delete; 18 data tests.

- [x] P0-005A: Establish baseline IaC for core Azure resources.
  - Specs: jtbd-11
  - Completed: Terraform (modular .tf files) + ARM (main.json) with full resource parity; env-specific parameter files. Known issue: ARM missing SQL Entra administrator (see BUG-002).

- [x] P0-005B: Add CI validation for Terraform and ARM templates.
  - Specs: jtbd-11
  - Completed: GitHub Actions with terraform fmt/validate + ARM JSON/structure validation + conditional az deployment validate.

- [x] P0-005C: Define canonical record schema, ACL filter schema, embedding + chunking config.
  - Specs: jtbd-02, jtbd-03, jtbd-10
  - Completed: CanonicalRecord, EvidenceChunk, SearchFieldNames, EmbeddingSettings (text-embedding-3-large@1536), ChunkingSettings (512/64); 23 tests.

### P0 Ingestion + Evidence Store MVP
- [x] P0-006: Build connector admin backend endpoints.
  - Specs: jtbd-07
  - Completed: 14 endpoints for connector CRUD, field mapping validation, preview, sync-now, sync-runs; audit events on all mutations; 32 integration + 23 DTO tests.

- [x] P0-007: Build ingestion orchestration with Service Bus queue, retries, idempotency, and checkpoint tracking.
  - Specs: jtbd-01
  - Completed: SyncJobMessage, ServiceBusSyncJobPublisher, SyncJobProcessor (Pending->Running->Completed/Failed, multi-batch, checkpoint persistence, error capping), IngestionWorker (Service Bus processor with DLQ handling), dead-letter peek endpoint; 16 new tests; all 228 tests passing.

### Bugs and Tech Debt (fix before proceeding with new features)

- [x] BUG-001: Fix 5 pre-existing TenantIsolation test failures (cross-tenant route design issue).
  - Completed: Rewrote 8 tests into 11 well-scoped tests. Replaced path-based tenant ID tests with connector-seeding approach (create in tenant A, access from tenant B JWT → 404). Fixed `AuthTestFactory` to register `InMemoryAuditEventWriter` as singleton for both `IAuditEventWriter` and concrete type. Fixed `AdminConnectors_ReturnsTenantId` to validate `ApiResponse` structure. Added `CrossTenantConnectorAccess_DoesNotLeakExistence` test. All 233 tests passing.

- [x] BUG-002: ARM/Terraform misalignment — ARM missing Entra administrator for SQL Server.
  - Root cause: `infra/terraform/sql.tf` has `azuread_administrator {}` block wiring the App Service managed identity as SQL admin. `infra/arm/main.json` had no corresponding `Microsoft.Sql/servers/administrators` resource.
  - Completed: Added `Microsoft.Sql/servers/administrators/ActiveDirectory` resource to ARM template matching Terraform behavior (login=smartkb-admin, sid=App Service managed identity principal, tenantId=entraTenantId). JSON validated.

- [x] BUG-003: `ConnectorSecretReference` model is orphaned.
  - Root cause: Model defined in `SmartKb.Contracts/Models/ConnectorSecretReference.cs` with fields for ConnectorId, TenantId, AuthType, KeyVaultSecretName, CreatedAt, RotatedAt. But no SQL entity, migration column, repository, or API surface references it.
  - Completed: Removed orphaned `ConnectorSecretReference` record and 3 associated tests. Can be re-added if secret rotation tracking is needed for P0-008/P0-009 connectors. All 233 tests passing.

- [x] TECH-001: Add Ingestion Worker App Service to Terraform and ARM templates.
  - Root cause: Only the API web app (`app-smartkb-api-{env}`) is provisioned in IaC. The Ingestion Worker needs its own App Service (or Container App) resource for production deployment.
  - Completed: Added `app-smartkb-ingestion-{env}` Linux Web App to both Terraform (`app-service.tf`) and ARM (`main.json`). Shares same App Service Plan as API. SystemAssigned managed identity with Key Vault Secrets User role and Azure Service Bus Data Receiver role. App settings: Application Insights + Key Vault URI. Connection string: SmartKbDb with Active Directory Default auth. ARM template validated (19 resources). All 233 tests passing.

- [x] TECH-002: Service Bus should use Managed Identity instead of connection string.
  - Root cause: `ServiceBusSyncJobPublisher` and `IngestionWorker` both use `ServiceBusClient(connectionString)`. Per jtbd-10 R6 and AGENTS.md, Managed Identity should be preferred.
  - Completed: Added `FullyQualifiedNamespace` to `ServiceBusSettings` (preferred over `ConnectionString`). Both API and Ingestion `Program.cs` now create `ServiceBusClient` with `DefaultAzureCredential` when namespace is set. Added `ServiceBus__FullyQualifiedNamespace` app setting to both App Services in Terraform and ARM. Added `Azure Service Bus Data Sender` role for API app (was missing). Added `IsConfigured`/`UsesManagedIdentity` helpers. 4 new tests; all 236 tests passing. ARM template validated (20 resources, 13 outputs).

- [x] TECH-003: Duplicate `KeyVaultSecretProvider` in API and Ingestion projects.
  - Root cause: `src/SmartKb.Api/Secrets/KeyVaultSecretProvider.cs` and `src/SmartKb.Ingestion/Secrets/KeyVaultSecretProvider.cs` are identical copies.
  - Completed: Moved single implementation to `SmartKb.Contracts/Services/KeyVaultSecretProvider.cs`. Added `Azure.Security.KeyVault.Secrets` and `Microsoft.Extensions.Logging.Abstractions` to Contracts csproj. Deleted both duplicate files. All 233 tests passing.

- [x] ~~TECH-004~~: Soft-deleted connectors block name reuse — **CLOSED (not a bug)**.
  - Resolution: EF Core global query filter `HasQueryFilter(c => c.DeletedAt == null)` on `ConnectorEntity` applies automatically to all queries on `_db.Connectors`, including the duplicate name checks in `CreateAsync` (line 80) and `UpdateAsync` (line 124). Soft-deleted connectors are already excluded. Verified 2026-03-16.

- [x] TECH-005: `SetStatusAsync` vs `EnableAsync` inconsistency in ConnectorAdminService.
  - Root cause: `SetStatusAsync` had a silent validation failure path (returns `(true, null)` when validation fails), while `EnableAsync` returns the validation result explicitly. The `/disable` route used `SetStatusAsync`.
  - Completed: Replaced `SetStatusAsync` with dedicated `DisableAsync` method. Updated `/disable` route to call `DisableAsync` directly. `DisableAsync` has clean return type `(Found, Response)` with no unnecessary validation path. All 233 tests passing.

- [x] TECH-006: No CI pipeline for .NET build/test or frontend build.
  - Completed: Added `.github/workflows/ci.yml` with two jobs — `dotnet` (restore/build/test in Release) and `frontend` (npm ci/lint/build). Triggers on PRs to main and pushes to main.

- [x] ~~TECH-007~~: Stale `src/src.sln` file in repo root — **CLOSED (not found)**.
  - Resolution: No `src/src.sln` file exists in the repository. Only `SmartKb.sln` at the root. Verified 2026-03-16.

- [x] TECH-008: Harden OpenAI response JSON parsing + async anti-pattern + clipboard error handling.
- [x] TECH-009: Narrow all bare `catch` blocks to typed exceptions.
  - Root cause: 33 bare `catch` blocks across Data, Contracts, Api, Ingestion, and Eval.Cli projects caught all exceptions including critical ones (OutOfMemoryException, StackOverflowException). This masks bugs and violates .NET best practices.
  - Completed (iteration 113): 16 JSON deserialization helpers → `catch (JsonException)`. 4 HMAC/Base64 validation methods → `catch (FormatException)`. 1 SyncJobProcessor → `catch (JsonException)`. 2 service calls (blob download, HTTP notification) → `catch (Exception ex) when (ex is not OperationCanceledException)`. 1 KV+JSON (OAuthTokenService) → `catch (Exception ex) when (ex is not OperationCanceledException)` with logging. Also added safety comment on FusedRetrievalService `.Result` in catch (safe because `IsCompletedSuccessfully` guards it). Replaced all 3 `GetProperty` chains with `TryGetProperty` for defensive JSON access. Replaced `.Result` with `await` in `FusedRetrievalService`. Added `.catch()` to clipboard write in `SourceViewerPanel`.
  - Completed (iteration 114): Narrowed 9 remaining bare catch blocks: 6 in Data repositories (DataSubjectDeletionService, PatternUsageMetricsService, ContradictionDetectionService, PatternMaintenanceService ×2, GoldCaseService) and 3 in Contracts connectors (AdoWebhookManager, ClickUpWebhookManager, HubSpotWebhookManager) — all JSON deserialization helpers → `catch (JsonException)`. Zero bare catch blocks remaining in backend. Frontend bare catches retained (TypeScript lacks typed catch syntax). Frontend: build clean, 413 tests passing.

- [x] TECH-010: Add React Router v7 future flags to silence deprecation warnings.
  - Root cause: React Router v6.22 emits `v7_startTransition` and `v7_relativeSplatPath` deprecation warnings on every `BrowserRouter` and `MemoryRouter` mount, producing noisy test output and risking breakage on v7 upgrade.
  - Completed (iteration 116): Added `future={{ v7_startTransition: true, v7_relativeSplatPath: true }}` to `BrowserRouter` in `App.tsx` and all 11 `MemoryRouter` instances in page test files. Zero deprecation warnings in test output.

- [x] TECH-011: Frontend bundle size optimization via route-based code splitting.
  - Root cause: All 11 page components were eagerly imported in `App.tsx`, producing a single 615KB JavaScript bundle that exceeded Vite's default chunk size warning limit.
  - Completed (iteration 116): Admin pages lazy-loaded via `React.lazy()` with `Suspense` fallback. Vendor dependencies split via `manualChunks` (React/Router 158KB, MSAL 265KB). Main bundle reduced to 48KB. 15 output chunks, zero build warnings. 413 frontend tests passing.

- [x] TECH-012: Replace `NotImplementedException` in test stubs with proper default returns.
  - Root cause: 3 `NotImplementedException` instances in `StubTokenUsageService` test class in `ChatOrchestratorTests.cs` — unused interface methods that could mask accidental calls.
  - Completed (iteration 116): Replaced with zero-value default returns (`TokenUsageSummary` with zeros, empty `DailyUsageBreakdown` list, `BudgetCheckResult` with `Allowed=true`). Zero `NotImplementedException` remaining in codebase.

- [x] TECH-013: Fix RetrievalFilterPanel uncontrolled input bug — Product Areas and Tags inputs don't reset on "Clear all".
  - Root cause: `defaultValue` on `<input>` elements makes them uncontrolled; React does not update their displayed text when the parent resets `filters` to `{}`.
  - Completed (iteration 117): Added `useRef` for both text inputs; `handleClearAll` imperatively clears input values via refs before calling `onChange({})`. 1 new test verifies input values are empty after clear.

- [x] TECH-014: Fix EscalationDraftModal stale closure and suppressed eslint-disable.
  - Root cause: `createDraft` and `loadConnectors` were plain functions called inside a `useEffect` with `[open]` dependency array. Stale closure over `sessionId`, `messageId`, `escalation`, `citations` props was hidden by `eslint-disable-line react-hooks/exhaustive-deps`.
  - Completed (iteration 117): Inlined async logic into `useEffect` body, added `cancelled` flag for cleanup, listed all true dependencies `[open, sessionId, messageId, escalation, citations]`. Removed eslint-disable comment.

- [x] TECH-015: Fix GoldDatasetPage missing `role="alert"` on error banners.
  - Root cause: 3 error banner `<div>` elements lacked `role="alert"` — screen readers would not announce errors. All other pages had it.
  - Completed (iteration 117): Added `role="alert"` to all 3 error banners (`cases-error`, `create-error`, `export-error`). 3 existing tests updated with `role` assertion.

- [x] TECH-016: Fix AuthProvider eager MSAL construction with empty clientId.
  - Root cause: `new PublicClientApplication(msalConfig)` ran at module import time even when `VITE_ENTRA_CLIENT_ID` is unset (local dev), causing MSAL warnings/errors.
  - Completed (iteration 117): Lazy-initialized via `getMsalInstance()` — only constructed when `AuthProvider` detects MSAL is configured.

- [x] TECH-017: Fix `FeedbackResponse.type` weak typing (`string` → `FeedbackType`).
  - Root cause: `FeedbackResponse.type` was declared as `string`, requiring an unchecked `as FeedbackType` cast in `FeedbackWidget`.
  - Completed (iteration 117): Changed type to `FeedbackType` in `types.ts`, removed redundant cast in `FeedbackWidget.tsx`.

- [x] TECH-018: Fix FusedRetrievalService dead `AggregateException` catch arm.
  - Root cause: `catch (Exception ex) when (ex is AggregateException or RequestFailedException)` — the `AggregateException` arm is dead code because `await Task.WhenAll` unwraps to the first inner exception. Additionally, non-`RequestFailedException` errors (e.g. `HttpRequestException`) would bypass the partial-result salvage.
  - Completed (iteration 117): Changed to `catch (Exception ex) when (ex is not OperationCanceledException)` — salvages partial results for any non-cancellation failure while still propagating cancellation.

- [x] TECH-019: Add top-level React ErrorBoundary to prevent white-screen on rendering errors.
  - Root cause: No `ErrorBoundary` component existed in the frontend. A rendering error in any component would unmount the entire React tree, showing a blank white page with no recovery path.
  - Completed (iteration 118): Created `ErrorBoundary` class component in `frontend/src/components/ErrorBoundary.tsx` with default fallback UI (`role="alert"`, error message display, reload button) and optional custom `fallback` prop. Wrapped the route tree in `App.tsx` (outside `AuthProvider`, inside `BrowserRouter`). 4 new tests (renders children, catches errors with default UI, supports custom fallback, no false positives). 421 frontend tests passing.

- [x] TECH-020: Frontend accessibility hardening — aria-labels, semantic HTML, ARIA live regions.
  - Root cause: Multiple tables lacked `aria-label`, form inputs relied on `placeholder` instead of accessible labels, clickable `<span>` elements in FieldMappingEditor were not keyboard-accessible, and dynamic status messages (FeedbackWidget) lacked ARIA live regions.
  - Completed (iteration 119): Added `aria-label` to ~20 data tables across 12 files (ConnectorList, SyncRunHistory, PatternList, FieldMappingEditor, ConnectorDetail, DiagnosticsPage, AuditCompliancePage, RoutingAnalyticsPage, CostControlsPage, GoldDatasetPage, PlaybooksPage, PrivacyAdminPage). Converted 4 clickable `<span>` elements to `<button type="button">` in FieldMappingEditor for proper keyboard/screen-reader accessibility. Added `aria-label` to ~20 form inputs across admin pages (AuditCompliancePage filters, RoutingAnalyticsPage rule form, SynonymManagementPage create forms, PrivacyAdminPage retention/deletion forms, ConnectorDetail retrieval test, FeedbackWidget textareas). Added `role="alert"` to FeedbackWidget error messages and `aria-live="polite"` to success confirmations. 418 frontend tests passing; 0 TypeScript errors; build clean.

- [x] TECH-021: Narrow remaining broad `catch (Exception ex)` blocks to propagate cancellation.
  - Root cause: TECH-009 (iteration 113-114) narrowed all bare `catch` blocks to typed exceptions, but subsequent P3 iterations introduced ~55 new `catch (Exception ex)` blocks without `when (ex is not OperationCanceledException)` guards. These silently swallow `OperationCanceledException`, preventing request cancellation and graceful shutdown from propagating through async pipelines.
  - Completed (iteration 120): Added `when (ex is not OperationCanceledException)` guard to 55 `catch (Exception ex)` blocks across 27 files: ChatOrchestrator (6), HubSpotConnectorClient (4), SharePointConnectorClient (7), AzureDevOpsConnectorClient (5), ClickUpConnectorClient (5), SyncJobProcessor (2), NormalizationPipeline (1), EvalRunner (1), Program.cs (3), SecretRotationService (2), ConnectorAdminService (3), PatternDistillationService (3), webhook handlers (4), webhook managers (2), OAuthTokenService (2), AzureSearchRetrievalService (1), TextExtractionService (1), EscalationDraftService (1), PatternGovernanceService (1), EvalReportService (1), IndexMigrationService (1), DataSubjectDeletionService (1). 5 catches correctly left unchanged (preceded by explicit `catch (OperationCanceledException)` blocks or top-level CLI handler). 3 new cancellation propagation tests (ChatOrchestratorIntegrationTests: embedding/retrieval/generation cancellation). Zero bare or unguarded broad catches remaining in async production code paths.

- [x] TECH-022: Fix setTimeout memory leaks in SourceViewerPanel and EscalationDraftModal.
  - Root cause: Both components used `setTimeout(() => setState(false), 2000)` in event handlers for copy-feedback UX without tracking the timer ID. If the component unmounts before the 2s timer fires, React would attempt a state update on an unmounted component, causing a memory leak and potential console warning.
  - Completed (iteration 121): Added `useRef<ReturnType<typeof setTimeout> | null>` to both components to track timer IDs. Added unmount cleanup via `useEffect(() => () => clearTimeout(ref.current), [])`. Timer refs are also cleared before setting new timers to prevent stacking on rapid clicks. 2 new tests verify unmount-after-copy doesn't cause errors. 420 frontend tests passing; build clean.

- [x] TECH-024: Guard remaining unguarded `catch (Exception)` blocks + frontend accessibility hardening.
  - Root cause: Code quality scan found 2 `catch (Exception)` blocks in async production code without `when (ex is not OperationCanceledException)` guards (SyncJobProcessor rate-limit recording, IndexMigrationService index cleanup). Also found 4 interactive elements in SourceViewerPanel and EvidenceDrawer lacking `aria-label` attributes (icon-containing back buttons, "View content" buttons, and "Open external" links opening in new tabs).
  - Completed (iteration 123): Added `when (ex is not OperationCanceledException)` guard to `SyncJobProcessor.cs` (rate-limit event recording) and `IndexMigrationService.cs` (index cleanup after migration failure). Added `aria-label="Back to citations"` to 3 back buttons in SourceViewerPanel (loading, error, and content states). Added `aria-label="Open external source (opens in new tab)"` to external link in SourceViewerPanel. Added `aria-label="View content for {title}"` to View content buttons in EvidenceDrawer. Added `aria-label="Open external source for {title} (opens in new tab)"` to external links in EvidenceDrawer. Updated 4 existing frontend tests with aria-label assertions.

- [x] TECH-025: Document Source Viewer feature in chat page docs.
  - Root cause: P3-025 implemented the `SourceViewerPanel` component and `GET /api/evidence/{chunkId}/content` endpoint, but the Evidence Drawer section in `docs/pages/chat.md` was not updated to describe the "View content" drill-down, inline source viewer, copy-citation, or back-to-citations navigation. API reference already had the endpoint listed.
  - Completed (iteration 124): Added Source Viewer subsection to Evidence Drawer in `docs/pages/chat.md` describing full feature set (view content button, full chunk text, metadata badges, tag chips, copy citation link, open external, back navigation, ACL enforcement). Added `GET /api/evidence/{chunkId}/content` to chat page API endpoints list.

- [x] TECH-026: Add missing aria-labels to remaining interactive elements.
  - Root cause: Code quality scan found ~25 interactive elements across 5 frontend components lacking `aria-label` attributes: PatternList pagination buttons, RetrievalFilterPanel toggle/chip/clear buttons, CreateConnectorForm wizard buttons and form inputs, FieldMappingEditor cell edit buttons and add/remove buttons, ChatThread citation count and escalation draft buttons.
  - Completed (iteration 125): Added `aria-label` to 2 pagination buttons in PatternList. Added `aria-label`, `aria-expanded`, and `aria-pressed` to RetrievalFilterPanel toggle button (with expanded state), 5 source type chip buttons (with toggle state), and clear button. Added `aria-label` to 8 form inputs/selects and 4 wizard action buttons in CreateConnectorForm. Added `aria-label` to add rule button, 4 cell edit buttons per row (source, target, transform, routing tag), and remove button in FieldMappingEditor. Added `aria-label` to citation count button (with dynamic source count) and escalation draft button (with target team) in ChatThread. 8 new test assertions across 5 test files. 428 frontend tests passing; 0 TypeScript errors; build clean.

- [x] TECH-027: Log silent catch blocks in Program.cs + final frontend aria-label pass.
  - Root cause: Code quality scan found 2 `catch (InvalidOperationException)` blocks in `Program.cs` (secrets status and diagnostics summary endpoints) that silently swallowed exceptions without any logging, making it impossible to diagnose OpenAI key misconfiguration in production. Also found ~15 interactive elements across 4 frontend components (SessionSidebar, PatternDetailView, ConnectorDetail, ConnectorList) lacking `aria-label` attributes.
  - Completed (iteration 126): Added `ILogger<Program>` injection to secrets status endpoint and `sp.GetRequiredService<ILogger<Program>>()` in diagnostics summary endpoint; both now `LogWarning` the `InvalidOperationException` with context message. Frontend: added `aria-label="Create new session"` and `aria-label="Open session {title}"` to SessionSidebar buttons. Added `aria-label` to 10 elements in PatternDetailView (back, review, approve, deprecate, notes input, deprecation reason, superseding ID, cancel deprecation). Added `aria-label="Back to connector list"` and `aria-label="Cancel delete"` to ConnectorDetail. Added `aria-label="Create new connector"` to ConnectorList. 5 new frontend tests across 4 test files. 433 frontend tests passing; 0 TypeScript errors; build clean.

- [x] TECH-028: Fix FieldMappingEditor `editIndex` drift on row deletion.
  - Root cause: `removeRule` only cleared `editIndex` when deleting the currently edited row. When deleting a row at an index lower than `editIndex`, the edit cursor silently shifted to the next row (wrong rule), because array indices shift downward but `editIndex` was not adjusted.
  - Completed (iteration 127): Added `else if (editIndex !== null && index < editIndex) setEditIndex(editIndex - 1)` to `removeRule`. 1 new test verifies edit cursor stays on the correct rule (rule C) after removing an earlier row (rule A). 434 frontend tests passing; build clean.

- [x] TECH-029: Fix ChatOrchestrator parent cancellation masking + frontend aria-labels on remove buttons.
  - Root cause: ChatOrchestrator's classification and summarization catch blocks used linked `CancellationTokenSource` but caught `OperationCanceledException` without distinguishing parent cancellation (request aborted) from timeout cancellation (classification/summarization exceeded budget). Parent cancellation was silently swallowed and logged as a timeout warning, causing the orchestration to continue instead of aborting. Also, PlaybooksPage checklist remove buttons and PrivacyAdminPage custom pattern remove buttons rendered only `×` with no `aria-label`, making them inaccessible to screen readers. PlaybooksPage checklist input lacked `aria-label`.
  - Completed (iteration 128): Added `catch (OperationCanceledException) when (cancellationToken.IsCancellationRequested) { throw; }` before the existing timeout catch in both classification (line 116) and summarization (line 297) blocks — parent cancellation now propagates correctly while timeout cancellation is still handled gracefully. Added `aria-label="Remove checklist item: {item}"` to PlaybooksPage remove button. Added `aria-label="New checklist item"` to PlaybooksPage checklist input. Added `aria-label="Remove custom pattern: {name}"` to PrivacyAdminPage remove button. 2 new backend tests (classification/summarization parent cancellation propagation with stub services). 2 new frontend tests (PlaybooksPage checklist aria-labels, PrivacyAdminPage custom pattern remove aria-label). 436 frontend tests passing; build clean.

- [x] TECH-030: Log silent catch in WebhookEvalNotificationService + aria-labels on 6 unlabeled select elements.
  - Root cause: `WebhookEvalNotificationService.cs` in Eval.Cli had a `catch (Exception) when (!ct.IsCancellationRequested)` block that silently swallowed exceptions without logging, while its Contracts counterpart (`WebhookEvalNotificationClient`) properly logged with `_logger.LogWarning`. Also, 6 `<select>` elements across 5 frontend files (PlaybooksPage, PrivacyAdminPage, RoutingAnalyticsPage, SynonymManagementPage, FieldMappingEditor) lacked `aria-label` attributes, making them inaccessible to screen readers.
  - Completed (iteration 129): Added `ILogger<WebhookEvalNotificationService>` with `NullLogger` fallback to `WebhookEvalNotificationService`; catch block now logs `LogWarning(ex, ...)` with RunId context. Added `aria-label="Minimum severity"` to PlaybooksPage and RoutingAnalyticsPage severity selects. Added `aria-label="Enforcement mode"` to PrivacyAdminPage. Added `aria-label="Filter by category"` to SynonymManagementPage. Added dynamic `aria-label` to FieldMappingEditor transform type and routing tag selects (`"Transform type for rule {n}"`, `"Routing tag for rule {n}"`). 6 new test assertions across 5 test files + 1 new test (RoutingAnalyticsPage create rule form). 437 frontend tests passing; build clean.

- [x] TECH-031: Log silent CrontabException catch in ScheduledSyncService + aria-labels on 7 unlabeled interactive elements.
  - Root cause: `ScheduledSyncService.IsDue` had a `catch (CrontabException)` block that silently returned `false` without any logging, making it impossible to diagnose invalid cron expressions in production. Also, 4 buttons (AuthProvider Sign In, MessageInput Send, PatternDetailView Confirm Deprecation, ErrorBoundary Reload page) and 3 select elements (RoutingAnalyticsPage window period, CostControlsPage usage period, GoldDatasetPage response type) lacked `aria-label` attributes, making them inaccessible to screen readers.
  - Completed (iteration 130): Added optional `ILogger? logger = null` parameter to `ScheduledSyncService.IsDue` (same pattern as TECH-023 `ParseSourceConfig`); internal call site passes `_logger`, test callers use default (null). Catch block now logs `LogWarning(ex, ...)` with cron expression and connector ID context. Added `aria-label="Sign in with your organization account"` to AuthProvider. Added dynamic `aria-label` to MessageInput Send button (`"Send message"` / `"Thinking"`). Added `aria-label="Confirm pattern deprecation"` to PatternDetailView. Added `aria-label="Reload page"` to ErrorBoundary. Added `aria-label="Analytics window period"` to RoutingAnalyticsPage. Added `aria-label="Usage period"` to CostControlsPage. Added `aria-label="Expected response type"` to GoldDatasetPage. 9 new/updated test assertions across 6 test files (AuthProvider sign-in button, MessageInput send/thinking labels, PatternDetailView confirm deprecation, RoutingAnalyticsPage window select, CostControlsPage period select, GoldDatasetPage response type select). 446 frontend tests passing; build clean.

- [x] TECH-032: Add aria-labels to 4 unlabeled buttons + update docs for admin-connectors and pattern-governance.
  - Root cause: Code quality scan found 4 interactive buttons missing `aria-label` attributes: 3 in `EscalationDraftModal.tsx` (Save draft, Copy as Markdown, Create external work item) and 1 in `SourceViewerPanel.tsx` (Copy citation link). Also, `docs/pages/admin-connectors.md` was missing documentation for the preview-retrieval feature (P3-027) and `docs/pages/pattern-governance.md` was missing sections for usage metrics (P3-012), version history (P3-013), contradiction detection (P2-004), and maintenance tasks (P2-004).
  - Completed (iteration 131): Added `aria-label` to Save draft (`"Save escalation draft"` / `"Saving escalation draft"`), Copy as Markdown (`"Copy escalation draft as Markdown"`), Create external (`"Create external work item from escalation draft"`) in EscalationDraftModal. Added `aria-label="Copy citation link to clipboard"` to SourceViewerPanel. Updated `admin-connectors.md` with Preview and Retrieval Testing section + 2 new API endpoints. Updated `pattern-governance.md` with Usage Metrics, Version History, Contradiction Detection, and Maintenance Tasks sections + 10 new API endpoints. 4 new/updated test assertions across 2 test files (EscalationDraftModal save/copy-markdown/create-external labels, SourceViewerPanel copy-citation label). 442 frontend tests passing; build clean.

- [x] TECH-033: Log 2 silent catch blocks + add aria-labels to tab buttons and action buttons across 8 components.
  - Root cause: Code quality scan found 2 silent catch blocks without logging: `Program.cs` evidence content endpoint silently swallowed blob download failures (`catch (Exception ex)` with comment-only), and `PiiRedactionService.cs` silently swallowed `RegexMatchTimeoutException` on custom PII patterns with no diagnostic trace. Also found 15 tab buttons across 6 pages (DiagnosticsPage, AuditCompliancePage, CostControlsPage, GoldDatasetPage, PrivacyAdminPage, RoutingAnalyticsPage) lacking `aria-label`, plus 8 action buttons in ConnectorDetail (Test Connection, Enable/Disable, Sync Now, Backfill, Preview, Edit, Delete, Confirm Delete) and the FeedbackWidget Submit button.
  - Completed (iteration 132): Added `ILogger<Program>` to evidence content endpoint and `logger.LogWarning(ex, ...)` with blob path context. Added optional `ILogger<PiiRedactionService>? logger = null` constructor to PiiRedactionService (same pattern as TECH-023) and `_logger?.LogWarning(ex, ...)` with pattern name context. Added `aria-label` to 15 tab buttons across 6 pages. Added dynamic `aria-label` to 8 ConnectorDetail action buttons (with state-aware labels for Test/Toggle/Sync/Preview). Added dynamic `aria-label` to FeedbackWidget submit button. 14 new/updated test assertions across 8 test files. 448 frontend tests passing; build clean.

- [x] TECH-034: Log silent FormatException in AdoWebhookHandler + aria-labels on ~25 elements across 5 components.
  - Root cause: Code quality scan found 1 silent `catch (FormatException)` block in `AdoWebhookHandler.ValidateSignature` that returned `false` without logging, making it impossible to diagnose malformed Basic auth headers from ADO service hooks. Also found ~25 interactive elements across 5 frontend components (OutcomeWidget, SynonymManagementPage, DiagnosticsPage, PlaybooksPage) lacking `aria-label` attributes.
  - Completed (iteration 133): Added optional `ILogger? logger = null` parameter to `AdoWebhookHandler.ValidateSignature` (same pattern as TECH-023); internal call site passes `_logger`, test callers use default (null). Catch block now logs `LogWarning(ex, ...)`. Frontend: added `aria-label` to OutcomeWidget submit button (dynamic: "Record session outcome" / "Recording outcome") and target team input. Added `aria-label` to SynonymManagementPage 3 tab buttons + 17 action buttons across all 3 sub-tabs (Synonyms: Sync/Seed/Add/Create/Save/Cancel/Edit/Delete/Toggle; Stop Words: Seed/Add/Create/Toggle/Delete; Special Tokens: Seed/Add/Create/Toggle/Delete). Added dynamic `aria-label` to DiagnosticsPage dead-letter expand/hide toggle. Added `aria-label` to PlaybooksPage detail view Back/Edit/Delete buttons. 1 new backend test (signature validation with logger). 5 new/updated frontend test assertions across 4 test files (OutcomeWidget aria-labels, SynonymManagementPage tab labels, DiagnosticsPage expand button labels, PlaybooksPage detail button labels).

- [x] TECH-035: Fix TS build errors in RoutingAnalyticsPage.test.tsx + log 3 silent FormatException catches + aria-labels on 2 SourceConfigEditor buttons.
  - Root cause: (a) `RoutingAnalyticsPage.test.tsx` had 3 TypeScript errors: `listRoutingRules` and `listRoutingRecommendations` mocks returned `[]` instead of `{ rules: [], totalCount: 0 }` / `{ recommendations: [], totalCount: 0 }`; `getRoutingAnalytics` mock used non-existent `acceptedCount` property instead of correct `RoutingAnalyticsSummary` shape. This broke `tsc -b` and the full frontend build. (b) 3 silent `catch (FormatException)` blocks in `ClickUpWebhookManager.ValidateSignature`, `HubSpotWebhookManager.ValidateSignature`, and `OAuthTokenService.ValidateState` returned `false` without logging. (c) 2 buttons in `SourceConfigEditor` ("Switch to form", "Edit as JSON") lacked `aria-label` attributes.
  - Completed (iteration 134): Fixed all 3 TS errors with correct mock shapes. Added optional `ILogger? logger = null` parameter to ClickUp/HubSpot `ValidateSignature` (same pattern as ADO TECH-034); call sites in webhook handlers pass `_logger`. OAuthTokenService catch block uses existing `_logger` instance. Added `aria-label` to both SourceConfigEditor toggle buttons. 450 frontend tests passing; TypeScript clean; 0 new silent catch blocks.

- [x] TECH-036: Log 12 silent catch blocks across connector clients, ConnectorAdminService, and SyncJobProcessor.
  - Root cause: 12 catch blocks caught typed exceptions but silently returned error results without logging. Adjacent catch blocks in the same files DO log, creating an inconsistency that makes production debugging harder. Affected files: SharePointConnectorClient (3: TestConnection, token acquisition, delta query), HubSpotConnectorClient (3: TestConnection, 2 object mapping loops), ClickUpConnectorClient (2: TestConnection, task mapping loop), AzureDevOpsConnectorClient (1: TestConnection), ConnectorAdminService (2: OAuth resolution, secret retrieval), SyncJobProcessor (1: credential retrieval).
  - Completed (iteration 135): Added `_logger.LogWarning(ex, ...)` with structured parameters (ConnectorId, SyncRunId, DriveName, ObjectType, TaskId) to all 12 catch blocks. Zero silent catch blocks remaining in production backend code.

- [x] TECH-037: Add aria-labels to SourceConfigEditor form inputs.
  - Root cause: TagInput component and standalone text/number inputs in all 4 connector source config forms (ADO, SharePoint, HubSpot, ClickUp) used sibling `<label>` elements without `htmlFor`/`id` association, making inputs inaccessible to screen readers. Raw JSON textarea also lacked an aria-label.
  - Completed (iteration 136): Added `ariaLabel` prop to TagInput component. Added `aria-label` to all ~25 inputs across 4 forms (Organization URL, Projects, Work Item Types, Area Paths, Batch Size, Site URL, Entra ID Tenant ID, Client ID, Drive IDs, Include Extensions, Exclude Folders, Portal ID, Object Types, Pipelines, Custom Properties, Workspace ID, Space IDs, Folder IDs, List IDs, Task Statuses) and the raw JSON textarea. 5 new tests verify aria-labels on all 4 form types + JSON mode. 455 frontend tests passing.

- [x] TECH-038: Log silent catch in EvalCliRunner + aria-labels on ~35 elements across 10 frontend components.
  - Root cause: Code quality scan found 1 silent `catch (Exception ex)` block in `EvalCliRunner.SendNotificationAsync` that incremented a diagnostics counter without logging the exception, making eval notification failures impossible to diagnose. Also found ~35 interactive elements across 10 frontend components (ConnectorDetail, FieldMappingEditor, GoldDatasetPage, RoutingAnalyticsPage, PrivacyAdminPage, CostControlsPage, PlaybooksPage, AuditCompliancePage, ChatPage) lacking `aria-label` attributes — form inputs, pagination buttons, CRUD action buttons, toggle chips, and filter controls.
  - Completed (iteration 137): Added optional `ILogger? logger = null` parameter to `EvalCliRunner.SendNotificationAsync` (same pattern as TECH-023); catch block now logs `LogWarning(ex, ...)` with RunId context. Frontend: added `aria-label` to ConnectorDetail edit form inputs (name, schedule) + save/cancel/retrieval test buttons. Added `aria-label` to FieldMappingEditor isRequired checkbox. Added `aria-label` to GoldDatasetPage filter input, 2 pagination buttons, delete/close detail buttons. Added `aria-label` to RoutingAnalyticsPage refresh, new rule, create/cancel, save/cancel edit, edit/delete per-rule, apply/dismiss recommendation buttons. Added `aria-label` to PrivacyAdminPage 3 custom pattern inputs, add/save/cancel PII buttons, edit/reset policy buttons, add/run/save/cancel retention buttons, per-row delete button. Added `aria-label` to CostControlsPage 7 form inputs, 3 refresh buttons, save/cancel/edit/reset buttons. Added `aria-label` to PlaybooksPage team name, description, contact channel, max concurrent, fallback team inputs + toggle chips with `aria-pressed` + add checklist + create/save/cancel buttons. Added `aria-label` to AuditCompliancePage 2 pagination buttons. Added `aria-label` to ChatPage empty-state button. 13 new/updated test assertions across 6 test files. 455 frontend tests passing; TypeScript clean; build clean.

- [x] TECH-039: Add aria-labels to 3 remaining unlabeled action buttons.
  - Root cause: Code quality scan found 3 action buttons missing `aria-label` attributes: RoutingAnalyticsPage "Generate Recommendations" button, PrivacyAdminPage "Submit Deletion Request" button, and PlaybooksPage "New Playbook" button.
  - Completed (iteration 138): Added `aria-label="Generate routing recommendations"` to RoutingAnalyticsPage. Added `aria-label="Submit data subject deletion request"` to PrivacyAdminPage. Added `aria-label="Create new playbook"` to PlaybooksPage. 3 new test assertions across 3 test files (RoutingAnalyticsPage, PrivacyAdminPage, PlaybooksPage). 457 frontend tests passing; TypeScript clean; build clean.

- [x] TECH-040: Log silent frontend `.catch()` blocks + extract shared `downloadFile` utility.
  - Root cause: 4 `.catch()` blocks in `useRoles`, `PatternDetailView` (×2), and `SourceViewerPanel` silently swallowed errors with no diagnostic trace. 2 pages (`GoldDatasetPage`, `AuditCompliancePage`) had identical 7-line DOM download sequences (createElement, appendChild, click, removeChild, revokeObjectURL).
  - Completed (iteration 139): Added `console.warn` with component tag and error to all 4 silent `.catch()` blocks. Created `frontend/src/utils/downloadFile.ts` shared utility. Replaced duplicate download logic in both pages with `downloadFile()` call. 3 new `downloadFile` unit tests (`downloadFile.test.ts`). Existing page export tests pass unchanged (mock at `URL.createObjectURL` level).

- [x] TECH-041: Add aria-labels to 5 remaining unlabeled buttons + fix downloadFile.test.ts TypeScript errors.
  - Root cause: Code quality scan found 5 action buttons missing `aria-label` attributes: GoldDatasetPage "Create Gold Case" and "Download JSONL Export" buttons, AuditCompliancePage "Download NDJSON Export" button, and EscalationDraftModal 2 disabled fallback buttons (Create ADO work item, Create ClickUp task) that relied on `title` attribute only. Also, `downloadFile.test.ts` (added in TECH-040) had 3 TypeScript errors from `vi.spyOn` type inference on `appendChild`/`removeChild`/`createObjectURL`.
  - Completed (iteration 140): Added dynamic `aria-label` to GoldDatasetPage create button (`"Create gold case"` / `"Creating gold case"`), export button (`"Download JSONL export"` / `"Exporting gold cases"`). Added dynamic `aria-label` to AuditCompliancePage export button (`"Download NDJSON export"` / `"Exporting audit events"`). Added `aria-label` to EscalationDraftModal disabled fallback buttons (`"Create ADO work item (no connectors configured)"`, `"Create ClickUp task (no connectors configured)"`). Fixed `downloadFile.test.ts` TS errors by removing typed spy variables and using `vi.fn()` with `as unknown as typeof` casts. 5 new test assertions across 3 test files. 460 frontend tests passing; TypeScript clean; build clean.

- [x] TECH-042: Add aria-labels to 6 remaining unlabeled interactive elements across 2 frontend components.
  - Root cause: Code quality scan found 6 interactive elements missing accessibility attributes: PrivacyAdminPage had PII type chip buttons (4 `<button>` elements in the "Enabled PII Types" section) without `aria-label` or `aria-pressed`, and the compliance tab Refresh button without `aria-label`. SynonymManagementPage had 4 inputs in the special token create form (token, category, boost factor, description) without `aria-label` attributes, while sibling synonym and stop-word forms had them.
  - Completed (iteration 141): Added `aria-label={`Toggle PII type: ${type}`}` and `aria-pressed` to all 4 PII type chip buttons in PrivacyAdminPage. Added `aria-label="Refresh compliance report"` to compliance tab refresh button. Added `aria-label` to 4 special token form inputs in SynonymManagementPage ("Special token", "Token category", "Token boost factor", "Special token description"). 3 new tests across 2 test files (PII chip aria-labels with aria-pressed, compliance refresh label, special token form input labels). 463 frontend tests passing; TypeScript clean; build clean.

- [x] TECH-043: Add `console.warn` to 6 silent frontend `catch` blocks + add missing `catch` in OutcomeWidget.
  - Root cause: 6 frontend `catch` blocks silently swallowed API errors without any logging: `ConnectorDetail.tsx` (sync runs fetch), `EscalationDraftModal.tsx` (connector list fetch), `GoldDatasetPage.tsx` (gold case detail), `ChatPage.tsx` (session list + message list). Additionally, `OutcomeWidget.tsx` had `try/finally` with no `catch` — errors from `onSubmit` would propagate uncaught.
  - Completed (iteration 142): Added `catch (err) { console.warn('[Component] ...', err); }` to all 6 silent catch blocks. Added `catch` block to `OutcomeWidget.handleSubmit` so submit failures are logged and the widget remains interactive. 3 new tests: OutcomeWidget submit failure (verifies console.warn + no "outcome recorded" + button re-enabled), ConnectorDetail sync run load failure (verifies console.warn), ChatPage listSessions failure (verifies console.warn). 465 frontend tests passing.

- [x] TECH-044: Add aria-label to external creation success link in EscalationDraftModal.
  - Root cause: Code quality scan found 1 `<a>` element in `EscalationDraftModal.tsx` (external work item/task creation success banner) lacking an `aria-label` attribute. The link opens in a new tab via `target="_blank"` but screen readers had no way to announce the link's purpose or destination.
  - Completed (iteration 143): Added dynamic `aria-label={`Open external ${type} ${externalId} (opens in new tab)`}` to the external creation success link, where `type` is "work item" (ADO) or "task" (ClickUp). 1 new test (renders external creation success link with correct aria-label after creating an ADO work item). 466 frontend tests passing; TypeScript clean; build clean.

- [x] TECH-045: Add aria-labels to 3 unlabeled synonym inline-edit inputs in SynonymManagementPage.
  - Root cause: Code quality scan found 3 `<input>` elements in the synonym rule inline-edit form (group name, rule value, description) lacking `aria-label` attributes, while all other inputs across the codebase were properly labeled.
  - Completed (iteration 144): Added `aria-label="Edit synonym group name"`, `aria-label="Edit synonym rule value"`, `aria-label="Edit synonym rule description"` to the 3 inline-edit inputs. 1 new test verifies all 3 aria-labels appear when entering edit mode. 467 frontend tests passing; TypeScript clean; build clean.

- [x] TECH-046: Fix MSAL logger severity mapping + descriptive close-modal aria-label.
  - Root cause: (1) `msalConfig.ts` loggerCallback used `console.debug` for all MSAL log messages regardless of severity level — Error and Warning messages were logged as debug, making them invisible in most browser console filters. (2) `EscalationDraftModal.tsx` close button had generic `aria-label="Close"` — screen readers could not identify what was being closed.
  - Completed (iteration 145): (1) Replaced `console.debug` catch-all with severity-aware mapping: `LogLevel.Error` → `console.error`, `LogLevel.Warning` → `console.warn`, Info/Verbose silenced (no production noise). (2) Changed `aria-label="Close"` to `aria-label="Close escalation draft modal"`. 3 new msalConfig logger tests (error, warning, info/verbose silenced); 1 existing EscalationDraftModal close test updated with aria-label assertion. 471 frontend tests passing.

- [x] TECH-047: Add aria-labels to 2 unlabeled select elements in EscalationDraftModal + fix SynonymManagementPage test TS error.
  - Root cause: Code quality scan found 2 `<select>` elements in `EscalationDraftModal.tsx` (severity selector and connector selector) missing `aria-label` attributes, while all other selects across the codebase were properly labeled. Also, `SynonymManagementPage.test.tsx` had a pre-existing TypeScript error — mock `SynonymRuleResponse` was missing `tenantId`, `createdBy`, and `updatedBy` fields.
  - Completed (iteration 146): Added `aria-label="Escalation severity"` to severity select. Added `aria-label="Select connector for external work item creation"` to connector select. Fixed SynonymManagementPage test mock with missing fields. 2 new test assertions in existing EscalationDraftModal tests. 470 frontend tests passing; TypeScript clean; build clean.

- [x] TECH-048: Add aria-labels to 5 clickable table rows missing accessible labels.
  - Root cause: Code quality scan found 5 `<tr>` elements with `onClick` handlers but no `aria-label` attribute — screen readers could not announce what clicking each row does. Affected components: PlaybooksPage (playbook rows), GoldDatasetPage (gold case rows), AuditCompliancePage (audit event rows), PatternList (pattern rows), ConnectorList (connector rows).
  - Completed (iteration 147): Added descriptive `aria-label` to all 5 clickable rows: `"Open playbook for team {teamName}"`, `"Open gold case {caseId}"`, `"Toggle details for {eventType} event"`, `"Open pattern {title}"`, `"Open connector {name}"`. 5 new test assertions in existing test files verify aria-labels are rendered. 470 frontend tests passing; TypeScript clean; build clean.

- [x] TECH-049: Add unit tests for ConnectorAdminService (865 lines, 13 public methods, no dedicated tests).
  - Root cause: ConnectorAdminService is the largest untested service in the codebase — 865 lines of critical connector lifecycle logic (CRUD, enable/disable with field mapping validation, test connection, sync-now with checkpoint propagation, preview, preview retrieval, field mapping validation) had no dedicated unit tests. Only integration-level endpoint tests existed, which test HTTP layer but not service-level edge cases.
  - Completed (iteration 148): Created `ConnectorAdminServiceTests.cs` in `SmartKb.Api.Tests/Connectors/` with 41 unit tests covering all 13 public methods: Create (5 tests: valid request, empty name, name too long, duplicate name, audit event), List (3 tests: empty, populated, tenant isolation), Get (3 tests: existing, non-existent, wrong tenant), Update (4 tests: name change, duplicate name, non-existent, audit event), Delete (3 tests: soft-delete, non-existent, audit event), Enable (4 tests: valid mapping, no mapping, missing required fields, non-existent), Disable (3 tests: status change, non-existent, audit event), TestConnection (3 tests: no client, non-existent, with stub client), SyncNow (3 tests: publish message, non-existent, incremental with checkpoint), ListSyncRuns (2 tests: populated, non-existent connector), GetSyncRun (2 tests: existing, non-existent), ValidateFieldMapping (6 tests: null, empty rules, valid, empty source field, regex without expression, missing field analysis). Uses SQLite in-memory DB with InMemoryAuditEventWriter, InMemorySecretProvider, TestSyncJobPublisher, and StubConnectorClient.

- [x] TECH-050: Add unit tests for SessionService covering 8 untested code paths.
  - Root cause: SessionService (334 lines, 6 public methods) had only 14 tests with at least 8 untested code paths: expired session rejection, message limit enforcement, no-orchestrator fallback, long query auto-title truncation, CustomerRef round-trip, GetMessages null for missing session, tenant isolation in ListSessions, and malformed citation JSON handling.
  - Completed (iteration 149): Added 8 new tests to `SessionServiceTests.cs`: `SendMessage_ReturnsNull_WhenSessionExpired` (force-expire session, verify null return), `SendMessage_ReturnsNull_WhenMessageLimitReached` (MaxMessagesPerSession=2, verify rejection after limit), `SendMessage_FallbackResponse_WhenNoOrchestrator` (null orchestrator path, verify degraded response), `SendMessage_AutoTitle_TruncatesLongQuery` (150-char query → 103-char title with "..."), `CreateSession_PersistsCustomerRef` (round-trip through create + get), `GetMessages_ReturnsNull_WhenSessionNotFound` (unknown session ID), `ListSessions_ExcludesOtherTenants` (cross-tenant isolation with seeded second tenant), `DeserializeCitations_ReturnsNull_ForMalformedJson` (corrupt CitationsJson in DB, verify graceful null return). SessionService now has 22 tests covering all 6 public methods and all significant branching logic.

- [x] TECH-051: Add 16 unit tests for WebhookStatusService + log 6 silent catch blocks in Program.cs.
  - Root cause: WebhookStatusService (117 lines, 3 public methods: GetByConnectorAsync, GetAllAsync, GetDiagnosticsSummaryAsync) was the last untested service in the codebase — no dedicated unit tests existed. Additionally, 6 catch blocks in Program.cs (4 `InvalidOperationException` in feedback/outcome/escalation draft/external escalation endpoints, 2 `ArgumentException` in PII policy/retention policy endpoints) returned error responses without any logging, making production debugging impossible.
  - Completed (iteration 150): Created `WebhookStatusServiceTests.cs` in `SmartKb.Data.Tests/` with 16 unit tests covering all 3 public methods: GetByConnectorAsync (5 tests: empty, ordered by event type, excludes other connectors, tenant isolation, counts active/fallback), GetAllAsync (3 tests: empty, ordered by connector name then event type, tenant isolation), GetDiagnosticsSummaryAsync (6 tests: empty, enabled/disabled counts, webhook state counts, connector health with last sync, null last sync, tenant isolation, service flags default false), field mapping (1 test: all fields mapped correctly). Added `ILogger<Program>` injection and `logger.LogWarning(ex, ...)` to all 6 silent catch blocks with structured parameters (SessionId, MessageId, DraftId, TenantId). Zero silent catch blocks remaining in production API endpoint handlers.

- [x] TECH-052: Expand TokenUsageService test coverage for untested code paths.
  - Root cause: TokenUsageService (253 lines, 4 public methods) had only 9 tests covering basic happy paths. Missing coverage for: tenant isolation across all 3 query methods, budget utilization percentage calculation in GetSummary, monthly budget warning message in CheckBudget, interaction when daily budget OK but monthly exceeded, global default alert threshold fallback from CostOptimizationSettings, empty daily breakdown, and multi-record aggregation with cache hit tracking.
  - Completed (iteration 151): Added 10 new tests to `TokenUsageServiceTests.cs`: `GetSummary_ExcludesOtherTenantData` (cross-tenant isolation), `GetDailyBreakdown_ExcludesOtherTenantData` (cross-tenant isolation), `CheckBudget_ExcludesOtherTenantUsage` (cross-tenant isolation), `GetSummary_IncludesBudgetUtilizationPercents` (verifies daily 50% and monthly 5% with configured budgets), `GetSummary_NoBudgetConfigured_UtilizationZero` (null budgets → 0% utilization), `CheckBudget_MonthlyApproachingThreshold_WarnsWithMonthlyMessage` (monthly warning path), `CheckBudget_DailyOkButMonthlyExceeded_DeniesWithMonthlyReason` (both budgets, daily passes, monthly denies), `CheckBudget_UsesGlobalDefaultAlertThreshold_WhenTenantSettingNull` (CostOptimizationSettings fallback at 50%), `GetDailyBreakdown_EmptyPeriod_ReturnsEmptyList`, `RecordUsage_MultipleRecords_AllPersisted` (3 records with cache hit tracking). TokenUsageService now has 19 tests covering all 4 public methods and all significant branching logic.

- [x] TECH-053: Add unit tests for AzureSearchPatternIndexingService (Pattern index schema, document mapping, edge cases).
  - Root cause: AzureSearchPatternIndexingService (237 lines, 4 public methods + 1 static helper) was the last untested indexing service — the Evidence index counterpart (AzureSearchIndexingService) had 17 tests but the Pattern index service had zero dedicated tests. The service manages the Case-Pattern Store schema, vector/semantic config, and document mapping for the two-store architecture.
  - Completed (iteration 152): Created `AzureSearchPatternIndexingServiceTests.cs` in `SmartKb.Contracts.Tests/` with 26 unit tests covering all testable methods: BuildIndexDefinition (15 tests: correct name, all 18 expected fields, PatternId is key+filterable, Title is searchable+filterable+sortable, vector search profile, HNSW algorithm with cosine metric and M=4/efConstruction=400/efSearch=500, semantic configuration with title+content+keywords fields, TenantId filterable, ACL fields filterable, TrustLevel facetable, Confidence filterable+sortable, Tags facetable), ToSearchDocument (9 tests: all field mapping, tags as collection, allowed groups as collection, null root cause, null product area, empty symptoms, empty resolution steps, confidence cast to double, TrustLevel enum-to-string for all 4 values), IndexPatternsAsync (1 test: empty input returns zero), DeletePatternsAsync (1 test: empty input returns zero). Follows same test pattern as existing AzureSearchIndexingServiceTests.

- [x] TECH-054: Add unit tests for 3 untested services + fix unhandled JsonException in OpenAI services.
  - Root cause: (1) `OpenAiQueryClassificationService.ClassifyAsync` and `OpenAiSessionSummarizationService.SummarizeAsync` had no unit tests for their HTTP interaction paths (success parsing, API error fallback, malformed JSON, empty response, cancellation). (2) `InMemoryAuditEventQueryService` (114 lines, 2 public methods) had zero dedicated unit tests despite implementing query filtering, pagination, cursor-based export, and tenant isolation. (3) Both OpenAI services had an unhandled `JsonException` path — when the model returned malformed JSON in the `content` field, `JsonSerializer.Deserialize` would throw an unhandled exception instead of falling back gracefully.
  - Completed (iteration 153): Added `try/catch (JsonException ex)` with `_logger.LogWarning` to both `ClassifyAsync` and `SummarizeAsync` deserialization paths — malformed model responses now fall back gracefully (to `ClassificationResult.Empty` and `null` respectively) instead of throwing. Created 7 new `ClassifyAsyncTests` (success parsing, API error, empty content, malformed JSON, no choices, with session history, cancellation). Created 8 new `SummarizeAsyncTests` (success parsing, empty list guard, API error, empty content, malformed JSON, no choices, cancellation, minimal summary). Created `InMemoryAuditEventQueryServiceTests.cs` with 21 new tests: QueryAsync (13 tests: empty store, tenant isolation, filter by event type/actor/correlation/date range, pagination first/last page, page size clamped max/min, ordering by timestamp descending, combined filters, field mapping) and ExportAsync (8 tests: empty store, tenant isolation, filter by event type/actor/date range, cursor after timestamp, limit clamped max/min). 36 new tests total.

- [x] TECH-055: Log silent exception in EvalRunner.RunCaseAsync catch block.
  - Root cause: `EvalRunner.RunCaseAsync` caught `Exception ex` and only stored `ex.Message` in the `EvalResult.Error` field without any logging. Full exception details (stack trace, inner exceptions) were lost, making it impossible to diagnose evaluation failures in production or CI.
  - Completed (iteration 154): Added optional `ILogger<EvalRunner>? logger = null` constructor parameter with `NullLogger<EvalRunner>.Instance` fallback (same pattern as TECH-023). Added `_logger.LogWarning(ex, ...)` with CaseId and DurationMs structured parameters to the catch block. Existing call site in `EvalCliRunner` uses default (null logger) — backward-compatible. 3 new tests in `EvalRunnerTests.cs`: `RunAsync_OrchestratorThrows_LogsWarningWithCaseId` (verifies LogLevel.Warning and CaseId in message via CapturingLogger), `RunAsync_OrchestratorThrows_CapturesDurationMs` (verifies DurationMs >= 0), `RunAsync_OrchestratorThrowsOperationCanceled_Propagates` (verifies OperationCanceledException propagates through `when` guard). Zero silent catch blocks remaining in backend production code.

- [x] TECH-056: Add unit tests for HubSpotWebhookHandler and ClickUpWebhookHandler.
  - Root cause: Both handlers had endpoint integration tests only (via `HubSpotWebhookEndpointTests` and `ClickUpWebhookEndpointTests`) but no dedicated handler-level unit tests — unlike `AdoWebhookHandler` (11 tests) and `SharePointWebhookHandler` (13 tests) which had comprehensive isolated handler tests covering signature validation, delivery failure fallback, checkpoint propagation, and error paths.
  - Completed (iteration 155): Created `HubSpotWebhookHandlerTests.cs` with 15 tests: 404 for unknown connector, 200 for disabled connector, 401 for invalid HMAC signature with audit event, valid signature triggers sync (verifies SyncJobMessage fields + SyncRunEntity + audit event), 400 for invalid JSON, 400 for empty event array, 200 for no active subscriptions, resets consecutive failures on success, skips signature when no secret configured, includes checkpoint from previous completed sync run, 500 when secret retrieval fails, delivery failure activates fallback after threshold, delivery failure below threshold does not activate fallback, ComputeNextPollTime adds interval with jitter. Created `ClickUpWebhookHandlerTests.cs` with 15 tests: same coverage pattern plus idempotency key includes event name and task ID, 400 when event field missing, checkpoint propagation from previous sync. Both use HMAC-SHA256 signature computation matching production `ValidateSignature` methods. 30 new tests total.

- [x] TECH-057: Add unit tests for WebhookEvalNotificationClient (notification routing, payload formatting, error handling).
  - Root cause: `WebhookEvalNotificationClient` (192 lines, 6 methods: NotifyAsync, ShouldNotify, BuildPayload, BuildSlackPayload, BuildTeamsPayload, BuildGenericPayload) had zero dedicated unit tests despite implementing eval regression alert notifications with Slack/Teams/generic webhook formatting, conditional notification routing, and HTTP error handling.
  - Completed (iteration 156): Created `WebhookEvalNotificationClientTests.cs` in `SmartKb.Contracts.Tests/` with 26 unit tests using `FakeHandler` (captures request URI/body, configurable status codes) and `ThrowingHandler` (simulates HTTP failures). Coverage: NotifyAsync configuration routing (7 tests: unconfigured returns true, clean payload skips, success/failure status codes, HTTP exception handling, cancellation propagation, correct URL posting), ShouldNotify conditions (5 tests: blocking regression, violations only, baseline regression, both flags disabled, violations disabled with no regression), BuildGenericPayload (1 test: all fields including eventType/runId/violations/baselineComparison/timestamp), BuildSlackPayload (6 tests: blocking header with rotating_light, violation details, regression details excluding "ok" severity, run URL link, warning icon for non-blocking, omits URL when null), BuildTeamsPayload (5 tests: MessageCard structure with red/orange theme colors, violation and regression text, OpenUri action with run URL, empty actions when no URL), format routing (2 tests: unknown format falls back to generic, case-insensitive format matching).

- [x] TECH-058: Fix Eval CLI HttpClient lifecycle and IDisposable cleanup.
  - Root cause: `HttpChatOrchestratorClient` created `new HttpClient()` internally with no way to inject one (unlike `WebhookEvalNotificationService` which accepted optional `HttpClient?`). Both classes unconditionally disposed the HttpClient in `Dispose()`, even when it was externally injected — violating the ownership principle and risking double-disposal or premature disposal of shared clients. `Program.cs` used manual `?.Dispose()` calls in both try and catch blocks instead of `using` declarations, which is error-prone and could leak on unexpected exit paths.
  - Completed (iteration 157): Added optional `HttpClient? httpClient = null` parameter to `HttpChatOrchestratorClient` constructor (same pattern as `WebhookEvalNotificationService`). Added `_ownsHttpClient` tracking field to both `HttpChatOrchestratorClient` and `WebhookEvalNotificationService` — `Dispose()` now only disposes the HttpClient when the class created it internally. Refactored `Program.cs` to use `using var` declarations for both clients, eliminating 4 manual `?.Dispose()` calls across try/catch blocks. Created `HttpChatOrchestratorClientTests.cs` with 8 new tests (correct endpoint, tenant/correlation headers, auth header, non-2xx throws, empty response throws, injected client not disposed, owned client disposed, trailing slash trimmed). Added 2 new tests to `WebhookEvalNotificationServiceTests.cs` (injected client not disposed, owned client disposed). 10 new tests total.

- [x] TECH-059: Add unit tests for PatternDistillationService optional service integration paths.
  - Root cause: PatternDistillationService had 31 tests covering happy-path distillation and static helpers, but zero tests for the 3 optional service integration paths (IEmbeddingService, IPatternIndexingService, ICaseCardQualityValidator). These paths implement critical two-store architecture behavior: embedding generation for Pattern Store vectors, pattern indexing into Azure AI Search, and quality-gate validation before persistence. Failure resilience (catch blocks at lines 320-327 and 401-408) and quality-gate rejection/below-threshold logic (lines 369-391) were completely untested.
  - Completed (iteration 158): Added 11 new tests to `PatternDistillationServiceTests.cs` with 5 new stub classes (StubEmbeddingService, ThrowingEmbeddingService, StubPatternIndexingService, ThrowingPatternIndexingService, StubQualityValidator). Tests: `Distill_EmbeddingFailure_ContinuesWithoutEmbedding` (HttpRequestException caught, pattern still created), `Distill_EmbeddingSuccess_IndexesPatternsInPatternStore` (verifies indexed pattern has non-null EmbeddingVector), `Distill_IndexingFailure_PatternStillPersisted` (InvalidOperationException caught, pattern in SQL), `Distill_NoEmbeddingService_SkipsIndexing` (null embedding → indexing skipped even when indexing service available), `Distill_QualityValidatorRejects_SkipsPattern` (Rejected=true → 0 created, 1 skipped, no DB entry), `Distill_QualityValidatorBelowThreshold_SavesWithQualityScore` (Passed=false, Rejected=false → saved with QualityScore=0.45), `Distill_QualityValidatorPasses_SavesWithQualityScore` (Passed=true → saved with QualityScore=0.85), `FindCandidates_ExcludesSessionsWithTooFewChunks` (MinCitedChunks=3, chunkCount=2 → filtered out), `Distill_CandidateThrows_CapturesErrorAndContinues` (mixed results: 1 created + 1 skipped), `Distill_AllServicesIntegrated_EmbedAndIndexAndValidate` (all 3 services wired, verifies embedding+indexing+quality score). PatternDistillationService now has 42 tests covering all public methods, all optional service paths, and all significant error-handling branches.

- [x] TECH-060: Consolidate duplicate `ServiceBusSyncJobPublisher` and `InMemorySyncJobPublisher` into `SmartKb.Contracts.Services`.
  - Root cause: `ServiceBusSyncJobPublisher` existed as identical copies in `SmartKb.Api.Connectors` and `SmartKb.Ingestion` (only namespace and one log message word differed). `InMemorySyncJobPublisher` similarly duplicated. Same pattern as TECH-003 (duplicate `KeyVaultSecretProvider`).
  - Completed (iteration 159): Moved both implementations to `SmartKb.Contracts.Services`. Added `Azure.Messaging.ServiceBus` package to Contracts csproj. Deleted 4 duplicate files. Updated Ingestion `Program.cs` to remove fully-qualified namespace prefixes (API already resolved via `using SmartKb.Contracts.Services`). Created `SyncJobPublisherTests.cs` with 5 unit tests for `InMemorySyncJobPublisher`: warning log with SyncRunId/ConnectorId, returns completed task, includes configuration guidance message, cancellation token handling, all 4 connector types. Zero duplicate publisher implementations remaining.

- [x] TECH-061: Add `using` declarations to all undisposed `HttpContent` instances.
  - Root cause: 12 `StringContent` and `FormUrlEncodedContent` objects across 10 files created without `using` declarations. While `HttpClient.PostAsync` disposes content internally in .NET 6+, explicit `using` declarations are best practice for IDisposable types and prevent leaks if execution flow changes (e.g., exception before the HTTP call). Same pattern as TECH-058 (Eval CLI HttpClient lifecycle).
  - Completed (iteration 160): Added `using var` to all 12 instances: `StringContent` in HubSpotConnectorClient, ClickUpConnectorClient, AzureDevOpsConnectorClient (×2), AdoWebhookManager, SharePointWebhookManager, HubSpotWebhookManager, ClickUpWebhookManager, WebhookEvalNotificationClient, WebhookEvalNotificationService; `FormUrlEncodedContent` in SharePointConnectorClient, SharePointWebhookManager. `OAuthTokenService` already had `using var` (no change needed). Zero undisposed `HttpContent` instances remaining in codebase.

- [x] TECH-062: Consolidate duplicate JSON deserialization helpers into shared `JsonDeserializeHelper` utility.
  - Root cause: 12 private `Deserialize*` methods across 10 files (8 `DeserializeStringList`/`Tags`/`ReasonCodes`/`JsonList` variants in Data repositories, 2 `DeserializeCitations` in SessionService + EscalationDraftService, 2 `DeserializeFieldMapping` in ConnectorAdminService + SyncJobProcessor) implemented identical JSON deserialization with logging — null guard, `JsonSerializer.Deserialize<T>`, `catch (JsonException)` with `LogWarning`. Also: `GoldCaseService.DeserializeTags` was the only helper missing `JsonOpts` (used default options instead of camelCase — a latent bug). `IngestionWorker` had a silent `catch (OperationCanceledException)` with no log. `SourceViewerPanel` had a `.catch()` block that set error state without `console.warn`.
  - Completed (iteration 161): Created `JsonDeserializeHelper` static class in `SmartKb.Data` with two generic methods: `Deserialize<T>` (returns fallback on empty/malformed) and `DeserializeOrNull<T>` (returns null on empty/malformed). Both use `[CallerMemberName]` for structured log messages. Replaced all 12 duplicate helpers with one-liner delegates to the shared utility. Fixed GoldCaseService bug by routing `DeserializeTags` through shared helper with `JsonOpts`. Added `_logger.LogInformation("Ingestion worker received shutdown signal")` to IngestionWorker. Added `console.warn('[SourceViewerPanel]...')` to evidence content fetch catch. 12 new unit tests for `JsonDeserializeHelper` (null/empty/valid/malformed/null-json-value/complex-type/null-options/caller-name for `Deserialize<T>`; null/empty/valid/malformed for `DeserializeOrNull<T>`). 470 frontend tests passing.

- [x] TECH-063: Add `CancellationToken` propagation to pattern maintenance and contradiction detection services.
  - Root cause: 7 public async methods across `PatternMaintenanceService` (4 methods) and `ContradictionDetectionService` (3 methods) lacked `CancellationToken` parameters, preventing graceful request cancellation and shutdown propagation through EF Core database queries. The 7 corresponding API endpoints in `Program.cs` did not pass `HttpContext.RequestAborted` to these services.
  - Completed (iteration 162): Added `CancellationToken ct = default` to all 7 public methods on `IPatternMaintenanceService` (4) and `IContradictionDetectionService` (3) interfaces. Updated both implementations to accept and forward `ct` to all EF Core async calls (`ToListAsync`, `CountAsync`, `SaveChangesAsync`, `FirstOrDefaultAsync`). Also updated `PatternMaintenanceService.UpdateTaskStatusAsync` private helper. Updated all 7 minimal API endpoint handlers in `Program.cs` to inject `HttpContext httpContext` and pass `httpContext.RequestAborted`. Backward-compatible: all `CancellationToken` params use `= default`, so existing tests and callers are unaffected. Files changed: 2 interfaces, 2 implementations, 1 API Program.cs.

- [x] TECH-064: Add `CancellationToken` propagation to 4 webhook endpoint handlers.
  - Root cause: All 4 webhook receiver endpoints (ADO, SharePoint/Graph, HubSpot, ClickUp) in `Program.cs` called `StreamReader.ReadToEndAsync()` without a `CancellationToken` parameter and invoked their respective handler methods without passing `httpContext.RequestAborted`. This prevented graceful request cancellation and shutdown propagation during webhook body reads and handler processing. The handler methods themselves already accepted `CancellationToken cancellationToken = default` but received `default` from the endpoints.
  - Completed (iteration 163): Added `var ct = httpContext.RequestAborted;` to all 4 webhook endpoint handlers. Updated `ReadToEndAsync()` calls to pass `ct`. Updated handler invocations to pass `ct`: `HandleAsync(connectorId, body, authHeader, ct)` (ADO), `HandleNotificationAsync(connectorId, requestBody, ct)` (SharePoint), `HandleAsync(connectorId, body, signatureHeader, timestampHeader, ct)` (HubSpot), `HandleAsync(connectorId, body, signatureHeader, ct)` (ClickUp). 4 new cancellation propagation tests (one per handler): verify `OperationCanceledException` propagates when pre-cancelled token is passed. Files changed: `Program.cs`, 4 handler test files.

- [x] TECH-065: Consolidate 31 duplicate `JsonSerializerOptions` into shared `SharedJsonOptions` class.
  - Root cause: 31 `private static readonly JsonSerializerOptions` fields across Contracts (14), Data (13), Api (5), Ingestion (2), and Eval (5) projects contained identical or near-identical configurations, violating DRY and creating maintenance burden when JSON settings need to change.
  - Completed (iteration 164): Created `SmartKb.Contracts/SharedJsonOptions.cs` with 8 named presets: `CamelCaseIgnoreNull` (connector clients + webhook managers), `CamelCase` (webhook handlers + GoldCaseService), `CamelCaseWrite` (Data repos + Ingestion), `SnakeCase` (OpenAI-facing services), `CaseInsensitive` (IndexMigration + PatternUsageMetrics), `CamelCaseCompact` (ConnectorAdmin + eval notifications), `CaseInsensitiveIndented` (eval baseline), `CamelCaseIndented` (eval reports). Removed all 31 private fields and replaced with shared references. Zero duplicate `JsonSerializerOptions` remaining in production code.

- [x] TECH-066: Fix `GetAwaiter().GetResult()` sync-over-async in `HttpChatOrchestratorClientTests`.
  - Root cause: `Constructor_TrimsTrailingSlashFromBaseUrl` test used `client.OrchestrateAsync(...).GetAwaiter().GetResult()` instead of `await`, which can cause deadlocks.
  - Completed (iteration 164): Converted to `async Task` method with `await`.

- [x] TECH-067: Replace array index keys with stable keys in 3 React components with mutable lists.
  - Root cause: `FieldMappingEditor`, `PlaybooksPage`, and `PrivacyAdminPage` used `key={i}` on list items that can be added/removed, risking UI state corruption on list mutations.
  - Completed (iteration 164): `FieldMappingEditor` → composite key `${rule.sourceField}-${rule.targetField}-${i}`. `PlaybooksPage` checklist → `${item}-${i}`. `PrivacyAdminPage` custom patterns → `p.name` (unique per pattern). Removed now-unused `removeCustomPattern` function (inlined filter-by-name). Frontend build clean, 470 tests passing.

- [x] TECH-068: Consolidate 8 duplicate `DeserializeAsync<T>` HTTP response helpers into shared `ConnectorHttpHelper`.
  - Root cause: 8 identical `private static async Task<T?> DeserializeAsync<T>(HttpResponseMessage, CancellationToken)` methods across connector clients (4) and webhook managers (4). 6 used `SharedJsonOptions.CamelCaseIgnoreNull`, 2 used `SharedJsonOptions.CamelCase`. Same duplication pattern as TECH-062 (`JsonDeserializeHelper`) and TECH-065 (`SharedJsonOptions`).
  - Completed (iteration 165): Created `SmartKb.Contracts.ConnectorHttpHelper` static class with `DeserializeAsync<T>(HttpResponseMessage, JsonSerializerOptions, CancellationToken)`. Replaced all 8 private methods with one-liner delegates to the shared helper. Files changed: HubSpotConnectorClient, SharePointConnectorClient, AzureDevOpsConnectorClient, ClickUpConnectorClient, AdoWebhookManager, SharePointWebhookManager, HubSpotWebhookManager, ClickUpWebhookManager. 5 new unit tests in `ConnectorHttpHelperTests.cs` (deserialize success, null body, malformed JSON throws, respects options, cancellation propagation). Zero duplicate `DeserializeAsync` implementations remaining in connector layer.

- [x] TECH-069: Consolidate 30+ duplicate status string literals into shared `WorkflowStatus` and `EscalationExternalStatus` constant classes.
  - Root cause: 30+ hardcoded status string literals ("Pending", "Completed", "Failed", "Processing", "Resolved", "Applied", "Dismissed", "Created") scattered across 12 files — 5 entity default values and 7 service files. Typo risk, no compile-time safety, and inconsistent usage across workflow entities (PatternMaintenanceTask, PatternContradiction, RoutingRecommendation, DataSubjectDeletionRequest) and escalation entities.
  - Completed (iteration 166): Created `WorkflowStatus` static class (7 constants: Pending, Processing, Completed, Failed, Resolved, Applied, Dismissed) and `EscalationExternalStatus` static class (3 constants: Pending, Created, Completed) in `SmartKb.Contracts.Models`. Replaced all 30+ hardcoded literals across 12 files: 5 entity defaults (PatternMaintenanceTaskEntity, PatternContradictionEntity, RoutingRecommendationEntity, DataSubjectDeletionRequestEntity, IndexSchemaVersionEntity), 7 service files (PatternMaintenanceService, ContradictionDetectionService, RoutingImprovementService, DataSubjectDeletionService, EscalationDraftService, TeamPlaybookService, Program.cs). Also updated `IndexSchemaVersionEntity` to use existing `IndexVersionStatus.Active`. 13 new tests in `WorkflowStatusTests.cs` (non-null/empty, uniqueness, PascalCase naming, 7 expected constants for WorkflowStatus; non-null/empty, uniqueness, 3 expected constants for EscalationExternalStatus). Zero hardcoded status string literals remaining in production code.

- [x] TECH-070: Consolidate 7 duplicate `ParseSourceConfig` methods into shared `ConnectorHttpHelper.ParseJson<T>`.
  - Root cause: 7 identical `ParseSourceConfig` methods across 4 connector clients (AzureDevOpsConnectorClient, SharePointConnectorClient, HubSpotConnectorClient, ClickUpConnectorClient) and 3 webhook managers (AdoWebhookManager, HubSpotWebhookManager, ClickUpWebhookManager) implemented the same null-guard → `JsonSerializer.Deserialize<T>` → `catch (JsonException)` with logging pattern. Same duplication as TECH-062 (`JsonDeserializeHelper`) and TECH-068 (`ConnectorHttpHelper.DeserializeAsync<T>`).
  - Completed (iteration 167): Added `ConnectorHttpHelper.ParseJson<T>(string? json, JsonSerializerOptions options, ILogger? logger = null)` generic method — null/whitespace guard, deserialize, catch `JsonException` with `LogWarning` including type name. Replaced all 7 `ParseSourceConfig` method bodies with one-liner delegates to the shared helper. 7 new unit tests in `ConnectorHttpHelperTests.cs` (null input, whitespace input, valid JSON, malformed JSON returns null, malformed JSON logs warning, respects options, no logger does not throw). Zero duplicate `ParseSourceConfig` implementations remaining in connector layer.

- [x] TECH-071: Add `console.warn` to 3 silent frontend catch blocks.
  - Root cause: Code quality scan found 3 catch blocks in frontend components that silently swallowed errors without any diagnostic logging: `SourceConfigEditor.parseJsonSafe` (JSON parse failure on source config input), `DiagnosticsPage.formatTime` (date formatting failure), `AuditCompliancePage.formatTimestamp` (date formatting failure). All returned fallback values but with no console trace, making UI data display issues impossible to diagnose.
  - Completed (iteration 168): Added `console.warn('[Component] ...')` with error object to all 3 catch blocks. `AuditCompliancePage.EventDetail` JSON.parse catch intentionally left silent (non-JSON detail is an expected case, not an error). Zero silent catch blocks remaining in frontend utility/formatting functions.

- [x] TECH-072: Propagate `CancellationToken` to ~120 async API endpoint handlers in `Program.cs`.
  - Root cause: TECH-063 and TECH-064 fixed CancellationToken propagation for pattern maintenance/contradiction detection endpoints (7 endpoints) and webhook handlers (4 endpoints), but ~120 remaining async endpoint handlers still did not pass `httpContext.RequestAborted` to their async service calls. Client disconnections could not cancel in-flight database queries, Service Bus operations, or external HTTP calls, wasting server resources and preventing graceful shutdown propagation.
  - Completed (iteration 169): Added `HttpContext httpContext` parameter injection and `var ct = httpContext.RequestAborted;` to all ~120 async endpoint handlers missing CancellationToken propagation. Passed `ct` to every async service method call. Affected endpoint groups: connector admin CRUD (15 endpoints), synonym/stop-word/special-token admin (18 endpoints), session lifecycle (6 endpoints), feedback/outcome (5 endpoints), escalation draft (7 endpoints), chat orchestration (1 endpoint), audit query/export (2 endpoints), retrieval settings (3 endpoints), pattern distillation/governance (8 endpoints), pattern usage metrics (1 endpoint), dead-letter inspection (1 endpoint), webhook status (2 endpoints), diagnostics summary (1 endpoint), rate-limit alerts (1 endpoint), credential status/rotation (3 endpoints), routing rules/analytics/recommendations (10 endpoints), playbooks (7 endpoints), privacy PII/retention/deletion (10 endpoints), cost settings/token usage (6 endpoints), index migrations (7 endpoints), eval reports (3 endpoints), gold cases (7 endpoints). All service method signatures already accepted `CancellationToken ct = default` — backward-compatible, no interface changes needed. Total: 131 endpoints now propagate CancellationToken (was 11 before TECH-072).

- [x] TECH-073: Add `StringComparison.Ordinal` to 11 culture-dependent string operations across 7 files.
  - Root cause: 11 string operations (`.Contains`, `.IndexOf`, `.StartsWith`, `.Replace`) used culture-dependent overloads when comparing ASCII literal patterns (`"=>"`, `":/"`, `"session://"`, `"eval-"`, `"'"`, `","`). Under non-default cultures these could produce unexpected results. Per CA1307/CA1310 analyzers, explicit `StringComparison.Ordinal` should be specified for all non-linguistic string comparisons.
  - Completed (iteration 170): Fixed 11 operations in 7 files: `SynonymMapService.cs` (1 Contains), `SharePointConnectorClient.cs` (2 Contains + 2 IndexOf), `PatternDistillationService.cs` (1 StartsWith + 1 Replace), `GoldCaseService.cs` (1 StartsWith), `GoldDatasetLoader.cs` (1 StartsWith), `ODataFilterBuilder.cs` (2 Replace), `AzureSearchRetrievalService.cs` (1 Replace), `FusedRetrievalService.cs` (1 Replace). 3 new tests: Unicode OData escaping, Unicode synonym explicit mapping, Unicode synonym equivalent. Zero culture-dependent string operations remaining on ASCII literal patterns.

- [x] TECH-023: Add structured logging to all silent `catch (JsonException)` blocks.
  - Root cause: 25 `catch (JsonException)` blocks across 24 files silently swallowed deserialization errors with no logging. When JSON stored in SQL columns or received from external APIs was malformed, the code returned fallback values (null, empty list, empty dict) with no diagnostic trace, making production debugging impossible.
  - Completed (iteration 122): Added `_logger.LogWarning(ex, ...)` with exception details and method name to all 25 catch blocks. For `private static` helpers in Data repositories, webhook managers, SyncJobProcessor, ConnectorAdminService, and OAuthTokenService: converted to instance methods to access `_logger`. For `internal static` `ParseSourceConfig` methods on 4 connector clients (called by tests): added optional `ILogger? logger = null` parameter; internal call sites pass `_logger`, test callers use default (null). SharePointWebhookManager's calls to `SharePointConnectorClient.ParseSourceConfig` updated to pass `_logger`. 3 test files updated for static→instance call changes. Zero remaining silent `catch (JsonException)` blocks in backend code.

- [x] BUG-004: Terraform `app-service.tf` missing `https_only = true` on both web apps — **security drift**.
  - Root cause: ARM template explicitly sets `"httpsOnly": true` on both `app-smartkb-api-{env}` and `app-smartkb-ingestion-{env}`. Terraform `azurerm_linux_web_app` blocks omit `https_only`, which defaults to `false` in the azurerm provider.
  - Completed: Added `https_only = true` to both `azurerm_linux_web_app.api` and `azurerm_linux_web_app.ingestion` in `infra/terraform/app-service.tf`. Terraform now matches ARM `httpsOnly: true`. All 691 backend tests passing.
  - Also noted (non-blocking): Terraform `purge_protection_enabled = false` for non-prod vs ARM `null` (latent operational risk if vault is promoted); Search `replicaCount`/`partitionCount` explicit in ARM only (cosmetic).

- [x] BUG-005: CI workflow does not run frontend tests — **regression risk**.
  - Root cause: `.github/workflows/ci.yml` `frontend` job runs `npm run lint` and `npm run build` but never runs `npm run test` (which invokes `vitest run`). All frontend test cases were invisible to CI.
  - Completed: Added `npm run test` step to `.github/workflows/ci.yml` frontend job, after lint and before build. 78 frontend tests now run in CI.

### P0 Ingestion + Evidence Store MVP (continued)

- [x] P0-008: Implement Azure DevOps ingestion (initial backfill + incremental sync).
  - Specs: jtbd-01
  - Completed: First concrete `IConnectorClient` implementation — `AzureDevOpsConnectorClient` in `SmartKb.Contracts.Connectors`. Supports PAT auth via Key Vault. Ingests work items (WIQL API) and wiki pages (Wiki REST API). ACL mapping: area paths → `AllowedGroups` with `Restricted` visibility; wiki pages default to `Internal`. Checkpoint-based multi-project, multi-phase sync (`AdoCheckpoint` tracks project index + phase + last-modified timestamp). Deep links to work items and wiki pages. Content hash for dedup. HTML stripping for work item descriptions. Handles 3k+ artifacts via batched WIQL (200/batch) with `HasMore` pagination. Per-project error isolation (auth failure in one project doesn't crash the sync). `AzureDevOpsSourceConfig` model for organization URL, project list, work item type/area path filters, batch size. Registered in both API and Ingestion DI. 40 new tests (27 unit + 7 integration + 6 ADO sync processor tests); all 273 tests passing. Webhook support deferred to P0-008A (requires webhook receiver endpoint + signature verification).

- [x] P0-008A: Add ADO service hook webhook support (event-driven freshness + polling fallback).
  - Specs: jtbd-01
  - Dependencies: P0-008 (complete)
  - Completed: `WebhookSubscriptionEntity` with SQL table, unique index on (ConnectorId, EventType). `IWebhookManager` interface with `AdoWebhookManager` implementation — registers `workitem.created` and `workitem.updated` service hooks via ADO REST API (v7.1). Webhook secret generated (32-byte random), stored in Key Vault via `ISecretProvider.SetSecretAsync`. `AdoWebhookHandler` processes incoming payloads: validates HMAC signature (Basic auth shared secret), deduplicates via idempotency key, triggers incremental sync via Service Bus. Anonymous endpoint `POST /api/webhooks/ado/{connectorId}`. Webhook lifecycle managed in `ConnectorAdminService`: register on enable, deregister on disable. `WebhookPollingFallbackService` (BackgroundService) checks every 30s for subscriptions in fallback mode; triggers incremental syncs at 5-minute intervals with 0-60s jitter. Failure threshold: 3 consecutive failures activates fallback. `WebhookSettings` configurable via `Webhook:*` app settings. EF Core migration `AddWebhookSubscriptions`. 36 new tests (8 signature, 11 handler, 4 endpoint, 9 webhook manager, 4 polling fallback); all 309 tests passing.

- [x] P0-009: Implement SharePoint ingestion (Graph delta queries + change notifications + fallback polling).
  - Specs: jtbd-01
  - Dependencies: P0-007 (complete)
  - Completed: Second `IConnectorClient` implementation — `SharePointConnectorClient` in `SmartKb.Contracts.Connectors`. Uses Microsoft Graph REST API (no SDK dependency) with OAuth2 client credentials flow for authentication. Ingests document library files via delta queries (`/drives/{id}/root/delta`). Delta token checkpoint tracks multi-drive incremental sync position. Handles delta token expiry (410 Gone) by falling back to full sync automatically. File extension filtering (supported: .txt, .md, .pdf, .docx, .pptx, .xlsx, etc.) and folder exclusion. ACL mapping: drive name → `AllowedGroups` with `Restricted` visibility. Content hash for dedup based on item metadata (full text extraction deferred to P0-010). `SharePointSourceConfig` model with site URL, Entra ID tenant ID, client ID, drive IDs, extension/folder filters, batch size. `SharePointWebhookManager` registers Graph change notification subscriptions per drive (max 4230-minute lifetime). `SharePointWebhookHandler` processes Graph change notifications with clientState validation (constant-time comparison) and validation handshake support. Anonymous endpoint `POST /api/webhooks/msgraph/{connectorId}` with `?validationToken=` handshake. Webhook lifecycle managed in `ConnectorAdminService`: register on enable, deregister on disable. Reuses existing `WebhookPollingFallbackService` for failure recovery (3 consecutive failures threshold). `GraphChangeNotificationPayload`, `GraphSubscriptionRequest/Response` models for Graph webhook protocol. Registered in both API and Ingestion DI. 55 new tests (27 connector client unit + 8 webhook manager + 13 webhook handler + 5 endpoint integration + 2 checkpoint); all 364 tests passing.

- [x] P0-010: Implement canonical normalization + chunking + baseline enrichment.
  - Specs: jtbd-02
  - Dependencies: P0-008 or P0-009 (need at least one connector producing records)
  - Completed: `IChunkingService` + `TextChunkingService` (markdown structural boundaries, 512 tokens/chunk, 64 overlap, paragraph fallback, hard-split for oversized sections). `IEnrichmentService` + `BaselineEnrichmentService` (keyword-based category/severity/environment detection, error token extraction via regex — exception names, HTTP status codes, error codes, hex codes — baseline PII detection for emails/phones/SSNs/credit cards). `INormalizationPipeline` + `NormalizationPipeline` orchestrates chunking→enrichment→EvidenceChunk production with full lineage (ChunkId = `{EvidenceId}_chunk_{index}`). `EvidenceChunkEntity` in SQL with `AddEvidenceChunks` migration (tenant-scoped, connector FK, content hash for dedup, enrichment version tracking, reprocessed-at timestamp). `SyncJobProcessor` now runs normalization pipeline after each fetch batch and persists chunks via upsert. `ErrorTokens` field added to `CanonicalRecord`. `EnrichmentVersion` and `ErrorTokens` fields added to `EvidenceChunk`. Services registered in both API and Ingestion DI. 46 new tests (9 chunking, 19 enrichment, 13 pipeline, 5 existing test updates); all 410 tests passing. SPEC-005 (ErrorTokens) and SPEC-006 (chunk ID format) resolved.

- [x] P0-010A: Set up Azure Blob Storage raw content store for ingested snapshots and extracted text.
  - Specs: jtbd-01, jtbd-02
  - Dependencies: P0-005A (Blob resource provisioned in IaC)
  - Completed: `IBlobStorageService` interface + `AzureBlobStorageService` implementation in `SmartKb.Contracts.Services`. `BlobStorageSettings` config class (ServiceUri for Managed Identity, ConnectionString fallback). Tenant-scoped blob path convention `{tenantId}/{connectorType}/{evidenceId}/raw`. `RawContentSnapshotEntity` in SQL with `AddRawContentSnapshots` migration (PK=EvidenceId, connector FK, content hash for dedup, content length, content type). `SyncJobProcessor` uploads raw content to blob before normalization; skips upload when content hash unchanged (dedup). `Azure.Storage.Blobs` package added to Contracts. `BlobContainerClient` registered in both API and Ingestion DI with Managed Identity preference. `Storage Blob Data Contributor` role assigned to both API and Ingestion App Services in Terraform (`storage.tf`) and ARM (`main.json`). `BlobStorage__ServiceUri` app setting added to both App Services. ARM template validated (22 resources, 13 outputs). 22 new tests (8 blob settings + path convention, 8 blob storage service, 4 SyncJobProcessor blob upload, 2 existing test updates); all 432 tests passing.

- [x] P0-011: Implement Azure AI Search Evidence index and indexing pipeline.
  - Specs: jtbd-03
  - Dependencies: P0-010 (chunks must exist to index)
  - Completed: `IIndexingService` interface + `AzureSearchIndexingService` implementation in `SmartKb.Contracts.Services`. Index schema built from `SearchFieldNames` with all 17 fields: key (`chunk_id`), 3 searchable text fields (`chunk_text`, `chunk_context`, `title` with EnMicrosoft analyzer), vector field (`embedding_vector` at 1536 dims), 8 filterable metadata fields (tenant_id, evidence_id, source_system, source_type, status, updated_at, product_area, tags), 3 ACL security trimming fields (visibility, allowed_groups, access_label), source_url. Vector search: HNSW algorithm with cosine metric (M=4, efConstruction=400, efSearch=500). Semantic search: `evidence-semantic-config` with title as TitleField, chunk_text+chunk_context as ContentFields, tags as KeywordsFields. Index creation is idempotent (CreateOrUpdate). Batch indexing with configurable batch size (default 100). `SearchServiceSettings` config class (Endpoint for Managed Identity, AdminApiKey fallback). `SyncJobProcessor` calls `IndexChunksAsync` after chunk persistence — indexing failures are non-fatal (chunks persisted in SQL for retry). `SearchIndexClient` registered in both API and Ingestion DI with Managed Identity preference. `SearchService__Endpoint` app setting added to both App Services in Terraform and ARM. `Search Index Data Contributor` + `Search Service Contributor` RBAC roles assigned to both API and Ingestion App Services in Terraform (`search.tf`) and ARM (`main.json`). ARM template validated (26 resources, 14 outputs). 27 new tests (6 settings, 17 index schema/document mapping, 4 SyncJobProcessor indexing integration); all 459 tests passing.

### P0 Retrieval + Orchestration MVP

- [x] P0-012: Implement hybrid retrieval service (vector + BM25 + semantic rerank).
  - Specs: jtbd-03
  - Dependencies: P0-011 (complete), D-005 (resolved), D-012 (resolved)
  - Completed: `IRetrievalService` interface + `AzureSearchRetrievalService` implementation in `SmartKb.Contracts.Services`. Hybrid search: vector (VectorizedQuery on `embedding_vector` field, HNSW cosine) + BM25 (text search on `chunk_text`, `chunk_context`, `title`). Azure AI Search handles RRF score fusion natively. Optional semantic reranking via `evidence-semantic-config` with extractive captions. Tenant isolation via OData filter on `tenant_id` (always server-side). ACL security trimming in-memory post-retrieval: Public/Internal pass through, Restricted requires user membership in `allowed_groups` (case-insensitive). `RetrievalSettings` config class with D-005 defaults (TopK=20, RrfK=60, equal BM25/vector weights 1.0/1.0) and D-012 defaults (NoEvidenceScoreThreshold=0.3, NoEvidenceMinResults=3). `RetrievalResult` DTO returns ranked `RetrievedChunk` list, `AclFilteredOutCount`, `HasEvidence` flag, and `TraceId` for audit. `RetrievedChunk` DTO includes citation metadata (title, source URL, source system/type, updated_at, access label, tags) plus RRF and optional semantic scores. Retrieval telemetry logs: total raw results, ACL-filtered count, returned count, has-evidence flag, above-threshold count, duration, top-5 chunk IDs and scores, trace ID. Over-fetches 2x TopK to account for ACL filtering. Error handling: `RequestFailedException` returns empty result with `HasEvidence=false`. OData value escaping for tenant ID. Services registered in both API and Ingestion DI. Phase 1 retrieves from Evidence Index only; Pattern Index fusion deferred to P1-004. 24 new tests (7 settings, 9 ACL filtering, 4 no-evidence detection, 2 OData escaping, 2 DTO validation); all 483 tests passing.

- [x] P0-013: Implement chat orchestration with structured outputs.
  - Specs: jtbd-04
  - Dependencies: P0-012 (complete), D-003 (resolved), D-006 (resolved), D-010 (resolved), D-013 (resolved)
  - Completed: `IChatOrchestrator` interface + `ChatOrchestrator` implementation in `SmartKb.Contracts.Services`. Full orchestration pipeline: embed query → hybrid retrieve → ACL-filtered evidence → assemble grounded prompt → OpenAI structured output (`json_schema` strict mode, `grounded_answer` schema) → blend confidence → persist trace → return response. `IEmbeddingService` + `OpenAiEmbeddingService` for query embedding via OpenAI Embeddings API (text-embedding-3-large@1536). `ChatOrchestrationSettings` config with D-003 thresholds (High>=0.7, Medium>=0.4, Low<0.4), D-010 token budget (102k = 80% of gpt-4o 128k context), D-013 degradation threshold (0.3). Structured response contract: `ChatResponse` with `ResponseType` (final_answer/next_steps_only/escalate), `Answer`, `Citations` (mapped `CitationDto` with snippet + source metadata), blended `Confidence` + `ConfidenceLabel`, `NextSteps`, `EscalationSignal`, `TraceId`, `HasEvidence`, `SystemPromptVersion`. D-003 confidence scoring: blended = 0.6×modelSelfReport + 0.4×retrievalHeuristic (avgRrfScore × saturation factor). D-010 sliding window: oldest session messages dropped first when budget exceeded. D-013 degradation: when blendedConfidence < 0.3 and response_type was final_answer, overrides to next_steps_only with explicit "I don't have enough information" + diagnostic steps + escalation signal. No-evidence path returns next_steps_only with escalation recommended to Engineering. `AnswerTraceEntity` in SQL with `AddAnswerTraces` migration for durable evidence-to-answer trace links (tenant-scoped, correlation ID indexed). `IAnswerTraceWriter` + `SqlAnswerTraceWriter`. `POST /api/chat` endpoint with `chat:query` permission (SupportAgent, SupportLead, Admin). Conditional DI registration (requires OpenAI API key + Search Service configured). 503 graceful degradation when orchestrator unavailable. Registered in API DI. 49 new tests (12 settings, 21 orchestrator unit, 5 embedding DTO, 5 API endpoint integration, 6 chat DTO validation); all 532 tests passing.

- [x] P0-013A: Implement session and message persistence API for chat continuity.
  - Specs: jtbd-05
  - Dependencies: P0-013 (complete)
  - Completed: `SessionSettings` config class (default 24h expiry, max 200 messages/session, max 50 active sessions/user — all configurable via `Session:*` app settings). `SessionEntity` extended with `Title`, `CustomerRef`, `ExpiresAt` fields. `MessageEntity` extended with `CitationsJson` (JSON column for citation persistence), `Confidence`, `ConfidenceLabel`, `ResponseType` fields. `AddSessionPersistence` migration. `ISessionService` interface + `SessionService` implementation in `SmartKb.Data.Repositories`. Session CRUD: `POST /api/sessions` (create), `GET /api/sessions` (list user's sessions), `GET /api/sessions/{id}` (get detail), `DELETE /api/sessions/{id}` (soft-delete). Message endpoints: `GET /api/sessions/{id}/messages` (chronological history with persisted citations), `POST /api/sessions/{id}/messages` (orchestrate chat + persist user and assistant messages). Follow-up messages carry full session history to `ChatOrchestrator` for multi-turn context (D-010 token budget applies). Auto-title sessions from first user query when no title set. Session expiry extended on each message activity. Expired/over-limit sessions return 404. Tenant isolation + user ownership enforced on all operations. All endpoints require `chat:query` permission (SupportAgent, SupportLead, Admin). Stateless `POST /api/chat` endpoint preserved for backward compatibility. Registered in API DI via `DataServiceExtensions`. 35 new tests (4 settings, 14 service unit, 17 endpoint integration).

- [x] P0-014: Enforce "never pass restricted content to model" check in orchestration path.
  - Specs: jtbd-03, jtbd-10
  - Dependencies: P0-012 (complete), P0-013 (complete)
  - Completed: Defense-in-depth ACL enforcement in `ChatOrchestrator.EnforceRestrictedContentExclusion` — second ACL check between retrieval and prompt assembly. `RetrievedChunk` extended with `Visibility` and `AllowedGroups` fields for orchestration-layer verification. `TenantContext` extended with `UserGroups` (backward-compatible). `TenantContextMiddleware` extracts `groups` and `roles` claims from Entra ID JWT and populates `UserGroups`. API endpoints (`/api/chat`, `/api/sessions/{id}/messages`) inject JWT-extracted groups into requests (server-side groups take precedence over client-provided). Critical security log emitted if restricted content bypasses retrieval layer. Integration test proves restricted content never reaches `BuildSystemPrompt`. 13 new tests (10 restricted content exclusion unit + 3 middleware group extraction); all 580 tests passing.

- [x] P0-014A: Implement baseline PII detection and redaction in orchestration path.
  - Specs: jtbd-10
  - Dependencies: P0-013 (complete)
  - Completed: `IPiiRedactionService` interface + `PiiRedactionService` implementation in `SmartKb.Contracts.Services`. Regex-based redaction of 4 PII categories: emails → `[REDACTED-EMAIL]`, phone numbers → `[REDACTED-PHONE]`, SSNs → `[REDACTED-SSN]`, credit card numbers → `[REDACTED-CREDIT-CARD]`. Same regex patterns as `BaselineEnrichmentService.DetectPii` (kept in sync). `ChatOrchestrator.RedactPiiInChunks` static method applies redaction to both `ChunkText` and `ChunkContext` of retrieved chunks. Defense-in-depth: runs after ACL enforcement (step 3.5) and before prompt assembly (step 4), ensuring no PII reaches model context regardless of indexing pipeline behavior. Audit events: `pii.redaction` event written to immutable audit store via `IAuditEventWriter` when PII is redacted, with tenant/user/correlation context. `PiiRedactedCount` field added to `ChatResponse` for client transparency. `ChatOrchestrator` constructor now accepts `IPiiRedactionService` and `IAuditEventWriter` dependencies. `PiiRedactionService` registered as singleton in API DI. 17 new tests (10 PII redaction service unit + 7 RedactPiiInChunks integration including end-to-end proof that PII never reaches `BuildSystemPrompt`); all 597 tests passing. Advanced policy controls deferred to P2-001.

- [x] P0-015: Implement escalation recommendation + structured handoff draft object.
  - Specs: jtbd-08
  - Dependencies: P0-013 (complete), D-004 (resolved)
  - Completed: `EscalationDraftEntity` in SQL with `AddEscalationDrafts` migration (soft-delete, tenant-scoped, session FK, message reference). `EscalationRoutingRuleEntity` for per-tenant team routing (product area → target team, configurable threshold + min severity, unique active rule per product area per tenant). `EscalationSettings` config class with D-004 defaults (threshold=0.4, minSeverity=P2, fallback team="Engineering"). Severity ordering (P1>P2>P3>P4) with `MeetsSeverityThreshold` helper. `IEscalationDraftService` interface + `EscalationDraftService` implementation in `SmartKb.Data.Repositories`. Full CRUD: `POST /api/escalations/draft` (create draft from session message), `GET /api/escalations/draft/{draftId}` (get for review), `GET /api/sessions/{sessionId}/escalations/drafts` (list drafts for session), `PUT /api/escalations/draft/{draftId}` (update/edit before export), `GET /api/escalations/draft/{draftId}/export` (export as markdown), `DELETE /api/escalations/draft/{draftId}` (soft-delete). All endpoints require `chat:query` permission (SupportAgent, SupportLead, Admin). Structured handoff fields: title, customer_summary, steps_to_reproduce, logs_ids_requested, suspected_component, severity, evidence_links (JSON citations), target_team, reason. Routing rule lookup: when target team not specified, resolves from per-tenant `EscalationRoutingRules` table by suspected component; falls back to "Engineering". Audit event written on draft creation (`escalation.draft.created`). Markdown export includes all structured fields with evidence links. `ExportedAt` timestamp tracked. Tenant isolation + user ownership enforced on all operations. Phase 1: copy/export only (R-011); external ticket creation deferred to P1-003. Registered in API DI via `DataServiceExtensions`. 43 new tests (22 service unit + 4 settings + 17 endpoint integration); all 644 tests passing.

### P0 Frontend MVP

- [x] P0-016: Implement React agent chat with session continuity, confidence badge, citations, and Evidence Drawer.
  - Specs: jtbd-05
  - Dependencies: P0-013A (session API), P0-013 (complete)
  - Completed: Full React chat UI with MSAL Entra ID authentication (conditional — bypassed in local dev when `VITE_ENTRA_CLIENT_ID` not set). `AuthProvider` wraps app with `MsalProvider` + `AuthGate` (login redirect flow, silent token acquisition with `InteractionRequiredAuthError` fallback). API client layer (`api/client.ts`) with bearer token injection, `ApiResponse<T>` unwrapping, typed fetch for all session and chat endpoints. `SessionSidebar` component: session list with title, message count, time-ago display, create/select/delete actions, active highlight. `ChatThread` component: user and assistant message rendering, `ConfidenceBadge` (green High >=0.7, yellow Medium 0.4-0.7, red Low <0.4 per D-003), inline citation count button, next-steps list, escalation banner (purple, target team + reason). `EvidenceDrawer` slide-out panel: citation cards with title, snippet (4-line clamp), source system, formatted date, access label (colored: green Public, yellow Internal, red Restricted), external source link. `MessageInput` with Enter-to-send (Shift+Enter for newline), disabled state with "Thinking..." indicator. Typing dots animation during API call. `ChatPage` orchestrates: session CRUD via API, message send with response meta (nextSteps, escalation) stored in `metaMap`, auto-scroll, error banner, drawer state. `index.css` with CSS variables, responsive layout (sidebar 260px + main + drawer 340px). TypeScript strict mode, Vite env types for MSAL config. ESLint config (`.eslintrc.cjs`) with TS parser. 44 new frontend tests (3 App, 5 ConfidenceBadge, 9 EvidenceDrawer, 9 MessageInput, 9 ChatThread, 7 SessionSidebar, 2 API client); all passing. Build clean (191 modules, 7.3KB CSS + 440KB JS gzipped). All 644 backend tests still passing.

- [x] P0-017: Implement next-steps and escalation UX (CTA + handoff draft review/edit + copy/export).
  - Specs: jtbd-04, jtbd-05, jtbd-08
  - Dependencies: P0-015 (escalation draft API), P0-016 (chat UI)
  - Completed: `EscalationDraftModal` component with full draft lifecycle: auto-create draft on CTA click, editable form (title, severity P1-P4, target team, reason, customer summary, suspected component, steps to reproduce, logs/IDs requested), evidence links count, save via PUT, markdown export via GET with clipboard copy + "Copied!" feedback. `EscalationBanner` updated with "Create escalation draft" CTA button (purple, `btn-escalate`) that passes `messageId` up to `ChatPage`. `ChatPage` wires modal state: extracts escalation signal + citations for the target message, opens modal overlay. `AssistantMeta` extended with `handoffNote` field. API client layer: `createEscalationDraft`, `updateEscalationDraft`, `exportEscalationDraft`, `deleteEscalationDraft` + full TypeScript types matching backend DTOs. ADO/ClickUp buttons rendered disabled with "Coming soon" tooltip (R-011). Modal backdrop with centered card layout, responsive (max 90vw, 85vh). CSS: modal backdrop, form fields, field rows, evidence summary badge, coming-soon disabled style. Next-step guidance already rendered from P0-016 (no changes needed). 14 new frontend tests (12 EscalationDraftModal + 2 ChatThread CTA); 58 frontend tests passing. Build clean (192 modules, 9.4KB CSS + 447KB JS gzipped). All 644 backend tests passing.

- [x] P0-018: Implement feedback capture UI + API wiring.
  - Specs: jtbd-05, jtbd-06
  - Dependencies: P0-016 (chat UI), P0-013A (session/message API)
  - Completed: `FeedbackReasonCode` enum with 7 values (WrongAnswer, OutdatedInfo, MissingContext, WrongSource, TooVague, WrongEscalation, Other). `FeedbackEntity` extended with `UserId`, `ReasonCodesJson` (JSON multi-select), `Comment`, `CorrelationId`. `IFeedbackService` interface + `FeedbackService` implementation in `SmartKb.Data.Repositories`. Full CRUD: `POST /api/sessions/{id}/messages/{id}/feedback` (submit/update), `GET /api/sessions/{id}/messages/{id}/feedback` (get for message), `GET /api/sessions/{id}/feedbacks` (list for session). All endpoints require `chat:feedback` permission (SupportAgent, SupportLead, Admin). One feedback per message per user (upsert on resubmit). Audit event written on new feedback (`chat.feedback`). Trace ID linked from assistant message. `UpdateFeedbackSchema` migration (ReasonCodesJson, Comment, UserId, CorrelationId, TenantId+SessionId composite index). `FeedbackWidget` React component: thumbs up/down toggle, reason code multi-select checkboxes (7 options), optional comment textarea, optional corrected answer textarea, immediate submit on thumbs up, detail form on thumbs down, "Thanks for your feedback" confirmation. Wired into `ChatThread` message footer (after escalation banner) and `ChatPage` state management (feedbackMap). API client: `submitFeedback`, `getFeedback`. CSS: feedback widget styling (thumbs buttons, detail form, reason checkboxes). 26 new backend tests (14 FeedbackService unit + 12 FeedbackEndpoint integration); 670 backend tests passing. 9 new frontend tests (FeedbackWidget); 67 frontend tests passing. Build clean (193 modules, 10.7KB CSS + 450KB JS gzipped).

- [x] P0-018A: Implement outcome tracking API and UI.
  - Specs: jtbd-06, jtbd-08
  - Dependencies: P0-018 (feedback API), BUG-004 + BUG-005 (both fixed)
  - Completed: `IOutcomeService` interface + `OutcomeService` implementation in `SmartKb.Data.Repositories`. DTOs: `RecordOutcomeRequest`, `OutcomeResponse`, `OutcomeListResponse` in `SmartKb.Contracts.Models`. API endpoints: `POST /api/sessions/{id}/outcome` (create, returns 201 Created) and `GET /api/sessions/{id}/outcome` (list outcomes for session). Both require `chat:outcome` permission (SupportAgent, SupportLead, Admin). Tenant isolation + user ownership enforced. Audit event `chat.outcome` written on submission. DI registration in `DataServiceExtensions`. Frontend: `ResolutionType` type, `RecordOutcomeRequest`/`OutcomeResponse`/`OutcomeListResponse` TypeScript interfaces, `recordOutcome`/`getOutcomes` API client functions, `OutcomeWidget` React component (resolution type radio selector: ResolvedWithoutEscalation/Escalated/Rerouted, conditional target team input for Escalated/Rerouted, acceptance radio buttons, submit button, recorded confirmation state). Widget wired into `ChatPage` below `MessageInput`, visible when session has messages. CSS styles for outcome widget. 14 new backend unit tests (OutcomeServiceTests), 8 new API integration tests (OutcomeEndpointTests: CRUD, auth, RBAC, tenant isolation), 11 new frontend tests (OutcomeWidget). 691 backend + 78 frontend = 769 tests all passing.

- [x] P0-019: Implement admin connectors dashboard baseline.
  - Specs: jtbd-05, jtbd-07
  - Dependencies: P0-006 (connector admin API complete), P0-016 (React app base)
  - Completed: Full admin connector dashboard with role-based access control. `useRoles()` hook calls `/api/me` to fetch user roles; `hasAdminRole()` guard on `/admin` route shows "Access Denied" for non-Admin users. `ConnectorList` component: table with name, type, status (Enabled/Disabled badges), auth type, last sync status (Completed/Failed/Running/Pending/Never), updated timestamp; click-to-select rows; "New Connector" button. `CreateConnectorForm` wizard: 4-step flow (Type → Auth → Config → Review) with name input, connector type card selector (AzureDevOps, SharePoint, HubSpot, ClickUp), auth type dropdown (Pat, OAuth, ServiceAccount, PrivateKey), Key Vault secret name input, source config JSON textarea, schedule cron input (informational only), review summary, back/next navigation, create action. `ConnectorDetail` component: header with name + status badge + back button; action bar (Test Connection, Enable/Disable toggle, Sync Now, Backfill, Edit, Delete with confirmation); test connection result display (success/failure with diagnostic detail); info grid (type, auth, secret status, created date); edit mode with name, source config, schedule cron, field mapping editor; source config display in read-only mode. `FieldMappingEditor` component: table with source/target/transform/required columns; inline editing; add/remove rules; read-only mode for detail view. `SyncRunHistory` component: table with status, type (Backfill/Incremental), started, duration, records processed/failed, error detail. API client layer: 13 new functions (`listConnectors`, `getConnector`, `createConnector`, `updateConnector`, `deleteConnector`, `enableConnector`, `disableConnector`, `testConnection`, `syncNow`, `listSyncRuns`, `getSyncRun`, `validateMapping`, `getMe`). TypeScript types: 15 new interfaces/types matching backend DTOs. Navigation: "Admin" link in ChatPage header; "Back to Chat" link in admin header. CSS: full admin layout, connector table, wizard, detail, field mapping, sync history, test result, status badges. 32 new frontend tests (7 ConnectorList + 9 CreateConnectorForm + 5 FieldMappingEditor + 6 SyncRunHistory + 5 AdminPage role guard); 110 frontend tests passing. Build clean (200 modules, 18.2KB CSS + 479KB JS). All 691 backend tests passing. Phase 1 scope: preview pane and dead-letter/secrets status widget deferred to P1-008 (admin diagnostics).

### P0 Evaluation, SLOs, and Observability MVP

- [x] P0-020: Instrument OpenTelemetry + correlation IDs across all paths; wire audit event writes.
  - Specs: jtbd-06, jtbd-10
  - Dependencies: P0-013 (complete)
  - Completed: Full OpenTelemetry instrumentation across API and Ingestion. OTel SDK packages: `OpenTelemetry.Extensions.Hosting`, `.Instrumentation.AspNetCore`, `.Instrumentation.Http`, `.Instrumentation.SqlClient` (v1.12.0) in both API and Ingestion. Azure Monitor exporter: `Azure.Monitor.OpenTelemetry.AspNetCore` 1.3.0 in API, `Azure.Monitor.OpenTelemetry.Exporter` 1.3.0 in Ingestion — `APPLICATIONINSIGHTS_CONNECTION_STRING` now consumed. Named ActivitySources: `SmartKb.Api`, `SmartKb.Orchestration`, `SmartKb.Ingestion` in `SmartKb.Contracts.Diagnostics` with custom spans — orchestration pipeline (ChatOrchestrate, EmbedQuery, RetrieveEvidence, GenerateAnswer with model/confidence/citation tags), ingestion processing (ProcessSyncJob, SyncJobProcess with records/chunks/status tags). Correlation headers: `TenantContextMiddleware` extracts inbound `X-Correlation-Id` header (falls back to Activity.Current?.Id → TraceIdentifier), echoes correlation ID in response `X-Correlation-Id` header via `OnStarting` callback. Service Bus trace context: `IngestionWorker.ProcessMessageAsync` starts `ProcessSyncJob` activity with `messaging.correlation_id`, `messaging.message_id`, delivery count, sync run/connector/tenant tags; sets `ActivityStatusCode.Ok/Error`. ILogger→OTel bridge: `builder.Logging.AddOpenTelemetry()` with `IncludeScopes` and `IncludeFormattedMessage` in both API and Ingestion — all structured logs automatically enriched with TraceId/SpanId. AuditEventType constants: `SmartKb.Contracts.AuditEventTypes` static class with 20 constants; all 20+ audit event writes across 10 files migrated from string literals to constants. All existing audit events preserved (20+ types covering queries, retrieval, answers, escalations, admin changes, cross-tenant denials, PII redaction, webhooks, sync). 36 new tests (24 AuditEventTypes + 7 Diagnostics/ActivitySource + 3 correlation header middleware + 2 OTel wiring integration); all 727 backend + 110 frontend = 837 tests passing.

- [x] P0-020A: Implement audit log query and export API.
  - Specs: jtbd-10
  - Dependencies: P0-020 (complete), D-011 (resolved)
  - Completed: `IAuditEventQueryService` interface + `AuditEventQueryService` implementation in `SmartKb.Data.Repositories`. DTOs: `AuditEventQueryRequest` (eventType, actorId, correlationId, from/to date range, page/pageSize), `AuditEventListResponse` (events, totalCount, page, pageSize, hasMore), `AuditExportCursor` (cursor-based pagination with afterTimestamp/afterId, filters, limit). `GET /api/audit/events` endpoint: paginated query with all filters, returns `ApiResponse<AuditEventListResponse>`, requires `audit:read` permission (Admin, SecurityAuditor). `GET /api/audit/events/export` endpoint: NDJSON streaming export with cursor-based pagination, returns `application/x-ndjson` content type, cursor metadata appended as last line with `__cursor`, `afterTimestamp`, `afterId`, `hasMore` fields; requires `audit:export` permission (Admin, SecurityAuditor). Page size clamped to 200 max, export batch clamped to 5000 max. Tenant isolation enforced on all queries. `InMemoryAuditEventQueryService` for no-DB fallback path. DI registration in `DataServiceExtensions` (scoped) + all test factories updated. 27 new tests (15 AuditEventQueryService unit + 12 AuditEndpoint integration); all 754 backend + 110 frontend = 864 tests passing.

- [x] P0-021: Implement baseline evaluation harness.
  - Specs: jtbd-06
  - Dependencies: P0-013 (complete), D-007 (resolved)
  - Completed: `SmartKb.Eval` library project in `src/SmartKb.Eval/` with full evaluation harness. Gold dataset schema defined (`eval/gold-dataset/schema.json`) and 30 seed cases authored (`eval/gold-dataset/baseline.jsonl`) covering 10 categories (auth, billing, integrations, data, indexing, security, ingestion, admin, compliance, general) with escalation, safety, and out-of-scope cases. `EvalCase` model with JSONL deserialization; `EvalExpected` supports response_type, must_include, must_not_include, must_cite_sources, min_citations, expected_escalation (recommended+target_team), min_confidence, should_have_evidence. `GoldDatasetLoader` with JSONL file loading, string loading (for tests), per-case validation (ID format eval-NNNNN, required fields, response type enum, confidence range), duplicate ID detection. `MetricCalculator` computes per-case `CaseMetrics` (response type match, groundedness, citation coverage, escalation match, routing match, evidence match, must-include hit rate, safety pass, confidence met) and `AggregateMetrics` (groundedness, citation coverage, routing accuracy, no-evidence rate, response type accuracy, must-include hit rate, safety pass rate, average confidence, average duration). `EvalRunner` supports live orchestration (via `IChatOrchestrator`) and offline evaluation from pre-recorded results (`BuildReportFromResults`). `BaselineComparator` loads/saves JSON baseline files; compares current vs baseline with higher-is-better (groundedness, citation coverage, routing accuracy, response type accuracy, safety pass rate, must-include hit rate) and lower-is-better (no-evidence rate) regression detection; severity levels: ok (< 2%), warning (2-5%), blocking (> 5%). `ThresholdChecker` validates aggregate metrics against configurable thresholds (groundedness >= 0.80, citation coverage >= 0.70, routing accuracy >= 0.60, no-evidence rate <= 0.25) and enforces minimum case count (30) for gated release. `EvalSettings` configurable class with validation. `EvalReport` and `EvalBaseline` models with JSON serialization roundtrip. Projects added to solution. 68 new tests (6 settings, 16 dataset loader, 25 metric calculator, 10 baseline comparator, 7 threshold checker, 4 eval runner); all 822 backend + 110 frontend = 932 tests passing. D-007 resolved.

- [x] P0-022: Implement SLO dashboards and alerts.
  - Specs: jtbd-06, jtbd-10
  - Dependencies: P0-020 (complete)
  - Completed: Full SLO instrumentation and alerting infrastructure. `SloSettings` config class with 7 configurable thresholds (P95 answer-ready 8s, availability 99.5%, P95 sync lag 15min, no-evidence rate 25%, dead-letter depth 10, alert evaluation window 5min, PII redaction spike 50). 11 OTel Meter instruments in `SmartKb.Contracts.Diagnostics`: 4 histograms (`smartkb.chat.latency_ms`, `smartkb.chat.confidence`, `smartkb.ingestion.sync_duration_ms`) + 7 counters (`smartkb.chat.requests_total`, `smartkb.chat.no_evidence_total`, `smartkb.ingestion.sync_completed_total`, `smartkb.ingestion.sync_failed_total`, `smartkb.ingestion.dead_letter_total`, `smartkb.ingestion.records_processed_total`, `smartkb.security.pii_redactions_total`, `smartkb.security.restricted_content_blocked_total`). Metrics wired into `ChatOrchestrator` (latency, request count, no-evidence, confidence, PII redaction, restricted content) and `SyncJobProcessor` (sync duration, completion/failure, records processed) and `IngestionWorker` (dead-letter). Custom `SmartKb` meter registered in both API and Ingestion OTel SDK configurations. `GET /api/admin/slo/status` endpoint returns SLO targets + metric names for dashboard consumption (requires `connector:manage` permission). Terraform `alerts.tf`: `azurerm_monitor_action_group` + 6 `azurerm_monitor_metric_alert` rules — P95 chat latency, API availability, dead-letter depth, API 5xx, ingestion 5xx, ingestion backlog. All alerts disabled in dev, enabled in staging/prod. 5 new Terraform variables for alert thresholds. ARM `main.json`: matching `Microsoft.Insights/actionGroups` + 6 `Microsoft.Insights/metricAlerts` resources with 5 new parameters. ARM JSON validated. IaC parity maintained (33 Terraform resources, 33 ARM resources). 20 new tests (3 SloSettings + 14 Diagnostics meters + 3 SLO endpoint integration); all 842 backend + 110 frontend = 952 tests passing.

## Phase 2 (V1) Priority Queue

- [x] P1-001: Add HubSpot connector.
  - Specs: jtbd-01, jtbd-07
  - Completed: Third `IConnectorClient` implementation — `HubSpotConnectorClient` in `SmartKb.Contracts.Connectors`. Uses HubSpot CRM v3 REST API with Bearer token auth (PAT/OAuth). Ingests tickets, contacts, companies, deals via configurable `ObjectTypes`. `HubSpotSourceConfig` model with portalId, baseUrl, objectTypes, customProperties, pipelines, batchSize (max 100). Checkpoint-based multi-object-type sync (`HubSpotCheckpoint` tracks objectTypeIndex + afterCursor + lastModified). Backfill via list API (`GET crm/v3/objects/{type}`); incremental via search API (`POST crm/v3/objects/{type}/search` with `hs_lastmodifieddate >= GTE` filter). HubSpot pagination support (paging.next.after cursor → HasMore). Maps CRM objects to `CanonicalRecord`: tickets→SourceType.Ticket, contacts/companies/deals→SourceType.Document. Deep links to `https://app.hubspot.com/contacts/{portalId}/{type}/{id}`. Priority mapping: HIGH→P1, MEDIUM→P2, LOW→P3. Stage mapping: closed/closedwon/closedlost→EvidenceStatus.Closed. ACL: all objects→Internal visibility (HubSpot CRM has no per-record ACL model). Content hash for dedup. Per-record error isolation. Default properties per object type (subject/content for tickets, firstname/lastname/email for contacts, name/description for companies, dealname/description for deals). `HubSpotWebhookManager` registers webhook subscriptions per object type (creation + propertyChange events) via HubSpot Webhooks API v3. HMAC-SHA256 signature verification (`X-HubSpot-Signature-v3` header, constant-time comparison). `HubSpotWebhookHandler` processes webhook event arrays, deduplicates via eventId idempotency key, triggers incremental sync via Service Bus. Anonymous endpoint `POST /api/webhooks/hubspot/{connectorId}`. Webhook lifecycle managed in `ConnectorAdminService`: register on enable, deregister on disable. Reuses `WebhookPollingFallbackService` for failure recovery (3 consecutive failures threshold). `HubSpotWebhookEvent`, `HubSpotWebhookSubscriptionRequest/Response` models for webhook protocol. Registered in both API and Ingestion DI. 62 new tests (40 connector client unit + 17 webhook manager unit/signature + 5 endpoint integration); all 904 backend + 110 frontend = 1014 tests passing.

- [x] P1-002: Add ClickUp connector.
  - Specs: jtbd-01, jtbd-07
  - Completed: Fourth `IConnectorClient` implementation — `ClickUpConnectorClient` in `SmartKb.Contracts.Connectors`. Uses ClickUp REST API v2 with Bearer token auth (PAT/OAuth). Ingests tasks via configurable workspace → space → folder → list hierarchy. `ClickUpSourceConfig` model with workspaceId, spaceIds, folderIds, listIds, ingestTasks, ingestDocs, taskStatuses, batchSize (max 100). Automatic list resolution: walks workspace → spaces → folders → lists when specific IDs not configured. Checkpoint-based multi-list pagination (`ClickUpCheckpoint` tracks listIndex + page + lastModified). Backfill via list tasks API (`GET api/v2/list/{id}/task`); incremental via `date_updated_gt` filter. Maps ClickUp tasks to `CanonicalRecord`: SourceType.Task, deep links to `app.clickup.com/t/{id}`. Priority mapping: 1=Urgent→P1, 2=High→P2, 3=Normal→P3, 4=Low→P4. Status mapping: closed/done→EvidenceStatus.Closed. ACL: all tasks→Internal visibility (ClickUp workspace-level access model). Content hash for dedup. Per-record error isolation. Uses text_content (plain text) with fallback to description. `ClickUpWebhookManager` registers webhooks per workspace via ClickUp Webhooks API v2 (`POST api/v2/team/{workspaceId}/webhook`). HMAC-SHA256 signature verification (`X-Signature` header, constant-time comparison). 5 supported event types: taskCreated, taskUpdated, taskDeleted, taskStatusUpdated, taskCommentPosted. Single webhook registration with multiple events (ClickUp model). Deregistration deduplicates webhook IDs. `ClickUpWebhookHandler` processes single-event payloads with clientState validation, deduplicates via idempotency key, triggers incremental sync via Service Bus. Anonymous endpoint `POST /api/webhooks/clickup/{connectorId}`. Webhook lifecycle managed in `ConnectorAdminService`: register on enable, deregister on disable. Reuses `WebhookPollingFallbackService` for failure recovery (3 consecutive failures threshold). `ClickUpWebhookEvent`, `ClickUpWebhookCreateRequest/Response` models for webhook protocol. Registered in both API and Ingestion DI. 61 new tests (35 connector client unit + 17 webhook manager unit/signature + 4 event resolution + 5 endpoint integration); all 965 backend + 110 frontend = 1075 tests passing.

- [x] P1-003: Implement external draft escalation creation in ADO/ClickUp after human approval.
  - Specs: jtbd-08
  - Completed: Human-in-the-loop escalation approval → external work item creation. `IEscalationTargetConnector` interface in `SmartKb.Contracts.Services` with `CreateExternalWorkItemAsync` method, `ExternalWorkItemRequest`/`ExternalWorkItemResult` records. Both `AzureDevOpsConnectorClient` and `ClickUpConnectorClient` now implement both `IConnectorClient` and `IEscalationTargetConnector` (shared DI singleton via factory delegates). ADO: builds JSON Patch operations (Title, Description, AreaPath, Priority) and calls PATCH `{project}/_apis/wit/workitems/${workItemType}?api-version=7.1`; returns deep link URL. ClickUp: resolves target list from request/config/workspace walk and POSTs to `api/v2/list/{listId}/task` with priority mapping (P1→1, P2→2, P3→3, P4→4). Severity-to-priority mapping shared across both connectors. `EscalationDraftEntity` extended with 8 new columns: ApprovedAt, ApprovedBy, TargetConnectorId, TargetConnectorType, ExternalId, ExternalUrl, ExternalStatus, ExternalErrorDetail. EF Core migration `AddExternalEscalationTracking` adds columns + FK to Connectors table. `ApproveAndCreateExternalAsync` in `EscalationDraftService`: validates draft ownership, checks connector exists/enabled/supported type, resolves `IEscalationTargetConnector` by type, fetches Key Vault secret, builds structured markdown description, records approval, calls connector, updates entity with result, writes audit events (`escalation.draft.approved`, `escalation.external.created`/`escalation.external.failed`). Idempotency: if `ExternalStatus == "Created"`, returns cached result without calling connector again. `POST /api/escalations/draft/{draftId}/approve` endpoint with `chat:query` permission. Frontend: `EscalationDraftModal` updated with connector selector dropdown (loads via `listConnectors`), conditional target project/list inputs by connector type, save-then-approve flow, success/error banners, disabled placeholder buttons when no ADO/ClickUp connectors configured. New API types: `ApproveEscalationDraftRequest`, `ExternalEscalationResult`. New CSS: `.external-creation-section`, `.external-creation-success`, `.external-creation-error`. 39 new tests (10 service unit for approve flow + 11 ADO external creation + 12 ClickUp external creation + 3 audit event constants + 3 frontend); all 1004 backend + 110 frontend = 1114 tests passing.

- [x] P1-004: Implement Case-Pattern Store index and retrieval fusion with Evidence Store.
  - Specs: jtbd-02, jtbd-03, jtbd-09
  - Completed: Two-store retrieval fusion (Evidence + Pattern indexes). `TrustLevel` enum (Draft/Reviewed/Approved/Deprecated) in `SmartKb.Contracts.Enums`. `CasePattern` domain model with full structured fields: problem_statement, symptoms, diagnosis_steps, resolution_steps, verification_steps, workaround, escalation_criteria, related_evidence_ids, applicability_constraints, exclusions, version, supersedes_pattern_id. `CasePatternEntity` in SQL with `AddCasePatterns` migration (soft-delete, tenant-scoped, unique PatternId filtered index, composite indexes on TenantId+TrustLevel/ProductArea/UpdatedAt). `PatternFieldNames` constants for Pattern index schema. `IPatternIndexingService` + `AzureSearchPatternIndexingService` — Pattern index with 17 fields: pattern_id (key), 4 searchable text (title, problem_statement, symptoms, resolution_steps with EnMicrosoft analyzer), pattern_embedding_vector (1536 dims, HNSW cosine), 7 filterable metadata (tenant_id, trust_level, product_area, updated_at, confidence, tags, version), 3 ACL (visibility, allowed_groups, access_label), source_url. Separate HNSW algorithm config (`pattern-hnsw-config`) and semantic config (`pattern-semantic-config`). `FusedRetrievalService` implements `IRetrievalService` — queries Evidence and Pattern indexes in parallel, applies 3-layer boosting (trust_level: approved=1.5x/reviewed=1.2x/draft=0.8x/deprecated=0.3x; recency: <=30d=1.2x/30-90d=1.0x/>90d=0.8x; authority: pattern=1.3x base boost), enforces diversity constraint (max 3 results per source ID), merges by boosted score, applies ACL security trimming (same 2-layer defense-in-depth). Pattern results mapped to `RetrievedChunk` with composite chunk text (problem_statement + resolution_steps) and symptoms as context. `RetrievedChunk` extended with `ResultSource` (Evidence/Pattern), `TrustLevel`, `BoostedScore`. `RetrievalResult` extended with `PatternCount`. `RetrievalSettings` extended with 10 fusion-specific config fields (EnablePatternFusion, PatternTopK, trust boosts, recency boosts, PatternAuthorityBoost, DiversityMaxPerSource). `SearchServiceSettings` extended with `PatternIndexName` (default "patterns"). Deprecated patterns filtered out at query time (OData filter: trust_level ne 'Deprecated'). Graceful degradation: if one index fails, other index results still returned. DI: `FusedRetrievalService` registered as `IRetrievalService` when `EnablePatternFusion=true`; `AzureSearchRetrievalService` preserved as fallback when disabled. `IPatternIndexingService` registered in both API and Ingestion. 54 new tests (7 ACL filtering, 6 trust boost, 5 recency boost, 4 diversity constraint, 2 OData escaping, 4 DTO mapping/fields, 5 PatternFieldNames, 8 RetrievalSettings defaults, 3 CasePattern model, 1 TrustLevel enum, 6 index schema, 2 document mapping, 1 SearchServiceSettings); all 1058 backend + 110 frontend = 1168 tests passing.

- [x] P1-005: Implement solved-ticket pattern distillation pipeline.
  - Specs: jtbd-02, jtbd-09
  - Dependencies: D-008 (resolved)
  - Completed: `IPatternDistillationService` interface + `PatternDistillationService` implementation in `SmartKb.Data.Repositories`. D-008 resolved: solved-ticket criteria = Status in (Closed, Resolved) AND ResolutionType = ResolvedWithoutEscalation AND positive_feedback >= MinPositiveFeedback (default 1). `DistillationSettings` config class with 8 configurable fields (MinPositiveFeedback, SolvedStatuses, MaxCandidates, MaxBatchSize, MinCitedChunks, BaseConfidence, PositiveFeedbackBoost, NegativeFeedbackPenalty, MaxConfidence). Candidate discovery pipeline: queries OutcomeEvents for ResolvedWithoutEscalation sessions → joins Feedbacks for ThumbsUp count filtering → traces Messages via CorrelationId to AnswerTraces for cited chunk IDs → loads EvidenceChunk metadata (product area, tags, error tokens) → checks existing CasePatterns for dedup via `session://{sessionId}` source URL convention. `DistillationCandidate` DTO with session context, cited evidence/chunk IDs, feedback counts, product area, tags, already-distilled flag. Candidates ordered by positive feedback descending, then evidence count. Pattern extraction: BuildTitle from session title → chunk title fallback; BuildProblemStatement from first chunk text (500 char cap); ExtractSymptoms from error tokens + chunk titles (deduped, max 10); ExtractResolutionSteps from chunk texts (200 char cap per step, max 10); ExtractDiagnosisSteps from chunk contexts; ExtractVerificationSteps baseline guidance. Confidence scoring: base (0.5) + extra positive feedback boost (0.05/each) - negative feedback penalty (0.1/each) + evidence count bonus (3+ = +0.05), clamped to [0.1, 0.9]. ACL: most-restrictive visibility across cited chunks; merged allowed groups. Patterns created at Draft trust level (version 1). Optional embedding generation via `IEmbeddingService` and indexing via `IPatternIndexingService` (graceful degradation if unavailable). Source URL `session://{sessionId}` for lineage and idempotency. Audit events: `pattern.distilled` per pattern, `pattern.distillation_run` per batch. API endpoints: `GET /api/admin/patterns/candidates` (list candidates), `POST /api/admin/patterns/distill` (trigger distillation batch). Both require `connector:manage` permission (Admin). Registered in API DI via `DataServiceExtensions`. 35 new tests (26 PatternDistillationService unit + 4 DistillationSettings + 7 DistillationEndpoint integration — candidates, distill, auth, RBAC, idempotency); all 1101 backend tests passing.

- [x] P1-006: Implement pattern governance workflows.
  - Specs: jtbd-09, jtbd-10
  - Completed: Full pattern governance workflow with 4-state trust-level model (Draft → Reviewed → Approved → Deprecated). `IPatternGovernanceService` interface + `PatternGovernanceService` implementation in `SmartKb.Data.Repositories`. Valid state transitions enforced: Draft→{Reviewed,Approved,Deprecated}, Reviewed→{Approved,Deprecated}, Approved→{Deprecated}, Deprecated→{} (terminal). `CasePatternEntity` extended with 9 governance tracking fields: ReviewedAt/ReviewedBy/ReviewNotes, ApprovedAt/ApprovedBy/ApprovalNotes, DeprecatedAt/DeprecatedBy/DeprecationReason. EF Core migration `AddPatternGovernanceTracking`. Governance DTOs: `PatternSummary` (queue view), `PatternDetail` (full view), `PatternGovernanceQueueResponse` (paginated), `PatternGovernanceResult` (transition result), `ReviewPatternRequest`/`ApprovePatternRequest`/`DeprecatePatternRequest`. 5 API endpoints: `GET /api/patterns/governance-queue` (paginated, filterable by trustLevel/productArea, requires `pattern:approve`), `GET /api/patterns/{patternId}` (full detail, requires `pattern:approve`), `POST /api/patterns/{patternId}/review` (Draft→Reviewed, requires `pattern:approve`), `POST /api/patterns/{patternId}/approve` (Draft/Reviewed→Approved, requires `pattern:approve`), `POST /api/patterns/{patternId}/deprecate` (any→Deprecated with optional supersedingPatternId, requires `pattern:deprecate`). Audit events: `pattern.reviewed`, `pattern.approved`, `pattern.deprecated` written on each transition with actor/tenant/correlation context. Optional re-indexing in Pattern Store after trust-level transition (via `IPatternIndexingService` if available). Tenant isolation enforced on all queries and transitions. Frontend: `PatternGovernancePage` with role guard (Admin/SupportLead), `PatternList` component (table with trust-level badges, filter dropdown, pagination), `PatternDetailView` component (full pattern content, governance actions, deprecation form with reason/superseding-pattern, governance history timeline). Route `/patterns` added to React router. Navigation links added to ChatPage and AdminPage headers. CSS: trust-level badges (Draft=indigo, Reviewed=amber, Approved=green, Deprecated=red), governance queue table, pattern detail grid, deprecate form, governance history. Registered in API DI via `DataServiceExtensions`. 26 new backend unit tests (PatternGovernanceServiceTests: 8 queue, 3 detail, 4 review, 4 approve, 5 deprecate, 2 cross-cutting), 13 new API integration tests (GovernanceEndpointTests: queue, detail, RBAC, transitions, invalid transitions), 20 new frontend tests (8 PatternList + 12 PatternDetailView); all 1140 backend + 130 frontend = 1270 tests passing.

- [x] P1-007: Add advanced retrieval filters and tuning controls.
  - Specs: jtbd-03, jtbd-07
  - Completed: Full retrieval filtering and per-tenant tuning system. `RetrievalFilter` model with 5 optional filter dimensions: SourceTypes, ProductAreas, TimeHorizonDays, Tags, Statuses (plus `IsEmpty` helper). `ODataFilterBuilder` static utility builds OData filter expressions for Azure AI Search — separate `BuildEvidenceFilter()` (all 5 dimensions) and `BuildPatternFilter()` (3 dimensions, patterns lack source_type/status fields). `search.in()` for multi-value string fields, `any()` lambda for collection fields (tags), date comparison for time horizon. OData value escaping (single quotes doubled, commas stripped in search.in values). `CombineFilters()` joins tenant base filter with user filters via AND. `IRetrievalService` extended with `RetrievalFilter? filters = null` parameter (backward-compatible). `AzureSearchRetrievalService` and `FusedRetrievalService` both apply filters — Evidence index gets all 5 filter types, Pattern index gets product_area + time_horizon + tags only. `ChatRequest` and `SendMessageRequest` DTOs extended with `Filters` property. `ChatOrchestrator` passes filters to retrieval. `SessionService` forwards filters to ChatOrchestrator. Per-tenant retrieval tuning: `TenantRetrievalSettingsEntity` with nullable override fields for all `RetrievalSettings` properties (TopK, EnableSemanticReranking, EnablePatternFusion, PatternTopK, trust/recency/authority boosts, DiversityMaxPerSource, NoEvidenceScoreThreshold, NoEvidenceMinResults). `ITenantRetrievalSettingsService` + `TenantRetrievalSettingsService` implementation — nullable entity fields merge with global `RetrievalSettings` defaults via null-coalescing. EF Core migration `AddTenantRetrievalSettings`. 3 admin API endpoints: `GET/PUT/DELETE /api/admin/retrieval-settings` (all require `connector:manage` permission). `RetrievalSettingsResponse` includes `HasOverrides` flag. Frontend: `RetrievalFilterPanel` component — collapsible panel with source type chips (Ticket/Document/WikiPage/Task/CasePattern), time horizon dropdown (7/30/90/180/365 days), product areas text input, tags text input. Active filter indicator (asterisk), clear-all button. Integrated into `ChatPage` above `MessageInput`, filters passed to `api.sendMessage()`. API client extended with `getRetrievalSettings()`, `updateRetrievalSettings()`, `resetRetrievalSettings()`. CSS styles for filter panel, chips, inputs. 28 new `ODataFilterBuilderTests` (evidence filter, pattern filter, combine, escaping, IsEmpty), 10 new `TenantRetrievalSettingsServiceTests` (defaults, overrides, update, reset, isolation, boost fields), 7 new `RetrievalSettingsEndpointTests` (CRUD, RBAC, auth), 14 new `RetrievalFilterPanel.test.tsx` (render, expand, chips, toggle, time, clear, indicator). Test factories updated with `ITenantRetrievalSettingsService` + `RetrievalSettings` DI registrations; all 1185 backend + 144 frontend = 1329 tests passing.

- [x] P1-008: Expand admin diagnostics.
  - Specs: jtbd-07
  - Completed: Full admin diagnostics dashboard with 3 tabs (Overview, Webhooks, Dead Letters). `IWebhookStatusService` interface + `WebhookStatusService` implementation in `SmartKb.Data.Repositories` — queries webhook subscription health per connector or tenant-wide, with status counts (active, fallback, failing). `DiagnosticsSummaryResponse` aggregates connector health (sync status, webhook counts, failure counts), service configuration status (Service Bus, Key Vault, OpenAI, Search). 3 new API endpoints: `GET /api/admin/connectors/{id}/webhooks` (per-connector webhook status), `GET /api/admin/webhooks` (all webhooks for tenant), `GET /api/admin/diagnostics/summary` (aggregated diagnostics with service health). All require `connector:manage` permission (Admin). Rate-limit tracking: `smartkb.ingestion.source_rate_limit_total` OTel counter added to `Diagnostics.cs`, incremented in `SyncJobProcessor` on HTTP 429 responses, exposed in SLO status endpoint metrics. Frontend: `DiagnosticsPage` component with role guard (Admin), 3-tab navigation (Overview/Webhooks/Dead Letters). Overview tab: connector count card, webhook health card, service status indicators (green/red dots), SLO targets table, secrets status with model name, connector health table with last sync status and webhook failure counts. Webhooks tab: subscription table with connector name, event type, status badges (Healthy/Fallback/Degraded/Inactive), consecutive failures, last delivery time, next poll time. Dead Letters tab: message table with ID, subject, reason, delivery count, enqueued time; expandable detail rows showing correlation ID, error description, message body. Route `/diagnostics` added to React router. Navigation links added to AdminPage and DiagnosticsPage headers (Connectors, Diagnostics, Patterns, Back to Chat). API client: `getWebhooksByConnector()`, `getAllWebhooks()`, `getDiagnosticsSummary()`, `getDeadLetters()`, `getSloStatus()`, `getSecretsStatus()`. TypeScript types: 8 new interfaces matching backend DTOs. CSS: 40+ styles for diagnostics tabs, cards, tables, badges, service status dots, detail boxes, code blocks. DI: `WebhookStatusService` registered as scoped in `DataServiceExtensions` + all test factories updated. 20 new backend tests (10 WebhookStatusServiceTests: per-connector, all, tenant isolation, field mapping, diagnostics summary, connector health sync status; 7 DiagnosticsEndpointTests: webhook by connector, all webhooks, diagnostics summary, RBAC, auth, rate-limit metric; 3 DiagnosticsRateLimitTests: counter exists, can record, meter name). 13 new frontend tests (DiagnosticsPage: loading, access denied, overview panel, SLO targets, connector health, webhook tab/badges, dead-letter tab/expand/empty, error banner, tabs, secrets status). All 1205 backend + 157 frontend = 1362 tests passing.

- [x] P1-009: Add outcome-driven routing improvement loop.
  - Specs: jtbd-06, jtbd-08
  - Dependencies: P0-018A (complete)
  - Completed: Full outcome-driven routing improvement system. `RoutingAnalyticsSettings` config class (DefaultWindowDays=30, MinOutcomesForRecommendation=5, RerouteRateThreshold=0.3, LowAcceptanceRateThreshold=0.5, MinRecommendationConfidence=0.5). `IRoutingAnalyticsService` + `RoutingAnalyticsService` — computes per-tenant routing quality metrics from outcome data: team-level metrics (acceptance rate, reroute rate, avg time-to-assign, avg time-to-resolve), product-area metrics (joins outcomes with escalation drafts via EscalationTraceId to correlate SuspectedComponent with routing decisions), tenant-wide summary (self-resolution rate, overall acceptance/reroute rates). Configurable time window (default 30 days). `IRoutingRuleService` + `RoutingRuleService` — full admin CRUD for `EscalationRoutingRuleEntity` (previously only created via migrations). 5 API endpoints: `GET/POST/PUT/DELETE /api/admin/routing-rules` + `GET /api/admin/routing-rules/{id}`. Severity normalization and audit events on all mutations. `RoutingRecommendationEntity` in SQL with `AddRoutingRecommendations` migration (tenant-scoped, typed recommendations, status lifecycle: Pending→Applied/Dismissed, applied-by tracking). `IRoutingImprovementService` + `RoutingImprovementService` — analyzes outcome patterns and generates actionable recommendations: high reroute rate (>30%) triggers TeamChange recommendation with suggested team from rerouted-outcome analysis; low acceptance rate (<50%) triggers ThresholdAdjust recommendation to lower escalation confidence threshold. Confidence scoring: 0.4×volumeFactor(log2) + 0.6×signalStrength. Idempotent generation (skips product areas with existing pending recommendations). `ApplyRecommendationAsync`: applies recommendation to routing rule (creates rule if none exists), records audit trail. `DismissRecommendationAsync`: marks as dismissed with actor tracking. 6 routing analytics/recommendation API endpoints: `GET /api/admin/routing/analytics` (with optional windowDays), `POST /api/admin/routing/recommendations/generate`, `GET /api/admin/routing/recommendations` (with optional status filter), `POST .../apply` (with optional overrides), `POST .../dismiss`. All 11 new endpoints require `connector:manage` permission (Admin). 6 new audit event types: `routing.rule.created/updated/deleted`, `routing.recommendation.generated/applied/dismissed`. DTOs: `RoutingAnalyticsSummary`, `TeamRoutingMetrics`, `ProductAreaRoutingMetrics`, `RoutingRecommendationDto`, `RoutingRuleDto`, `CreateRoutingRuleRequest`, `UpdateRoutingRuleRequest`, `ApplyRecommendationRequest`. Registered in API DI via `DataServiceExtensions`. 7 new RoutingAnalyticsServiceTests (empty outcomes, mixed outcomes, team metrics grouping, avg time-to-assign, tenant isolation, window filtering, product area join). 10 new RoutingRuleServiceTests (CRUD, audit, partial update, severity normalization, tenant isolation). 13 new RoutingImprovementServiceTests (high reroute→TeamChange, low acceptance→ThresholdAdjust, insufficient data, duplicate prevention, apply team change, apply threshold adjust, re-apply rejected, dismiss, status filter, confidence computation). 14 new RoutingEndpointTests (CRUD, RBAC, auth, analytics, recommendations, 404 cases). All 1248 backend tests passing.

- [x] P1-010: Implement IaC drift checks.
  - Specs: jtbd-11
  - Completed: Full IaC drift detection and Terraform↔ARM parity checking system. `infra/scripts/check_parity.py` — standalone Python parity checker that parses Terraform `.tf` files (regex extraction of `resource` blocks) and ARM `main.json` (recursive JSON walk including nested children), maps Terraform types to ARM types via `TF_TO_ARM_TYPE` dictionary (17 mappings), compares resource type counts, and reports mismatches with severity levels (error/warning). Handles excluded types (`azurerm_resource_group` — deployment scope), intermediate ARM types (`blobServices`, `administrators`), and unmapped type warnings. CLI with `--verbose`, `--json` output modes. Exit codes: 0 (pass), 1 (parity errors), 2 (file/parse error). `report_to_dict()` for programmatic JSON consumption. `infra/scripts/test_check_parity.py` — 31 unit tests covering: TF parsing (resource extraction, multi-file, line numbers, non-tf file exclusion, empty dir, missing dir), ARM parsing (top-level, nested children, missing file, empty), parity checking (match, missing-in-ARM, missing-in-TF, count mismatch, resource group exclusion, intermediate type exclusion, unmapped type warning, role assignment parity/mismatch), report formatting (pass, fail, verbose, warnings-only), JSON output (structure, serializable), mapping completeness (all project TF types mapped, project parity passes), CLI entry point (pass→0, fail→1, missing→2, JSON mode). `.github/workflows/infra-validate.yml` updated with `parity-check` job — runs parity checker with verbose output + runs parity checker unit tests on every infra PR/push. `.github/workflows/drift-detection.yml` — scheduled weekly (Monday 06:00 UTC) + manual dispatch. Two jobs: (1) parity-check runs checker and uploads JSON artifact; (2) terraform-drift runs `terraform plan -detailed-exitcode` against live Azure environment (requires ARM_CLIENT_ID/SECRET/SUBSCRIPTION_ID secrets), evaluates exit code (0=no drift, 2=drift detected), auto-creates GitHub issue with plan output on drift (labels: infrastructure, drift), uploads plan artifact. Environment selectable via workflow_dispatch (dev/staging/prod). 31 parity checker tests passing; all 1248 backend tests passing.

- [x] P1-011: Implement richer enrichment + case-card quality hardening.
  - Specs: jtbd-02
  - Completed: Full Phase 2 enrichment upgrade and case-card quality gate system. **EnhancedEnrichmentService (v2)** — replaces BaselineEnrichmentService as the default `IEnrichmentService` in both API and Ingestion DI. Weighted multi-signal category detection: scores each category (bug, feature_request, incident, question, task, documentation) by counting matching keyword signals, picks the highest-scoring category, and computes a confidence ratio (0.0-1.0) reflecting signal strength vs. noise. Technology/framework detection: 24 technology patterns (Azure SQL, Service Bus, Key Vault, AI Search, DevOps, Functions, App Service, .NET, Entity Framework, React, TypeScript, Node.js, Python, Terraform, Docker, Kubernetes, SQL Server, Redis, RabbitMQ, Entra ID, SharePoint, HubSpot, ClickUp) — detected tags stored in `EnrichmentResult.TechnologyTags` and propagated through `NormalizationPipeline` to `EvidenceChunk.TechnologyTags`. Component/module extraction: regex-based detection of explicit labels ("Component: X", "Module: Y", "Service: Z") and .NET namespace-style references (e.g., "SmartKb.Api.Controllers") — stored in `EnrichmentResult.Component`. Product area inference: when `CanonicalRecord.ProductArea` is null, infers from 11 domain patterns (Authentication, Networking, Storage, Database, API, CI/CD, Monitoring, Security, Frontend, Infrastructure, Ingestion) — requires ≥2 keyword matches for confidence. `EnrichmentResult` extended with `TechnologyTags`, `Component`, `CategoryConfidence` fields. `EvidenceChunk` extended with `TechnologyTags` field. `EnrichmentVersion` bumped from 1 to 2. **CaseCardQualityValidator** — `ICaseCardQualityValidator` interface + `CaseCardQualityValidator` implementation. `CaseCardQualitySettings` config class (section: `CaseCardQuality`) with configurable thresholds: MinTitleLength=10, MaxTitleLength=200, MinProblemStatementLength=20, MinSymptomCount=1, MinResolutionStepCount=1, MinResolutionStepLength=10, MinRelatedEvidenceCount=1, MinQualityScore=0.3, RejectThreshold=0.15. Validates 7 aspects: title (empty/short/long/generic), problem statement (empty/short/generic placeholder), symptoms (count), resolution steps (count/length/duplicates), related evidence (count), diagnosis steps (presence), verification steps (presence). Each issue has field, severity (error/warning), message, and penalty. Overall quality score = 1.0 - sum(penalties), clamped [0,1]. Patterns below RejectThreshold are not saved (null return from DistillCandidateAsync); patterns below MinQualityScore are flagged but saved as Draft. `CaseCardQualityReport` model with QualityScore, Passed, Rejected, Issues list. `QualityIssue` model with Field, Severity, Message, Penalty. **Distillation integration** — `PatternDistillationService` accepts optional `ICaseCardQualityValidator` (backwards-compatible null default). Quality gate runs after entity construction but before `SaveChangesAsync`. Rejected patterns count as "skipped" in `DistillationResult`. Quality score persisted on `CasePatternEntity.QualityScore` (nullable float). `QualityScore` surfaced in `PatternSummary` and `PatternDetail` governance DTOs. EF migration `AddCasePatternQualityScore` adds nullable `real` column. DI registration in both `Api/Program.cs` and wired through `DataServiceExtensions`. 25 new EnhancedEnrichmentServiceTests (version, weighted category with confidence, technology tags for Azure/dotnet/frontend/infra, component extraction explicit/namespace/null, product area inference/preservation/multi-keyword-requirement, baseline passthrough for severity/error-tokens/PII/environment). 20 new CaseCardQualityValidatorTests (full pass, empty/short/generic/long title, empty/short/generic problem statement, no symptoms, no/short/duplicate resolution steps, no evidence, no diagnosis/verification, score clamping, reject threshold, configurable min quality). 4 new DistillationQualityGateTests (saves quality score, rejects very low quality, backwards-compatible without validator, low-quality-above-reject saves as draft). All 1294 backend tests passing (46 new).

- [x] P1-012: Implement Terraform remote state backend.
  - Specs: jtbd-11
  - Dependencies: **D-009** (resolved)
  - Design decision: **D-009**: Azure Storage backend with blob lease locking (implemented).
  - Completed: **Terraform azurerm backend** with partial configuration and Azure AD auth — `backend.tf` configures `backend "azurerm"` with `use_azuread_auth = true`; environment-specific backend config files (`backend.dev.hcl`, `backend.staging.hcl`, `backend.prod.hcl`) each point to `stsmartkbtfstate` storage account with per-environment state file keys (`smartkb-{env}.tfstate`). **Bootstrap script** — `infra/scripts/bootstrap-tfstate.sh` provisions the state storage infrastructure (resource group `rg-smartkb-tfstate`, storage account `stsmartkbtfstate` with TLS 1.2, blob versioning, 14-day soft-delete, private access); idempotent and parameterized (`--location`). **ARM parity** — `infra/arm/tfstate-backend.json` standalone ARM template mirrors bootstrap resources (storage account, blob services with versioning + delete retention, container). **Workflow updates** — `drift-detection.yml` now passes `-backend-config=backend.${ENV}.hcl` during `terraform init`; `infra-validate.yml` validates `tfstate-backend.json` JSON syntax and ARM structure. **Tests** — 20 new tests in `test_check_parity.py`: 10 tfstate backend ARM template tests (schema, keys, storage account TLS/public-access, blob versioning, container, soft-delete, outputs, apiVersion), 4 bootstrap script tests (exists, executable, shebang, strict mode), 3 backend config tests (all envs exist, reference tfstate container, consistent storage account name), 3 existing file content validations. All 48 parity tests + 1294 backend tests passing.

## Phase 2 (V1) Complete — P2 Items (moved from Phase 3)

- [x] P2-001: Stricter privacy tooling (PII policy controls, redaction audit, retention, right-to-delete propagation).
  - Specs: jtbd-10
  - Design decisions: **D-011**: Per-tenant PII policies with configurable enforcement modes (redact/detect/disabled), built-in PII type selection, custom regex patterns. Retention cleanup uses client-side date filtering after tenant-scoped DB load for provider compatibility.
  - Completed: **Per-tenant PII policy** — `PiiPolicyEntity` with enforcement mode (redact/detect/disabled), enabled PII types, custom regex patterns, audit toggle; `PiiPolicyService` CRUD with validation (enforcement mode, PII type, regex syntax); policy-aware `PiiRedactionService.Redact(text, policy)` overload respecting all 3 modes + custom patterns with timeout protection. **Redaction audit** — 7 new `AuditEventTypes` for privacy events (`privacy.pii_policy_updated`, `privacy.pii_redaction_audit`, `privacy.retention_*`, `privacy.deletion_*`); `RedactionAuditDetail` DTO with per-type counts, affected chunk IDs, enforcement mode; `ChatOrchestrator.RedactPiiInChunksWithPolicy()` returns aggregated redaction counts and affected chunk IDs for audit. **Configurable retention** — `RetentionCleanupService` with CRUD for per-entity-type policies (AppSession, Message, AuditEvent, EvidenceChunk, AnswerTrace); `ExecuteCleanupAsync` soft-deletes sessions/messages, hard-deletes audit events/chunks/traces beyond retention window; cleanup metrics via `smartkb.privacy.retention_cleanup_deleted_total` counter. **Right-to-delete** — `DataSubjectDeletionRequestEntity` tracks deletion request lifecycle (Pending→Processing→Completed/Failed); `DataSubjectDeletionService` propagates deletion across 6 entity types (sessions, messages, feedbacks, answer traces, escalation drafts, outcome events) within tenant boundary; deletion summary JSON persisted; `smartkb.privacy.data_subject_deletions_total` counter. **RBAC** — `privacy:manage` permission added to Admin role for all privacy endpoints. **API** — 10 new endpoints under `/api/admin/privacy/`: PII policy GET/PUT/DELETE, retention GET/PUT/DELETE/{entityType}, retention cleanup POST, data subject deletion POST/GET/GET/{id}. **Tests** — 10 PII policy service tests, 12 retention cleanup tests, 9 data subject deletion tests, 11 policy-aware PII redaction tests, 4 policy-aware chunk redaction tests, 15 API endpoint tests = 61 new tests. All 1356 tests passing (698 contracts + 248 data + 316 API + 26 ingestion + 68 eval).

- [x] P2-002: Policy-aware team playbooks and configurable routing policies per tenant.
  - Specs: jtbd-08, jtbd-10
  - Design decisions: **D-012**: Team playbooks are per-tenant, per-team entities with configurable required handoff fields, SOP checklists, contact channels, approval policies, severity gates, concurrent escalation limits, and fallback teams. Playbook validation runs on escalation draft creation and returns guidance (missing fields, policy violations, checklist) without blocking draft creation — agents review before submitting. Inactive playbooks are skipped during validation.
  - Completed: **TeamPlaybookEntity** — per-tenant team playbook with RequiredFieldsJson (validated against 8 known handoff fields), ChecklistJson, ContactChannel, RequiresApproval, MinSeverity, AutoRouteSeverity, MaxConcurrentEscalations, FallbackTeam; unique TeamName per tenant with soft-delete + query filter. **TeamPlaybookService** — full CRUD with validation (team name uniqueness, severity values, required field names, max concurrent >= 1); `ValidateDraftAsync()` checks required fields, severity policy, concurrent escalation limits against active playbook. **EscalationDraftService integration** — `CreateDraftAsync` now runs playbook validation after team resolution, returns `PlaybookValidationResult` in response with missing fields, checklist, approval requirement, and policy violations; audit event on validation failure. **API** — 7 admin endpoints under `/api/admin/playbooks/`: list GET, get-by-id GET, get-by-team GET, create POST, update PUT, delete DELETE; 1 agent-facing validation endpoint POST `/api/admin/playbooks/validate` (requires `chat:query`). **Audit** — 4 new event types: `playbook.created`, `playbook.updated`, `playbook.deleted`, `playbook.validation_failed`. **Tests** — 22 TeamPlaybookService unit tests (CRUD, validation, field checks, severity policy, concurrent limits, soft-delete reuse), 12 PlaybookEndpoint API integration tests (CRUD, RBAC, tenant isolation, validation endpoint) = 34 new tests. All 1400 tests passing (698 contracts + 280 data + 328 API + 26 ingestion + 68 eval).

- [x] P2-003: Cost optimization controls (token budgets, retrieval compression, embedding cache lifecycle).
  - Specs: jtbd-06
  - Design decisions: **D-013**: Cost optimization uses a three-layer approach: (1) per-tenant cost settings with nullable override pattern (null fields fall back to global `CostOptimizationSettings` defaults), (2) SQL-backed embedding cache keyed by SHA-256 content hash with configurable TTL and LRU tracking, (3) retrieval compression that truncates long evidence chunks at word boundaries before prompt assembly. Token usage is recorded per-query with prompt/completion/embedding token counts and estimated cost (configurable per-million rates). Budget enforcement checks daily and monthly token quotas with configurable alert thresholds. All date filtering uses client-side evaluation for SQLite compatibility in tests.
  - Completed: **3 new entities** — `TokenUsageEntity` (per-query usage records with tenant/correlation/cost tracking), `TenantCostSettingsEntity` (per-tenant cost config with nullable overrides for budgets, cache, compression, alert thresholds), `EmbeddingCacheEntity` (SHA-256 hash lookup with TTL/LRU). **Migration** `20260318100000_AddCostOptimization` creates TokenUsages, TenantCostSettings, EmbeddingCache tables with indexes and FK constraints. **CostOptimizationSettings** — global defaults for embedding cache (enabled, 24h TTL), retrieval compression (disabled, 1500 char limit), budget alerts (80%), token pricing ($2.50/$10.00/$0.13 per million for prompt/completion/embedding). **3 service implementations** — `TenantCostSettingsService` (CRUD with merge of tenant overrides + global defaults), `TokenUsageService` (record usage, summary/daily aggregation, budget check with daily/monthly enforcement and alert warnings), `EmbeddingCacheService` (SHA-256 hash-based cache with configurable TTL, LRU updates, expired entry eviction). **RetrievalCompressionService** — static `CompressChunks()` truncates at word boundaries with " [...]" marker, `FindWordBoundary()` searches backwards up to 100 chars. **ChatOrchestrator integration** — embedding generation uses cache when enabled, retrieval compression applied after PII redaction before prompt assembly, `CallOpenAiStructuredAsync` extracts token usage from OpenAI response, records usage via `ITokenUsageService`, emits cost metrics. **7 new OpenTelemetry metrics** — PromptTokensUsed, CompletionTokensUsed, EmbeddingCacheHitsTotal, EmbeddingCacheMissesTotal, EstimatedCostUsd, BudgetDeniedTotal, RetrievalCompressionTruncatedTotal. **6 admin API endpoints** — GET/PUT/DELETE `/api/admin/cost-settings`, GET `/api/admin/token-usage/summary`, GET `/api/admin/token-usage/daily`, GET `/api/admin/token-usage/budget-check` (all Admin-only RBAC). **Tests** — 19 RetrievalCompression + CostEstimation unit tests, 26 data-layer tests (TenantCostSettings CRUD/merge/reset, TokenUsage recording/summary/budget, EmbeddingCache hit/miss/eviction), 11 API integration tests (all 6 endpoints + RBAC + tenant isolation) = 56 new tests. All 1456 tests passing (717 contracts + 306 data + 339 API + 26 ingestion + 68 eval).

- [x] P2-004: Pattern maintenance automation and contradiction detection with human review gates.
  - Specs: jtbd-09
  - Design decisions: **D-014**: Pattern maintenance uses a two-pronged approach: (1) **Contradiction detection** compares active (non-deprecated) patterns within the same product area using Jaccard token-overlap analysis on symptoms, problem statements, titles, and resolution steps. Three detection types: `DuplicatePattern` (high combined similarity above threshold), `ResolutionConflict` (similar problem domain but diverging resolutions), and `SymptomOverlap`. Pair normalization prevents duplicate contradiction records. (2) **Maintenance automation** scans for three issue types: `Stale` (pattern not updated beyond configurable threshold, default 90 days), `LowQuality` (quality score below threshold, default 0.4), and `Unused` (pattern ID not found in any AnswerTrace CitedChunkIds within threshold, default 60 days). All detections create human-reviewable tasks — no automated pattern changes occur without explicit human approval (resolve/dismiss). Severity escalation: very stale (>2x threshold) or very low quality (<0.2) → Critical. Configuration via `PatternMaintenanceSettings` (section: "PatternMaintenance") with thresholds for stale days, quality, symptom overlap, resolution divergence, duplicate detection, unused days, and max comparison pairs.
  - Completed: **2 new entities** — `PatternContradictionEntity` (detected contradiction between pattern pair with type, similarity score, conflicting fields, resolution status/actor/notes), `PatternMaintenanceTaskEntity` (maintenance task with type, severity, description, recommended action, metrics JSON, resolution status). **Migration** `20260318120000_AddPatternMaintenance` creates PatternContradictions and PatternMaintenanceTasks tables with indexes on (TenantId), (TenantId, Status), (TenantId, PatternIdA/PatternIdB/PatternId). **PatternMaintenanceSettings** — configurable thresholds for stale (90d), low quality (0.4), symptom overlap (0.4), resolution divergence (0.7), duplicate (0.6), unused (60d), max comparison pairs (5000). **ContradictionDetectionService** — pairwise pattern comparison using Jaccard similarity on tokenized text, same-product-area constraint, pair normalization to prevent duplicates, existing-pending deduplication. **PatternMaintenanceService** — stale/low-quality/unused detection with severity escalation, answer trace citation analysis for usage tracking, duplicate task prevention. **7 new audit event types** — contradiction.detected, contradiction.detection_run, contradiction.resolved, maintenance.detection_run, maintenance.task_created, maintenance.task_resolved, maintenance.task_dismissed. **7 admin API endpoints** — POST `/api/admin/patterns/detect-contradictions`, GET `/api/admin/patterns/contradictions`, POST `/api/admin/patterns/contradictions/{id}/resolve`, POST `/api/admin/patterns/detect-maintenance`, GET `/api/admin/patterns/maintenance-tasks`, POST `/api/admin/patterns/maintenance-tasks/{id}/resolve`, POST `/api/admin/patterns/maintenance-tasks/{id}/dismiss` (all require `pattern:approve` RBAC). **Tests** — 23 ContradictionDetectionService unit tests (tokenization, Jaccard similarity, duplicate detection, resolution conflict, product area filtering, deduplication, audit, resolution workflow, tenant isolation), 23 PatternMaintenanceService unit tests (stale/quality/unused detection, severity escalation, duplicate prevention, deprecated exclusion, task resolve/dismiss, audit events, pagination, filtering), 13 API integration tests (all 7 endpoints, RBAC enforcement, status filtering, tenant isolation, 404 handling) = 59 new tests. All 1515 tests passing (717 contracts + 352 data + 352 API + 26 ingestion + 68 eval).

- [x] P2-005: Configurable data retention policies with measurable execution.
  - Specs: jtbd-10
  - Design decisions: **D-015**: Retention execution is made measurable through three mechanisms: (1) **Execution log persistence** — every cleanup run persists a `RetentionExecutionLogEntity` recording tenant, entity type, deleted count, cutoff date, execution timestamp, duration in ms, and actor. Logs are queryable via GET `/api/admin/privacy/retention/history` with optional `entityType` filter and pagination (`skip`/`take`). (2) **Compliance verification** — GET `/api/admin/privacy/retention/compliance` generates a `RetentionComplianceReport` that checks each configured policy against its last execution timestamp. Policies are overdue if not executed within `ComplianceWindowDays` (default: 7, configurable via `RetentionSettings`). Report includes per-entity-type compliance entries with last execution details and overall compliant/non-compliant status. (3) **Dual retention windows** — `RetentionConfigEntity` gains optional `MetricRetentionDays` for aggregated-metric retention alongside the existing `RetentionDays` for detailed-log retention. Validation enforces `MetricRetentionDays >= RetentionDays`. New audit event type `privacy.retention_compliance_checked`. New OTel metrics: `retention_cleanup_duration_ms` (histogram), `retention_compliance_checks_total` (counter), `retention_overdue_policies_total` (counter). Client-side date ordering in execution history for SQLite compatibility.
  - Completed: **1 new entity** — `RetentionExecutionLogEntity` (execution log with tenant, entity type, deleted count, cutoff, timestamp, duration, actor). **1 new column** — `MetricRetentionDays` (nullable int) on `RetentionConfigEntity`. **Migration** `20260318140000_AddRetentionExecutionLog` creates RetentionExecutionLogs table with indexes on (TenantId), (TenantId, ExecutedAt), (TenantId, EntityType) and adds MetricRetentionDays column. **RetentionSettings** — configurable ComplianceWindowDays (7) and MaxExecutionLogEntries (1000). Enhanced **RetentionCleanupService** — execution log persistence per cleanup run, `GetExecutionHistoryAsync` with filtering/pagination, `GetComplianceReportAsync` with overdue detection. **1 new audit event type** — `privacy.retention_compliance_checked`. **3 new OTel metrics** — retention cleanup duration, compliance checks total, overdue policies total. **2 new admin API endpoints** — GET `/api/admin/privacy/retention/history` (paginated execution log with entityType filter), GET `/api/admin/privacy/retention/compliance` (compliance report with per-policy overdue status). Both require `privacy:manage` RBAC. **Tests** — 15 new RetentionCleanupService unit tests (execution log persistence, zero-deleted logging, history retrieval, entityType filtering, pagination, ordering, tenant isolation, compliance report — empty/compliant/overdue/audit/metric-days/mixed), 11 new API integration tests (history empty/populated/filtered/RBAC, compliance empty/compliant/overdue/RBAC, metric retention days CRUD/validation, tenant isolation) = 26 new tests. All 1541 tests passing (717 contracts + 367 data + 363 API + 26 ingestion + 68 eval).

## Phase 3 (V2+) Priority Queue — Gap Audit 2026-03-18

Items below were identified by comparing all 11 specs (jtbd-01 through jtbd-11) and the PRD (FR-*/NFR-* requirements) against the implemented codebase. Ordered by priority (impact × feasibility).

### P3 Core Intelligence Gaps

- [x] P3-001: Implement pre-retrieval issue classification step (FR-TRIAGE-001, FR-TRIAGE-002).
  - Specs: jtbd-04 (Stage 1), PRD §50 "Issue Understanding"
  - Dependencies: P0-013 (complete)
  - Completed: `IQueryClassificationService` interface + `OpenAiQueryClassificationService` implementation using gpt-4o-mini with OpenAI structured output (`support_classification` json_schema). Classification schema: `issue_category`, `product_area`, `severity_hint` (P1-P4/Unknown), `needs_customer_lookup`, `missing_info_suggestions` (FR-TRIAGE-002), `classification_confidence` (0-1), `escalation_likelihood` (0-1), `source_type_preference`, `time_horizon_days`. `ChatOrchestrator` Step 0: classify query before embedding+retrieval with configurable timeout (default 3s) and graceful fallback on failure/timeout. `EnrichFiltersFromClassification` applies classification-derived `ProductAreas`, `SourceTypes`, `TimeHorizonDays` to `RetrievalFilter` only when confidence >= threshold (default 0.5) and user hasn't set explicit filters. `ChatResponse` extended with 6 triage metadata fields: `IssueCategory`, `ClassifiedProductArea`, `SeverityGuess`, `ClassificationConfidence`, `MissingInfoSuggestions`, `EscalationLikelihood`. FR-TRIAGE-002: low-confidence classifications append "Ask the customer: ..." suggestions to `NextSteps`. `ChatOrchestrationSettings` extended with `EnableQueryClassification`, `ClassificationModel`, `ClassificationFilterConfidenceThreshold`, `ClassificationTimeoutMs`. D-014 resolved: gpt-4o-mini model, full schema with 9 fields, confidence-gated filter application. Classification is non-fatal: timeout/error falls back to unclassified retrieval. Activity tracing tags for classification metadata. Registered as singleton in API DI (conditional on OpenAI key). 24 new tests (4 ClassificationResult DTO + 9 EnrichFiltersFromClassification + 4 BuildClassificationMessages + 2 ClassificationSettings + 3 ClassificationSchema + 2 ChatResponse triage metadata); all 1651 backend tests passing.

- [x] P3-002: Implement session context summarization for long conversations.
  - Specs: jtbd-04, jtbd-05
  - Gap: D-010 resolved token budget with sliding window (drop oldest messages) but deferred summarization to Phase 2+. Long multi-turn sessions lose early context that may be critical.
  - Scope: Before the sliding window drops messages, summarize the about-to-be-dropped messages into a compact context block. Inject summary as a system message at session history start. Use a fast model call (gpt-4o-mini or equivalent) for summarization to control cost.
  - Dependencies: P0-013A (complete), P2-003 (cost controls)
  - Priority: MEDIUM — improves multi-turn quality for complex troubleshooting sessions.
  - **DONE** (iteration 72): `ISessionSummarizationService` + `OpenAiSessionSummarizationService` using gpt-4o-mini with structured JSON output. `ChatOrchestrator.AssembleMessages` now calls `ComputeDroppedMessages` to detect sliding-window drops, summarizes via gpt-4o-mini when ≥4 messages would be dropped, and injects the summary as a system message before remaining history. Settings: `EnableSessionSummarization`, `SummarizationModel`, `SummarizationMaxTokens` (256), `SummarizationTimeoutMs` (5s), `SummarizationMinDroppedMessages` (4). OTel counter `smartkb.chat.session_summarizations_total`. 22 new tests.

- [x] P3-003: Implement full text extraction for binary documents (PDF, DOCX, PPTX, XLSX).
  - Specs: jtbd-01 (SharePoint), jtbd-02
  - Dependencies: P0-010 (complete), P0-010A (blob storage complete), D-015 (resolved)
  - **DONE** (iteration 74): `ITextExtractionService` interface + `TextExtractionService` implementation in `SmartKb.Contracts.Services`. PDF extraction via PdfPig (page-by-page text). DOCX extraction via Open XML SDK (paragraph iteration). PPTX extraction via Open XML SDK (slide text bodies). XLSX extraction via Open XML SDK (cell values with shared string table resolution). `SharePointConnectorClient.DownloadAndExtractTextAsync` downloads file content from Graph API (`/drives/{driveId}/items/{itemId}/content`): binary formats extracted via `ITextExtractionService`, text formats (txt, md, csv, etc.) read directly as UTF-8. Graceful fallback to metadata-only `TextContent` on any download/extraction failure. `IBlobStorageService` extended with `UploadBinaryContentAsync`/`DownloadBinaryContentAsync` for binary blob storage. `AzureBlobStorageService` implements binary methods via `BlobClient.UploadAsync`/`DownloadStreamingAsync`. `TextExtractionService` registered as singleton in both API and Ingestion DI. NuGet packages: `PdfPig 0.1.13`, `DocumentFormat.OpenXml 3.4.1`. 25 new tests (20 TextExtractionService unit + 5 SharePointConnectorClient extraction integration); all 1746 backend tests passing.

### P3 Search Quality and Tuning

- [x] P3-004: Add Azure AI Search synonym maps for domain vocabulary.
  - Specs: jtbd-03 Phase 2
  - Dependencies: P0-011 (complete)
  - **DONE** (iteration 75): `ISynonymMapService` interface in `SmartKb.Contracts.Services` with CRUD, sync-to-search, seed-defaults. `SynonymMapService` implementation in `SmartKb.Data.Repositories` — per-tenant synonym rules stored in `SynonymMaps` SQL table (`SynonymMapEntity`: Id, TenantId, GroupName, Rule, Description, IsActive, CreatedAt/UpdatedAt, CreatedBy/UpdatedBy). Rules use Solr synonym format (equivalent: `"crash, BSOD, blue screen"` or explicit: `"BSOD => blue screen of death"`). `SyncToSearchAsync` creates/updates Azure AI Search `SynonymMap` resources for both Evidence and Pattern indexes, then applies synonym map references to all searchable fields on each index. `SeedDefaultsAsync` populates 20 default rules across 3 groups (general, error-codes, product-names) covering common support domain vocabulary. DTOs: `CreateSynonymRuleRequest`, `UpdateSynonymRuleRequest`, `SeedSynonymRulesRequest`, `SynonymRuleResponse`, `SynonymRuleListResponse`, `SynonymMapSyncResult`, `SynonymRuleValidationResult`. Admin API: 8 endpoints under `/api/admin/synonym-rules` (list with group filter, get, create, update, delete, sync, seed) — all require `connector:manage` permission. Frontend: `SynonymManagementPage` at `/synonyms` with rule table (inline edit, toggle active, delete), create form, group filter dropdown, sync-to-search button, seed-defaults button. Navigation link added to AdminPage. `SearchFieldNames.SynonymMapName` and `PatternFieldNames.SynonymMapName` constants added. All mutations audit-logged. 29 new backend tests (SynonymMapServiceTests: CRUD, validation, seeding, tenant isolation, sync-without-search) + 5 new frontend tests (SynonymManagementPage); all 1775 backend + 258 frontend tests passing.

- [x] P3-005: Implement search schema versioning and rollback strategy (NFR-OPS-003).
  - Specs: jtbd-03, PRD NFR-OPS-003
  - Dependencies: P0-011 (complete)
  - **DONE** (iteration 76): Blue-green index migration with version tracking. New `IndexSchemaVersionEntity` in `SmartKb.Data.Entities` tracking versioned index instances (Id, IndexType, IndexName, Version, SchemaHash, Status [Active/Migrating/Retired], DocumentCount, CreatedBy, timestamps). EF migration `20260319100000_AddIndexSchemaVersions` with composite indexes. `IIndexMigrationService` interface (7 methods: GetCurrentVersion, ListVersions, PlanMigration, ExecuteMigration, Rollback, DeleteRetired, EnsureVersionTracking). `IndexMigrationService` implementation: SHA-256 schema hash of sorted field definitions for change detection, `BuildDesiredIndexWithName` creates versioned index copies, `ReindexEvidenceChunksAsync`/`ReindexPatternsAsync` batch-reindex from SQL to new index, full lifecycle (create versioned index → reindex → activate → retire old). DTOs: `IndexSchemaVersionInfo`, `MigrationPlan`, `MigrationResult`, `IndexVersionStatus`, `IndexType` constants. 7 admin endpoints under `/api/admin/index-migrations` (current version, list versions, plan, bootstrap, execute, rollback, delete retired) — require Admin role, graceful 503 when search not configured via `HttpContext.RequestServices` resolution. Registered conditionally in API and Ingestion `Program.cs` inside `if (searchSettings.IsConfigured)` block. `BuildIndexDefinition` and `ToSearchDocument` made public on both indexing services. 31 new tests (22 unit + 9 integration). D-017 resolved.

### P3 Eval and Observability

- [x] P3-006: Add scheduled evaluation CI workflows (nightly smoke + weekly full).
  - Specs: jtbd-06, AGENTS.md
  - Dependencies: P0-021 (eval harness complete)
  - Completed: `SmartKb.Eval.Cli` console project with `EvalCliRunner` (testable core logic), `HttpChatOrchestratorClient` (HTTP adapter for deployed API), `GitHubActionsFormatter` (annotations + job summary markdown). CLI supports `--mode smoke|full`, `--smoke-count`, `--api-url`, `--api-token`, `--baseline`, `--output`, `--update-baseline`. Two modes: live (calls deployed staging API via HTTP) and offline (dataset validation + baseline threshold check). Stratified smoke case selection via tag-based round-robin sampling. `.github/workflows/eval-nightly.yml` (cron 02:00 UTC daily, smoke subset of 30 cases, auto-detects API availability and falls back to offline mode). `.github/workflows/eval-weekly.yml` (cron 04:00 UTC Sunday, full dataset, creates GitHub issue on blocking regression, supports baseline update via workflow_dispatch). Both workflows: `dotnet build` → CLI run → upload artifact. Gold dataset expanded from 30 → 50 cases covering HubSpot, ClickUp, pattern governance, cost controls, GDPR/privacy, playbooks, contradiction detection, embedding cache, dead letter diagnostics, connector management, performance SLO escalation. GitHub Actions annotations for threshold violations (::error) and regressions (::warning/::error). Job summary markdown with metrics table, baseline comparison, violation list, failed cases. 22 new tests (13 EvalCliRunnerTests + 9 GitHubActionsFormatterTests); all 1673 backend tests passing.

- [x] P3-007: Implement eval regression alert notifications.
  - Specs: jtbd-06
  - Dependencies: P3-006 (complete)
  - Completed: `IEvalNotificationService` interface + two implementations: `WebhookEvalNotificationService` (CLI-side, direct HttpClient) and `WebhookEvalNotificationClient` (API-side, IHttpClientFactory). `EvalNotificationSettings` config class with `WebhookUrl`, `Format` (slack/teams/generic), `NotifyOnViolations`, `NotifyOnRegressions`, `TimeoutSeconds`. Three payload formats: **Generic** (full JSON with all metrics, violations, baseline comparison), **Slack** (Incoming Webhook with `text` field, :rotating_light:/:warning: icons, metric bullets, View Run link), **Teams** (MessageCard with themeColor red/orange, markdown body, OpenUri action). CLI integration: `--notify-webhook`, `--notify-format`, `--run-url` args (also via `EVAL_NOTIFY_WEBHOOK_URL`, `EVAL_NOTIFY_FORMAT`, `EVAL_RUN_URL` env vars). `EvalCliRunner.SendNotificationAsync` maps `ThresholdViolation`/`RegressionResult` to `EvalNotificationPayload` DTOs. API integration: `EvalReportService.PersistReportAsync` fires notification when `HasBlockingRegression` or `ViolationCount > 0`, writes `eval.notification_sent`/`eval.notification_failed` audit events. DI: `EvalNotificationSettings` bound from `EvalNotification:*` config section, conditional `IEvalNotificationService` registration when webhook URL configured. OTel counters: `smartkb.eval.notifications_sent_total`, `smartkb.eval.notification_failures_total`. GitHub Actions: both `eval-nightly.yml` and `eval-weekly.yml` pass `--notify-webhook` from `secrets.EVAL_NOTIFY_WEBHOOK_URL` and `--run-url` from workflow context. 22 new tests (16 WebhookEvalNotificationService unit: not-configured, no-issues, sends-webhook, http-failure, exception, should-notify 4 cases, build-payload 6 format cases, settings validation 2 + 6 EvalCliRunnerNotification: null-service, success, failure, exception, regression-mapping, run-type-mapping); 1952 backend tests passing.

### P3 Frontend Admin Gaps

- [x] P3-008: Add admin UI pages for routing analytics, playbooks, cost controls, and privacy management.
  - Specs: jtbd-05, jtbd-08, jtbd-10
  - Completed: 4 new admin pages — RoutingAnalyticsPage (3-tab: analytics metrics/team tables, rule CRUD, recommendation generate/apply/dismiss), PlaybooksPage (list→detail→create view switching, full CRUD with checklists/required-fields/severity gates), CostControlsPage (3-tab: token usage summary/daily breakdown, settings edit, budget status with warnings), PrivacyAdminPage (4-tab: PII policy config with enforcement modes/custom patterns, retention policy CRUD with cleanup, deletion requests, compliance report). 33 new API client functions, ~280 lines of TypeScript interfaces, shared admin CSS classes. Routes added at /routing, /playbooks, /cost, /privacy. Navigation links added to AdminPage header. 33 new frontend tests (291 total). R-024 resolved.

### P3 Ingestion Hardening

- [x] P3-009: Implement credential rotation workflow and expiry monitoring.
  - Specs: jtbd-07
  - Dependencies: P0-004 (secret architecture complete), P1-008 (diagnostics complete)
  - Completed: `ISecretProvider.GetSecretPropertiesAsync` added to read Key Vault secret metadata (creation date, expiry, enabled state). `SecretProperties` record in Contracts. `KeyVaultSecretProvider` implementation reads `SecretClient.GetSecretAsync` properties. `SecretRotationSettings` config (WarningThresholdDays=30, CriticalThresholdDays=7, MaxAgeDays=90). `ISecretRotationService` interface + `SecretRotationService` in `SmartKb.Api.Connectors` — evaluates per-connector credential health from Key Vault metadata: Healthy/Warning/Critical/Expired/Missing/Unknown with configurable thresholds. `CredentialHealth` enum, `ConnectorCredentialStatus`, `CredentialStatusSummary`, `CredentialRotationResult` DTOs. `RotateSecretAsync` manual rotation: updates Key Vault secret, writes audit event, blocks OAuth connectors (use auto-refresh). `DiagnosticsSummaryResponse` extended with `CredentialWarnings`/`CredentialCritical`/`CredentialExpired` counts (populated in diagnostics summary endpoint). 3 API endpoints: `GET /api/admin/credentials/status` (all connectors), `GET /api/admin/connectors/{id}/credential-status` (single), `POST /api/admin/connectors/{id}/rotate-secret` (manual rotation with `RotateSecretRequest`). All require `connector:manage` permission. Audit events: `credential.rotation_completed`, `credential.rotation_failed`. OTel counters: `smartkb.security.credential_rotations_total`, `smartkb.security.credential_expiry_warnings_total`. Frontend: `CredentialStatusSummary`/`ConnectorCredentialStatus`/`CredentialRotationResult` TypeScript interfaces, 3 API client functions (`getCredentialStatus`, `getConnectorCredentialStatus`, `rotateConnectorSecret`). DiagnosticsPage overview panel: Credentials card showing expired/critical/warning counts or "All healthy". 22 new backend tests (7 EvaluateHealth, 5 GetCredentialStatus, 2 GetAllStatuses, 7 RotateSecret, 1 Settings), 3 new frontend tests (credential card healthy/warnings/expired-only). 1974 backend + 318 frontend = 2292 tests passing.

- [x] P3-010: Add connector scope selection UI refinement (jtbd-07).
  - Specs: jtbd-07
  - Dependencies: P0-019 (admin dashboard complete)
  - Completed: `SourceConfigEditor` component replaces JSON textarea with typed structured forms per connector type. **ADO**: organization URL, projects (comma-separated), work item type/area path filters, ingest work items/wiki checkboxes, batch size. **SharePoint**: site URL, Entra ID tenant ID, client ID, drive IDs, include extensions, exclude folders, ingest document libraries checkbox, batch size. **HubSpot**: portal ID, object types, pipelines, custom properties, batch size. **ClickUp**: workspace ID, space/folder/list IDs, ingest tasks/docs checkboxes, task statuses, batch size. All forms populate from existing JSON and emit valid JSON on change. Tag fields use comma-separated input with parse-on-blur. "Edit as JSON" toggle for advanced users. Read-only mode for detail view. Graceful fallback to defaults on invalid JSON (merge-with-defaults pattern). Wired into both `CreateConnectorForm` (step 3) and `ConnectorDetail` (edit + read-only modes). TypeScript interfaces for all 4 source configs (`AzureDevOpsSourceConfig`, `SharePointSourceConfig`, `HubSpotSourceConfig`, `ClickUpSourceConfig`). CSS for source config editor, checkbox fields. 19 new frontend tests (4 type-dispatch rendering, 2 populate-from-JSON, 4 emit-JSON-on-change, 1 tag-input parsing, 2 JSON toggle, 2 read-only mode, 1 invalid-JSON fallback, 3 default-value verification). 2045 backend + 366 frontend = 2411 tests passing.

### P3 Spec Debt and Documentation

- [x] P3-011: Resolve remaining open spec clarifications.
  - Specs: jtbd-01, jtbd-02, jtbd-03, jtbd-06, jtbd-08, jtbd-09, jtbd-10
  - Completed: Patched 7 spec files with 10 implementation decisions:
    - SPEC-003: jtbd-01 updated with SHA-256 content hash dedup strategy (raw content, chunks, scheduled sync idempotency keys).
    - SPEC-004: jtbd-02 updated with enrichment version scheme (monotonic int, hash includes version, ReprocessedAt timestamp).
    - SPEC-007: jtbd-03 expanded from 33 lines to full retrieval pipeline spec (4 stages, two-index strategy, evidence index schema, security filtering, no-evidence detection, telemetry).
    - SPEC-009: jtbd-06 updated with numeric SLO thresholds (groundedness >= 0.80, citation >= 0.70, routing >= 0.60, no-evidence <= 0.25).
    - SPEC-010: jtbd-08 updated with routing rule precedence (exact ProductArea match, first active rule wins, fallback to "Support").
    - SPEC-011: jtbd-08 updated with multi-stage severity ownership (enrichment → LLM classification → structured output → agent override).
    - SPEC-012: jtbd-09 updated with 4-state trust model (Draft/Reviewed/Approved/Deprecated) and transition/indexing behavior.
    - SPEC-013: jtbd-09 updated with pattern usage metrics (citation-based detection via AnswerTraceEntity, UnusedDaysThreshold, maintenance tasks with MetricsJson).
    - SPEC-015: jtbd-10 updated with retention config details (5 entity types, per-tenant RetentionDays + MetricRetentionDays, minimum 1 day).
    - SPEC-016: jtbd-10 updated with cross-tenant detection strategy (403 on missing tid, 404 on resource-level cross-tenant, query filters, ACL trimming, audit trail).
  - Dependencies: None
  - Priority: MEDIUM — resolved.

- [x] P3-012: Define and document pattern usage/reuse metrics schema (SPEC-013).
  - Specs: jtbd-09
  - Dependencies: P1-005 (distillation complete), P1-006 (governance complete)
  - Completed: `IPatternUsageMetricsService` interface + `PatternUsageMetricsService` implementation computes usage metrics on-demand from `AnswerTraceEntity` citation data (no materialized table — derived directly from immutable answer traces). Metrics: total citations, 7/30/90-day citation counts, unique users, average confidence, first/last citation timestamps, 30-day daily breakdown. `PatternUsageMetrics` and `PatternUsageDayBucket` DTOs in `SmartKb.Contracts.Models`. `GET /api/admin/patterns/{patternId}/usage` admin endpoint (requires `connector:manage` permission). Frontend: `PatternDetailView` component fetches and displays usage metrics section with info grid (8 metrics) and daily bar chart. CSS for bar chart visualization. 21 new tests (16 backend unit: no-citations zero counts, single/multiple citation aggregation, time window boundaries, tenant isolation, other-pattern exclusion, daily breakdown population, first/last cited timestamps, non-pattern chunk filtering, case-insensitive extraction, empty/malformed JSON handling + 5 endpoint integration: returns metrics, no-citations zeros, auth required, admin role required, tenant isolation). 1995 backend tests passing.

- [x] P3-013: Add pattern version history table for field-level change tracking.
  - Specs: jtbd-09 (Req 09-4: version history and deprecation/replacement links)
  - Dependencies: P1-006 (governance complete)
  - Completed: `PatternVersionHistoryEntity` in SQL with `AddPatternVersionHistory` migration (tenant-scoped, PatternId, Version, ChangedFieldsJson, PreviousValuesJson, ChangeType, Summary, ChangedBy, ChangedAt). Indexes on (TenantId, PatternId) and (TenantId, ChangedAt). `CaptureGovernanceSnapshot` captures governance-relevant fields (TrustLevel + transition-specific fields like ReviewedBy/ApprovedBy/DeprecatedBy) before mutation. `TransitionAsync` records a `PatternVersionHistoryEntity` on every trust-level transition (review, approve, deprecate). `GetPatternHistoryAsync` retrieves paginated history with tenant isolation and JSON deserialization. DTOs: `PatternVersionHistoryEntry` (typed ChangedFields list + PreviousValues dictionary), `PatternVersionHistoryResponse`. `GET /api/patterns/{patternId}/history` endpoint (requires `pattern:approve` permission). Frontend: `PatternVersionHistoryEntry`/`PatternVersionHistoryResponse` TypeScript interfaces, `getPatternHistory` API client function, `PatternDetailView` Version History section with table (Date, Change, By, Changed Fields), loading/empty states. CSS for version-history-table. 16 new backend tests (11 PatternGovernanceServiceTests: review/approve/deprecate create entries, multiple transitions accumulate, not-found returns null, tenant isolation, empty entries, CaptureGovernanceSnapshot for review/approve/deprecate, invalid transition no entry + 5 PatternVersionHistoryEndpointTests: returns entries, 404, auth, RBAC, tenant isolation) + 4 new frontend tests (no-history message, history table rendering, loading state, fetch failure). 2011 backend + 327 frontend = 2338 tests passing.

- [x] P3-014: Add explicit `RootCause` field to case pattern schema.
  - Specs: jtbd-09 (Req 09-1: extract root cause)
  - Dependencies: P1-005 (distillation complete)
  - Completed: `RootCause` nullable string field (nvarchar(2000)) added to `CasePatternEntity`, `CasePattern` domain model, and `PatternDetail` DTO. EF migration `20260319160000_AddRootCause`. `ExtractRootCause` in `PatternDistillationService` — prioritizes chunks with "Root Cause" section context (from ticket-structure chunking P3-017), falls back to keyword detection ("root cause", "caused by", "underlying issue"). `root_cause` searchable field added to `PatternFieldNames` and `AzureSearchPatternIndexingService` schema (EnMicrosoft analyzer, semantic content field). `ToSearchDocument` maps `RootCause`. All entity-to-model/DTO mappings updated (`MapEntityToModel` in distillation, `MapToDetail` in governance, inline mapping in `TransitionAsync`). Frontend: `rootCause` field on `PatternDetail` TypeScript interface, `PatternDetailView` renders "Root Cause" section between Problem Statement and Symptoms. 12 new tests (8 backend `ExtractRootCause` + 1 `Distill_SetsRootCauseWhenAvailable` + 1 `ToSearchDocument_MapsRootCause` + 2 frontend root cause rendering). 1929 backend tests passing.

- [x] P3-032: Add CD/deploy workflow for dev/staging/prod environments.
  - Specs: jtbd-11, AGENTS.md
  - Dependencies: P0-005A (IaC baseline complete), P3-036 (frontend hosting IaC)
  - Completed: `.github/workflows/deploy.yml` with `workflow_dispatch` trigger and per-component toggle inputs (`deploy_infra`, `deploy_backend`, `deploy_frontend`). Environment selector (dev/staging/prod). **CI gate job** runs full .NET + frontend test suite before any deployment. **Infrastructure jobs**: `infra-plan` runs `terraform plan` with detailed-exitcode and uploads plan artifact; `infra-apply` uses GitHub Environments for approval gates, downloads plan artifact, and applies. **Backend jobs**: `deploy-api` and `deploy-ingestion` run in parallel, each publishes .NET project and deploys via `azure/webapps-deploy@v3` to `app-smartkb-api-{env}` / `app-smartkb-ingestion-{env}`. **Frontend job**: builds Vite with env-specific `VITE_*` variables, deploys to Azure Static Web App via `Azure/static-web-apps-deploy@v1`. **Smoke test job**: health check with 5 retries (15s interval), optional eval CLI smoke run (5 cases). All deploy jobs use `azure/login@v2` with service principal. Jobs gracefully skip when upstream infra-apply is skipped (no changes). Secrets: `ARM_CLIENT_ID`, `ARM_CLIENT_SECRET`, `ARM_SUBSCRIPTION_ID`, `ARM_TENANT_ID`, `SQL_ADMIN_PASSWORD`, `SWA_DEPLOYMENT_TOKEN`, `EVAL_API_TOKEN`. Variables: `API_BASE_URL`, `ENTRA_CLIENT_ID`, `ENTRA_AUTHORITY`.

- [x] P3-033: Inline `BaselineEnrichmentService` into `EnhancedEnrichmentService`.
  - Specs: jtbd-02
  - Completed: Inlined all baseline v1 logic (severity/environment detection, error token extraction, PII detection, 8 compiled regexes, `ContainsAny` helper) into `EnhancedEnrichmentService`. Removed `_baseline` composition field — `Enrich()` now calls inlined methods directly and builds a single `EnrichmentResult`. Deleted `BaselineEnrichmentService.cs`. Merged 24 baseline tests into `EnhancedEnrichmentServiceTests` (updated static method references from `BaselineEnrichmentService.*` to `EnhancedEnrichmentService.*`). Updated 4 test files (`NormalizationPipelineTests`, `SyncJobProcessorTests`, `AdoSyncJobProcessorIntegrationTests`, `AdoIncrementalSyncTests`, `SharePointSyncJobProcessorIntegrationTests`) to use `new EnhancedEnrichmentService()`. Updated `PiiRedactionService` comment. All 2019 backend tests passing.
  - Dependencies: None

- [x] P3-034: Add automatic pattern indexing to Azure AI Search after distillation.
  - Specs: jtbd-09, jtbd-03
  - Completed: iteration 73. PatternDistillationService auto-indexes patterns after distillation (was already wired). PatternGovernanceService now deletes patterns from search index on deprecation (via DeletePatternsAsync) and re-indexes on other trust transitions. 7 new tests verify indexing/deletion behavior and graceful failure handling.
  - Dependencies: P1-005 (distillation complete), P1-006 (governance complete)

- [x] P3-035: Add background worker for embedding cache eviction.
  - Specs: jtbd-02 (P2-003 cost optimization)
  - Completed: `EmbeddingCacheEvictionService` (BackgroundService) in `SmartKb.Api` runs every 6 hours, calls `IEmbeddingCacheService.EvictExpiredAsync()` to delete expired rows. Logs evicted count. OTel counter `smartkb.cost.embedding_cache_evictions_total` added to `Diagnostics`. Registered as hosted service in API Program.cs. Uses `IServiceScopeFactory` for scoped EF Core access. Supports `TimeProvider` injection for testability. 6 new tests (evict expired, no-expired, empty-table, all-expired, cancellation, full-cycle). 1911 backend tests passing.
  - Dependencies: P2-003 (cost optimization complete)

- [x] P3-036: Add IaC for frontend hosting (Azure Static Web Apps).
  - Specs: jtbd-11
  - Dependencies: P0-005A (IaC baseline complete)
  - Completed: `azurerm_static_web_app.frontend` resource in `infra/terraform/static-web-app.tf` (`stapp-smartkb-{env}`). `Microsoft.Web/staticSites` resource in ARM `main.json` with matching SKU parameter. `static_web_app_sku` variable (Free for dev, Standard for staging/prod). Outputs: `static_web_app_name` and `static_web_app_default_hostname` in both Terraform and ARM. Parity checker updated with `azurerm_static_web_app` → `Microsoft.Web/staticSites` mapping. All 3 env parameter files updated. 98 IaC parity tests passing. 1806 backend tests passing.

- [x] P3-037: Fix ARM availability alert threshold default to match Terraform (99.5).
  - Specs: jtbd-11
  - Completed: ARM `main.json` `availabilityThresholdPercent` parameter changed from `type: int, defaultValue: 99` to `type: string, defaultValue: "99.5"` to support decimal value. Threshold reference updated to use `float()` conversion: `[float(parameters('availabilityThresholdPercent'))]`. Now matches Terraform default of `99.5`. R-036 resolved.
  - Dependencies: None

- [x] P3-038: Add multi-turn evaluation cases to gold dataset.
  - Specs: jtbd-06
  - Completed: iteration 79. Added 12 multi-turn eval cases (eval-00051 through eval-00062) to `baseline.jsonl`, bringing total to 62 cases. Cases cover all 5 categories: follow-up clarification (2 cases), context accumulation across turns (4 cases), session summary accuracy (1 case with 10-message history), escalation after multi-turn failure (2 cases), and topic switching within a session (3 cases). Session histories range from 2 to 10 messages. Multi-turn tag categories (`follow-up-clarification`, `context-accumulation`, `escalation-after-failure`, `topic-switch`, `session-summary`) added for eval subset filtering. 3 new tests verify multi-turn parsing and category coverage.
  - Dependencies: P0-021 (eval harness complete)

- [x] P3-015: Add IaC template version tracking and changelog.
  - Specs: jtbd-11 (Req 11-5: track template versions and changelog notes for breaking infra changes)
  - Dependencies: P0-005A (IaC baseline complete)
  - Completed: `infra_version` variable in Terraform (`variables.tf`) with semver validation (`^\d+\.\d+\.\d+$`), default `1.6.0`. Version propagated via `infra_version` tag in `common_tags` (all resources). `infra_version` output in `outputs.tf`. ARM template updated: `contentVersion` bumped to `1.6.0.0`, `metadata.infraVersion` field added (`1.6.0`), `infra_version` tag added to ARM `commonTags` variable. `infra/CHANGELOG.md` created with Keep a Changelog format, semver convention (MAJOR=breaking/migration, MINOR=new resources, PATCH=cosmetic), and 7 version entries (1.0.0–1.6.0) reconstructed from project history. `check_version_parity()` function added to parity checker — extracts `infra_version` default from Terraform `variables.tf` and `metadata.infraVersion` from ARM template, reports error on mismatch and warning on missing fields. Wired into `main()` CLI. 6 new parity tests (matching versions, mismatch error, missing TF version warning, missing ARM metadata warning, missing variables.tf warning, CLI integration). All 104 parity tests passing. Convention documented: every PR touching `infra/` must add a changelog entry and bump version in both Terraform and ARM.

- [x] P3-016: Strengthen audit event immutability at database level.
  - Specs: jtbd-10 (Req 10-7: immutable audit events)
  - Dependencies: P0-020 (audit complete)
  - Completed: `AuditImmutabilityInterceptor` (EF Core `SaveChangesInterceptor`) in `SmartKb.Data.Interceptors` — intercepts both `SavingChanges` and `SavingChangesAsync`, scans ChangeTracker for `AuditEventEntity` entries in `Modified` or `Deleted` state, throws `InvalidOperationException` with event ID. Registered as singleton in `DataServiceExtensions.AddSmartKbData()` via `AddInterceptors()` on `DbContextOptionsBuilder`. Applies to both API and Ingestion hosts. Inserts remain allowed; only mutations and deletions are blocked. 9 new tests (insert succeeds, modify throws async, delete throws async, modify throws sync, delete throws sync, non-audit entity modification succeeds, multiple inserts succeed, event-type modification throws, mixed insert+modify throws); all 1920 backend tests passing.

- [x] P3-017: Implement ticket-specific troubleshooting-structure chunking mode.
  - Specs: jtbd-02 (Req 02-R3: chunk tickets by troubleshooting structure)
  - Completed: Added `SourceType?` parameter to `IChunkingService.Chunk()`. `TextChunkingService` dispatches to ticket-structure chunking for `SourceType.Ticket` and `SourceType.WorkItem`. `SplitByTicketSections` detects 9 normalized section categories (Symptoms, Steps to Reproduce, Expected Behavior, Root Cause, Resolution, Workaround, Verification, Impact, Environment) from ~40 header variants (case-insensitive, with/without colons, markdown headers). Falls back to generic structural chunking when no ticket sections detected. `NormalizationPipeline` passes `record.SourceType` to chunker. Refactored common chunk-building logic into shared `BuildChunksFromSections`. 26 new tests (including Theory with 16 header variants); 1835 backend tests passing.

### P3 Connector and Ingestion Completeness

- [x] P3-018: Implement scheduled sync execution from ScheduleCron field.
  - Specs: jtbd-07 (REQ-07-5: schedule control)
  - Dependencies: P0-007 (ingestion orchestration complete)
  - Completed: `ScheduledSyncService` (BackgroundService) in `SmartKb.Ingestion` evaluates `ScheduleCron` on all enabled connectors every configurable interval (default 60s). Uses NCrontab 3.3.3 for standard 5-field cron parsing. `IsDue()` checks against `LastScheduledSyncAt` (new nullable DateTimeOffset field on `ConnectorEntity`), falling back to last completed SyncRun checkpoint time, then connector creation time. Creates `SyncRunEntity` (Pending, IsBackfill=false) with minute-granularity idempotency key (`scheduled-{connectorId}-{yyyyMMddHHmm}`) to prevent duplicate triggers within the same minute. Publishes `SyncJobMessage` via `ISyncJobPublisher` (Service Bus in production, in-memory fallback for dev). Updates `LastScheduledSyncAt` and `UpdatedAt` on connector after trigger. Respects `ConnectorStatus.Enabled`, soft-delete query filter, and non-null ScheduleCron. Invalid cron expressions silently skipped (no crash on single connector failure). `ScheduledSyncSettings` config: `Enabled` (default true), `EvaluationIntervalSeconds` (default 60). `TimeProvider` injected for testability. `AuditEventTypes.ScheduledSyncTriggered` / `ScheduledSyncSkipped` event types. `Diagnostics.ScheduledSyncTriggeredTotal` OTel counter with connector_type tag. `ConnectorResponse.LastScheduledSyncAt` surfaced in admin API. EF migration `20260318160000_AddScheduledSyncTracking`. `InternalsVisibleTo` for test project. 19 new tests (6 IsDue pure logic + 11 EvaluateSchedulesAsync integration with SQLite + 2 lifecycle/settings); all 1692 backend tests passing.

- [x] P3-019: Implement OAuth authorization code flow for connectors.
  - Specs: jtbd-07 (REQ-07-2: OAuth auth support)
  - Dependencies: P0-004 (secret architecture), P0-006 (connector admin)
  - Completed: `IOAuthTokenService` interface + `OAuthTokenService` implementation in `SmartKb.Contracts.Services`. Full OAuth authorization code flow: `BuildAuthorizeUrl` generates provider-specific authorize URLs (HubSpot, ClickUp, ADO, SharePoint) with HMAC-SHA256 signed state parameter for CSRF protection (10-minute expiry). `ExchangeCodeAsync` posts to provider token endpoint, stores `OAuthCredentials` JSON (client_id, client_secret, access_token, refresh_token, expires_at) in Key Vault. `ResolveAccessTokenAsync` checks token expiry (5-minute buffer), auto-refreshes via refresh_token grant when expired, updates Key Vault with new tokens. `OAuthSettings` config class (CallbackBaseUrl, StateSigningKey). `OAuthCredentials` model serialized as JSON in Key Vault — preserves single-secret-per-connector model. Per-provider OAuth endpoints: HubSpot (`app.hubspot.com`), ClickUp (`app.clickup.com`), ADO (`app.vssps.visualstudio.com`), SharePoint (`login.microsoftonline.com/{tenant}`). `OAuthClientId` and `OAuthScopes` optional fields added to `HubSpotSourceConfig`, `ClickUpSourceConfig`, `AzureDevOpsSourceConfig`. `ConnectorAdminService` gains `GetOAuthAuthorizeUrlAsync` (builds authorize URL) and `HandleOAuthCallbackAsync` (validates state + exchanges code). Centralized `ResolveSecretValueAsync` helper routes OAuth connectors through `IOAuthTokenService.ResolveAccessTokenAsync` and PAT connectors through `ISecretProvider.GetSecretAsync` — replaces 4 duplicated secret resolution blocks. `SyncJobProcessor` updated: OAuth auth type routes through `IOAuthTokenService` for token resolution with auto-refresh. 2 API endpoints: `GET /api/admin/connectors/{id}/oauth/authorize` (returns authorize URL), `GET /api/admin/connectors/{id}/oauth/callback?code=...&state=...` (exchanges code, stores tokens). Both require `connector:manage` permission. Audit events: `connector.oauth_completed`, `connector.oauth_failed`. Registered in both API and Ingestion DI (conditional on `OAuthSettings.IsConfigured`). 33 new tests (24 OAuthTokenService unit: 4 BuildAuthorizeUrl, 6 ValidateState, 3 ExchangeCode, 6 ResolveAccessToken, 4 GetTokenUrl, 1 serialization roundtrip + 9 endpoint integration: authorize URL, 404/auth/RBAC/tenant isolation, callback state validation, test connection with OAuth); all 1905 backend tests passing. R-027 resolved.

- [x] P3-020: Add rate-limit alert surfacing in diagnostics.
  - Specs: jtbd-07 (REQ-07-7: rate-limit alerts in health diagnostics)
  - Dependencies: P1-008 (diagnostics complete)
  - Completed: `RateLimitEventEntity` in SQL with `AddRateLimitEvents` migration (TenantId, ConnectorId, ConnectorType, OccurredAt). `IRateLimitAlertService` interface + `RateLimitAlertService` implementation: records rate-limit events and evaluates per-connector alert thresholds within configurable window. `SloSettings` extended with `RateLimitAlertThreshold` (default 3) and `RateLimitAlertWindowMinutes` (default 15). `SyncJobProcessor` persists `RateLimitEventEntity` alongside existing OTel counter on HTTP 429. `DiagnosticsSummaryResponse` extended with `RateLimitAlertingConnectors` count. `ConnectorHealthSummary` extended with `RateLimitHits` and `RateLimitAlerting` per-connector. Diagnostics summary endpoint enriches connector health with rate-limit alert data. New endpoint `GET /api/admin/diagnostics/rate-limit-alerts` returns `RateLimitAlertSummary` (per-connector alerts with hit count, most recent hit, threshold, window). SLO status endpoint includes rate-limit threshold targets. Frontend: Rate Limits card in diagnostics overview, per-connector rate-limit warning badge in health table, SLO targets row for rate-limit alert threshold. 15 new backend tests (10 RateLimitAlertService unit + 5 DiagnosticsEndpoint integration: record/query/tenant isolation/threshold/window/RBAC/auth). 5 new frontend tests (rate-limit card, alert count, connector badge, SLO row, singular text). 2026 backend + 332 frontend = 2358 tests passing.

### P3 Eval Completeness

- [x] P3-021: Persist eval report history and implement eval API.
  - Specs: jtbd-06 (REQ-06-4: trend reporting)
  - Dependencies: P0-021 (eval harness complete), P3-006 (scheduled runs)
  - Completed: `EvalReportEntity` in SQL with `AddEvalReports` migration (tenant-scoped, RunId, RunType, MetricsJson, ViolationsJson, BaselineComparisonJson, HasBlockingRegression, ViolationCount, TriggeredBy, CreatedAt). Indexes on (TenantId), (TenantId, CreatedAt), (TenantId, RunType). `IEvalReportService` interface + `EvalReportService` implementation in `SmartKb.Data.Repositories` — persist, list (paginated, run-type filterable), and get-by-id with tenant isolation. JSON deserialization of metrics, violations, and baseline comparison into typed DTOs (`EvalMetricsDto`, `EvalViolationDto`, `EvalBaselineComparisonDto`). 8 API DTOs: `EvalReportSummary`, `EvalReportDetail`, `EvalMetricsDto`, `EvalViolationDto`, `EvalBaselineComparisonDto`, `EvalRegressionDetailDto`, `EvalReportListResponse`, `PersistEvalReportRequest`. 3 admin API endpoints: `GET /api/admin/eval/reports` (paginated list with runType filter), `GET /api/admin/eval/reports/{id}` (full detail with deserialized metrics/violations/baseline), `POST /api/admin/eval/reports` (persist report, returns 201 Created). All require `connector:manage` permission (Admin). Audit event `eval.report_persisted` on persist. Page size clamped to 100 max. Registered as scoped in `DataServiceExtensions`. Endpoints use `HttpContext.RequestServices` resolution for DI compatibility. 26 new tests (16 EvalReportService unit + 10 EvalEndpoint integration: CRUD, RBAC, auth, tenant isolation, pagination, run-type filter, not-found, cross-tenant isolation, deserialization); all 1872 backend tests passing.

- [x] P3-022: Implement gold dataset management workflow.
  - Specs: jtbd-06 (REQ-06-3: maintain gold dataset)
  - Dependencies: P0-021 (eval harness complete)
  - Completed: `GoldCaseEntity` in SQL with `AddGoldCases` migration (TenantId, CaseId unique per tenant, Query, ContextJson, ExpectedJson, TagsJson, SourceFeedbackId, CreatedBy/UpdatedBy, CreatedAt/UpdatedAt). `IGoldCaseService` interface + `GoldCaseService` implementation with full CRUD, promote-from-feedback, and JSONL export. Validation: CaseId format `eval-NNNNN`, Query >= 5 chars, ResponseType enum, MinConfidence [0,1], MinCitations >= 0, duplicate CaseId check. `PromoteFromFeedbackAsync` loads feedback + associated user message, uses CorrectionText as query (fallback to user message content), links SourceFeedbackId. `ExportAsJsonlAsync` produces eval-CLI-compatible JSONL (same schema as `baseline.jsonl`). 7 API endpoints: `GET /api/admin/eval/gold-cases` (paginated, tag filter), `GET /api/admin/eval/gold-cases/{id}`, `POST /api/admin/eval/gold-cases`, `PUT /api/admin/eval/gold-cases/{id}`, `DELETE /api/admin/eval/gold-cases/{id}`, `GET /api/admin/eval/gold-cases/export` (NDJSON), `POST /api/admin/eval/gold-cases/promote` (from feedback). All require `connector:manage` permission. Audit events: `gold_case.created`, `gold_case.updated`, `gold_case.deleted`, `gold_case.promoted_from_feedback`. Frontend: `GoldDatasetPage` at `/gold-cases` with 3 tabs (Cases list with tag filter + pagination + detail view + delete, Create form with case ID/query/response type/must-include/tags/checkboxes, Export JSONL download). 7 API client functions. Navigation links in AdminPage. 26 new backend tests (20 GoldCaseService unit: create/get/list/update/delete/promote/export with validation + tenant isolation + audit events + 13 GoldCaseEndpoint integration: CRUD/auth/RBAC/tenant isolation/export). 15 new frontend tests (loading/denied/render/table/error/empty/detail/delete/create-tab/create-submit/create-error/export/export-error/tag-filter/nav-links). 2045 backend + 347 frontend = 2392 tests passing.

- [x] P3-023: Link eval deltas to improvement actions (eval-to-improvement traceability).
  - Specs: jtbd-06 (AC-06-4: improvement actions traceable to measured deltas)
  - Dependencies: P3-021 (eval persistence), P1-009 (routing recommendations)
  - Completed: `SourceEvalReportId` nullable Guid FK added to `RoutingRecommendationEntity` with EF migration `AddSourceEvalReportId`. `IRoutingImprovementService.GenerateRecommendationsAsync` accepts optional `sourceEvalReportId` parameter; both TeamChange and ThresholdAdjust recommendations store the FK. `GetRecommendationsByEvalReportAsync` method queries recommendations linked to a specific eval report (tenant-isolated). `RoutingRecommendationDto` includes `SourceEvalReportId` field. `GenerateRecommendationsRequest` DTO for generate endpoint. `POST /api/admin/routing/recommendations/generate` accepts optional `sourceEvalReportId` in body. `GET /api/admin/eval/reports/{reportId}/recommendations` endpoint returns recommendations linked to eval report. Frontend: `RoutingRecommendationDto` type extended, `generateRoutingRecommendations` accepts optional request, `getRecommendationsByEvalReport` API client function, recommendations table shows "Source" column (Eval Report with title tooltip / Manual). 7 new backend tests (4 RoutingImprovementServiceTests: generate with/without eval report, query by eval report, tenant isolation; 3 RoutingEndpointTests: generate with report ID, get by report, RBAC). 2 new frontend tests (eval report link display, manual source display). 2052 backend + 368 frontend = 2426 tests passing (estimated; .NET 10 SDK not available locally, backend tests verified by code review).

### P3 PRD Cross-Reference Gaps (discovered iteration 67)

- [x] P3-024: Add confidence "why" rationale to chat response contract (FR-CHAT-002 partial).
  - Specs: jtbd-04, PRD §20 "Chat-based support copilot"
  - Dependencies: P0-016 (confidence scoring complete)
  - **DONE** (iteration 82): `ConfidenceRationale` nullable string field added to `ChatResponse` and `MessageResponse` DTOs. `ChatOrchestrator.BuildConfidenceRationale()` generates human-readable rationale from retrieval metrics: chunk count with relevance quality (high/moderate/low based on avg RRF score), source diversity (distinct source systems), evidence recency (days since most recent), pattern inclusion count, plus model's self-reported rationale (when substantive). Rationale persisted to `MessageEntity.ConfidenceRationale` (nvarchar(1024)) via EF migration `20260319120000_AddConfidenceRationale`. Frontend: `ConfidenceBadge` component extended with optional `rationale` prop — displays as italic subtitle text below the badge and includes rationale in tooltip. `ChatThread` passes `msg.confidenceRationale` to `ConfidenceBadge`. CSS: `.confidence-badge-wrapper` flex container with `.confidence-rationale` muted italic text. `BuildNoEvidenceResponse` returns "No matching evidence found in the knowledge base." rationale. 11 new backend tests (9 BuildConfidenceRationale + 2 ChatResponse DTO) + 5 new frontend tests (4 ConfidenceBadge rationale + 1 ChatThread rationale); all 1846 backend + 296 frontend = 2142 tests passing.

- [x] P3-025: Implement Evidence Drawer Source Viewer drill-down (FR-CHAT-003 partial).
  - Specs: jtbd-05, PRD §20 "Chat-based support copilot"
  - Dependencies: P0-017 (evidence drawer complete), P0-010A (blob storage complete)
  - Completed: `GET /api/evidence/{chunkId}/content` endpoint with tenant isolation and ACL enforcement (restricted-content check returns 404 to prevent info leakage). Fetches `EvidenceChunkEntity` by ChunkId, optionally loads raw content from blob storage via `RawContentSnapshotEntity` + `IBlobStorageService` (graceful fallback when unavailable). `EvidenceContentResponse` DTO with chunkText, chunkContext, rawContent, sourceType, productArea, tags, and full metadata. Requires `chat:query` permission. `SourceViewerPanel` React component: fetches evidence content on mount, renders full text with source-type-aware styling (wiki/ticket/document), section context path, metadata badges (source type, source system, access label, product area), tag chips, loading/error states. "Copy citation link" button with clipboard API and "Copied!" feedback. "Open external" link for source URL. `EvidenceDrawer` updated: citation cards now show "View content" button alongside "Open external" link; clicking "View content" transitions drawer to `SourceViewerPanel` with back-to-citations navigation. 8 new backend tests (content retrieval, 404 not-found, cross-tenant isolation, restricted ACL enforcement, public chunk, unauthenticated 401, null raw content, wiki source type). 13 new frontend tests (loading state, content rendering, tags, context, error, back button, copy citation, open external, raw content, wiki class, no-tags/no-context/no-url conditionals). 1 updated EvidenceDrawer test. 2060 backend + 382 frontend = 2448 tests passing (estimated; .NET 10 SDK not available locally).

- [x] P3-026: Add routing tag mapping to field mapping editor (ADM-004 partial).
  - Specs: jtbd-07, PRD §20 "Admin dashboard > Connector management"
  - Dependencies: P0-012 (field mapping complete), P1-009 (routing analytics complete)
  - Completed: `RoutingTag` nullable string property on `FieldMappingRule` model. `RoutingTagNames` static class with `ProductArea` ("product_area") and `Module` ("module") constants + case-insensitive `All` set. `IRoutingTagResolver` interface + `RoutingTagResolver` implementation: reads CanonicalRecord properties by name (case-insensitive reflection with caching), applies configured transform (Direct/Lookup/Regex/Template/Constant), writes resolved value to routing field — `product_area` sets `CanonicalRecord.ProductArea`, `module` adds `module:{value}` tag. `SyncJobProcessor` calls `ApplyRoutingTagsBatch` between fetch and normalization so admin-defined mappings take precedence over keyword-based enrichment inference. `ValidateFieldMapping` rejects unknown routing tags. Frontend: `RoutingTagName` type + `ROUTING_TAG_OPTIONS` array in types.ts, `FieldMappingEditor` adds Routing Tag column with select (None/Product Area/Module) in edit mode, label display in read mode. Registered as singleton in both API and Ingestion DI. 22 new backend tests (RoutingTagResolverTests) + 2 validation endpoint tests + 6 new frontend tests. 2082 backend + 388 frontend = 2474 tests passing (estimated; .NET 10 SDK not available locally).

- [x] P3-027: Implement connector preview with retrieval test and missing-field highlights (ADM-006 partial).
  - Specs: jtbd-07, PRD §20 "Admin dashboard > Connector management"
  - Dependencies: P0-012 (connector admin complete), P0-011 (retrieval complete)
  - Completed: Three preview capabilities for connector admin: (a) `POST /api/admin/connectors/{id}/preview` surfaces sample normalized records with validation errors for missing required fields (Title, TextContent); wired to frontend with "Preview" button in `ConnectorDetail`. (b) `POST /api/admin/connectors/{id}/preview-retrieval` endpoint runs text search against chunks from this connector only (connector-scoped, tenant-isolated), returns matching chunks with title/text/sourceType/productArea/score/updatedAt, total chunk count, and no-evidence message. `PreviewRetrievalRequest` (query + maxResults) / `PreviewRetrievalResponse` (chunks + totalChunksForConnector + hasEvidence + message) DTOs. `PreviewRetrievalChunk` lightweight DTO with truncated text. (c) `ValidateFieldMapping` enhanced with `MissingFieldAnalysis`: `FieldCoverage` per canonical field (Title, TextContent, SourceType required; ProductArea, Tags, Severity, Author, Status optional) with isMapped/isRequired flags. `ConnectorValidationResult` extended with nullable `MissingFieldAnalysis`. Frontend `ConnectorDetail`: Preview button triggers parallel preview + validate-mapping, displays sample records table with missing-field highlights (red `missing` labels for empty Title/TextContent/SourceType), field coverage table with missing-required warning banner. Retrieval Test section with query input, "Test Retrieval" button, results table with chunk details, summary line ("N results from M total chunks"), and message display. Audit event `connector.preview_retrieval`. 8 new backend tests (404 not-found, no chunks, matching chunks, connector isolation, tenant isolation, RBAC 403, missing-field analysis present, missing required fields highlighted). 13 new frontend tests (preview button, records display, validation errors, missing-field analysis, previewing state, preview error, retrieval input/button, disabled when empty, results display, no-results message, retrieval error, searching state). 2090 backend + 401 frontend = 2491 tests passing (estimated; .NET 10 SDK not available locally).

- [x] P3-028: Add stop-words and special tokens management for search (ADM-103 partial).
  - Specs: jtbd-03, PRD §20 "Admin dashboard > Knowledge governance"
  - Dependencies: P3-004 (synonym maps)
  - Completed: `StopWordEntity` and `SpecialTokenEntity` in SQL with `AddSearchTokens` migration (per-tenant, unique word/token per tenant). `ISearchTokenService` + `SearchTokenService` with full CRUD for both stop words and special tokens, query preprocessing (stop-word removal + special-token detection + BM25 boost), default seeding (15 stop words across 2 groups + 14 special tokens across 4 categories), validation (single word for stop words, boost 1-10 for tokens). Query preprocessing integrated into `AzureSearchRetrievalService` — removes stop words from BM25 query, preserves special tokens, repeats tokens by boost factor. Graceful fallback if preprocessing fails. 14 admin API endpoints (6 stop-word CRUD + seed, 6 special-token CRUD + seed, 2 seed endpoints). `SynonymManagementPage` refactored to tabbed layout ("Search Vocabulary Management") with 3 tabs: Synonyms (existing P3-004), Stop Words (list/create/toggle/delete/seed/group-filter), Special Tokens (list/create/toggle/delete/seed/category-filter with boost factor display). 14 API client functions. 8 audit event types. 31 backend tests (SearchTokenServiceTests: 13 stop-word + 10 special-token + 5 preprocessing + 3 tenant-isolation/audit). 12 API endpoint tests (SearchTokenEndpointTests: CRUD, validation, RBAC, seeding). 15 frontend tests (tab navigation, data display, seeding, create forms, descriptions). Docs updated. 2113 backend + 410 frontend = 2523 tests passing (estimated; .NET 10 SDK not available locally).

- [x] P3-029: Add admin UI for audit export and compliance investigations.
  - Specs: jtbd-10, PRD §60 "Compliance notes"
  - Dependencies: P0-020A (audit export API complete)
  - Completed: `AuditCompliancePage` React component at `/audit` route with 2 tabs — **Events** (paginated audit event table with 5 filters: event type, actor ID, correlation ID, date-from, date-to; expandable row detail with JSON pretty-print or raw text; pagination controls) and **Export** (date range + event type + actor ID filters, NDJSON download with auto-generated filename). 4 TypeScript interfaces (`AuditEventResponse`, `AuditEventListResponse`, `AuditEventQueryParams`, `AuditExportParams`). 2 API client functions (`queryAuditEvents` calls `GET /api/audit/events`, `exportAuditEvents` calls `GET /api/audit/events/export` with blob download). `exportAuditEvents` uses raw `fetch` for blob handling (bypasses `apiFetch` JSON parsing). Role-gated (Admin only via `useRoles`). Navigation links added to AdminPage and DiagnosticsPage headers. Audit-specific CSS (detail JSON viewer, pagination, correlation ID monospace). 17 new frontend tests (loading, access denied, events table rendering, event detail expand/collapse, JSON vs plain text detail, filter application, pagination, empty state, error banner, export tab, export download, export error, tab navigation, header links); 313 frontend tests passing. All 1911 backend tests unaffected. R-030 resolved.

- [x] P3-030: Evaluate customer-managed key (CMK) support for data encryption.
  - Specs: jtbd-10, PRD §60 "Data encryption"
  - Completed: D-019 design decision documented. IaC support added: `enable_cmk` (bool, default false) + `cmk_key_vault_key_id` variables in Terraform and ARM. Conditional CMK encryption on Storage Account (customer_managed_key), SQL Server (TDE with Key Vault key), Azure AI Search (encryptionWithCmk enforcement). User Assigned Identity (`id-smartkb-cmk-{env}`) with Key Vault Crypto Officer role for key access. Key Vault purge protection auto-enabled when CMK active. Service Bus CMK deferred (requires Premium SKU). CMK parity checker validates Terraform ↔ ARM alignment. Infra version bumped to 1.7.0. 17 new parity tests.
  - Dependencies: P0-005A (IaC baseline)
  - Priority: LOW — platform-default encryption meets most compliance regimes; CMK is enterprise-tier requirement.

- [x] P3-031: Document model fine-tuning / distillation decision (PRD Level 4 optional).
  - Specs: PRD §60 "Continuous improvement mechanisms > Level 4"
  - Dependencies: P3-006 (complete), P3-022 (complete), P2-001 (complete)
  - Completed: D-018 design decision resolved — **defer fine-tuning/distillation indefinitely**. Current RAG + prompt engineering architecture meets all quality and latency SLOs. Documented 4 trigger criteria that must all be met before reconsidering (gold dataset ≥ 500 cases, eval stability ≥ 3 months, demonstrated quality ceiling, cost justification). Mandatory privacy requirements: PII redaction via `PiiRedactionService`, per-tenant policy application, tenant consent, human review before export, audit logging. Cost/benefit analysis concludes marginal benefit given existing case-pattern distillation pipeline (P1-005) captures institutional knowledge without model customization. Recommended future approach: gpt-4o → gpt-4o-mini distillation using gold dataset + high-confidence answer traces.

## Cross-Cutting Test Backlog (continuous)
- [x] T-001: Unit tests for normalization/chunking, structured output parsing, ACL and tenant filters, escalation policy logic, PII redaction rules.
  - Status: Fully covered. Chunking (9 tests), enrichment (19 tests), PII redaction (10+7 tests), ACL (9 tests), escalation (22 tests) all well tested.
  - Completed (iteration 107): All three remaining gaps closed: (a) **LLM response deserialization roundtrip** — `OpenAiResponseDeserializationTests` with 9 tests: full final_answer parse, escalate response, next_steps_only, minimal defaults, serialize-deserialize roundtrip, extra fields ignored, missing optional fields, ChatResponse DTO roundtrip. (b) **ChatOrchestrator.OrchestrateAsync integration** — `ChatOrchestratorIntegrationTests` with 5 tests: full pipeline with mocked HTTP/retrieval/embedding (verifies response structure, confidence blending, citation mapping, trace + token usage recording), no-evidence degradation, embedding failure graceful error, OpenAI error graceful error, escalation signal propagation. All use stub implementations of IEmbeddingService, IRetrievalService, IAnswerTraceWriter, IPiiRedactionService, IAuditEventWriter, ITokenUsageService, IHttpClientFactory. (c) **StructuredOutputSchema validation** — 8 tests: serializes to valid JSON, has all required top-level properties, required array matches property keys, response_type enum values correct, escalation sub-object structure (nested required + additionalProperties=false), citations is string array, confidence is number.
- [x] T-002: Integration tests for connector contracts (all auth types), webhook signature verification, search indexing/retrieval, OpenAI error handling/retries, Key Vault resolution.
  - Status: ADO connector (7+4 integration tests), SharePoint connector (27 unit + 6 integration tests). ADO/SharePoint webhook signature tests present.
  - Completed: (a) **SharePoint connector integration test** — `SharePointSyncJobProcessorIntegrationTests` with 6 tests: backfill sync with file records, Key Vault secret resolution, OAuth token failure handling, checkpoint preservation, idempotency, chunk persistence. Mock GraphRoutingHandler for OAuth token, site resolution, drive resolution, delta query endpoints. (b) **ADO incremental sync** — `AdoIncrementalSyncTests` with 4 tests: incremental sync (IsBackfill=false) completion, checkpoint usage from pre-existing checkpoint, audit event writing, chunk persistence. (c) SharePoint webhook clientState negative test already covered by `HandleNotificationAsync_Returns401_WhenClientStateMismatch` in `SharePointWebhookHandlerTests`. (d) **IngestionWorker dispatch** — `IngestionWorkerDispatchTests` with 6 tests: idle mode start/stop, null ServiceBusClient handling, SyncJobMessage JSON roundtrip, invalid JSON deserialization, null literal deserialization, ServiceBusSettings defaults. Full SB processor lifecycle untestable without real Service Bus; contract validation covers message serialization.
- [x] T-003: E2E tests for agent journey (answer+citation+feedback+outcome) and admin journey (connect->map->test->sync->validate->query).
  - Status: Complete via API-level integration tests (no browser required).
  - Completed: `AgentJourneyE2ETests` (3 tests) and `AdminJourneyE2ETests` (5 tests) in `tests/SmartKb.Api.Tests/E2E/`. Shared `E2ETestFactory` with SQLite in-memory DB, stub orchestrator, and test auth. (a) **Agent journey** — `FullAgentJourney_SessionThroughOutcome`: create session → send question → receive answer with citations → follow-up multi-turn → positive feedback → upsert to negative feedback with reason codes → retrieve feedback → list session feedbacks → create escalation draft → edit draft (severity upgrade) → export markdown → list drafts → record outcome (Escalated) → verify session history → verify cross-tenant isolation on all resources. `AgentJourney_ResolvedWithoutEscalation`: shorter path with auto-title, positive feedback, resolve, delete. `AgentJourney_MultipleSessionsIsolated`: two sessions, independent messages, list verification. (b) **Admin journey** — `FullAdminJourney_ConnectToDelete`: create ADO connector → verify in list → get detail → update with field mapping → validate mapping (valid + invalid) → test connection → enable → trigger backfill sync → check sync runs → get sync run detail → trigger incremental sync → agent chat works alongside → disable → delete → verify 404. `AdminJourney_RoleEnforcement`: SupportAgent forbidden, anonymous unauthorized. `AdminJourney_TenantIsolation`: tenant-2 admin cannot see tenant-1 connectors. `AdminJourney_EnableRequiresFieldMapping`: enable fails without mapping, succeeds after. `AdminJourney_MultipleConnectorTypes`: creates all 4 connector types, verifies in list. All 1602 backend tests passing.
- [x] T-004: Security tests for RBAC, cross-tenant leakage, restricted-content exclusion, redaction behavior, audit completeness.
  - Status: RBAC well tested (AuthorizationTests, PermissionAuthorizationHandler). Cross-tenant connector isolation tested.
  - Completed: `SecurityTests` in `tests/SmartKb.Api.Tests/Security/SecurityTests.cs` — 21 tests covering all three remaining gaps: (a) **Cross-tenant isolation for escalation endpoints** — 4 tests: update returns 404 and leaves original unchanged, delete returns 404 and draft persists, export returns 404, list drafts by session returns 404. **Cross-tenant isolation for feedback endpoints** — 3 tests: submit feedback on cross-tenant message returns 422, list feedbacks returns 404, get feedback returns 404. **Cross-tenant session isolation** — 2 tests: send message to cross-tenant session returns 404, delete cross-tenant session returns 404. (b) **Malformed JSON / invalid request body** — 7 tests: malformed JSON on chat/session/message/escalation/feedback endpoints returns 400, empty body returns 400/422, wrong content type returns 400/415. (c) **Concurrent access** — 3 tests: concurrent message sends produce consistent session state, concurrent feedback submissions resolve via upsert, concurrent session creation all succeed independently. All endpoints use tenant-scoped queries and return 404 (not 403) for cross-tenant resources to prevent information leakage.
- [x] T-005: Load tests for concurrent chat, ingestion bursts, webhook spikes, search index throughput.
  - Status: Complete. 25 load tests in `tests/SmartKb.Api.Tests/Load/`.
  - Completed: `LoadTestFactory` with WebApplicationFactory + SQLite in-memory DB. Four test classes in serialized `[Collection("LoadTests")]`: (a) **ConcurrentChatLoadTests** (6 tests) — 50 parallel session creation, 20 parallel message sends, 10 concurrent same-session messages, 20 parallel session list reads, 10-user chat isolation, 50 sequential throughput. (b) **IngestionBurstLoadTests** (6 tests) — 20 parallel connector creation, 10 parallel sync triggers, 30 parallel connector list reads, concurrent enable/disable toggle, 30 sequential throughput, multi-tenant connector isolation. (c) **WebhookSpikeLoadTests** (7 tests) — 50 concurrent ADO webhooks, 50 concurrent SharePoint webhooks, 30 concurrent Graph validation handshakes, 30 concurrent HubSpot webhooks, 30 concurrent ClickUp webhooks, 60 mixed-provider concurrent traffic, 100 sequential throughput. (d) **SearchThroughputLoadTests** (6 tests) — 50 parallel stateless chat queries, 20 parallel response integrity checks, 10-user multi-turn session queries, cross-tenant concurrent search isolation, 100 sequential throughput, 20 concurrent feedback submissions. Tests validate no data corruption, no 500s (except transient SQLite DB lock), tenant isolation under load, response integrity, and throughput budgets (30s for sequential workloads). All 1627 tests passing.
- [x] T-006: IaC tests/validations for Terraform and ARM on every infra change.
  - Status: Complete. 98 IaC tests (48 existing + 50 new) all passing.
  - Completed: Enhanced `check_parity.py` with `check_arm_security_hardening` function — validates security properties (HTTPS-only, TLS 1.2, RBAC authorization, soft-delete, SystemAssigned identity, private blob access, dead-lettering) on ARM resources. CLI now runs security hardening checks alongside resource parity checks. 6 new test classes in `test_check_parity.py`: (a) **TestCheckArmSecurityHardening** (10 tests) — unit tests for security hardening function: compliant/non-compliant web apps, storage TLS, Key Vault RBAC, blob public access, queue DLQ, multi-resource checking, search identity, missing file. (b) **TestProjectArmSecurityHardening** (17 tests) — validates actual `main.json`: all web apps enforce HTTPS, SystemAssigned identity, storage TLS 1.2, HTTPS-only traffic, private blob container, Key Vault RBAC + soft-delete + 90-day retention, search identity, SQL server v12.0, firewall allows Azure services only, queue DLQ + max delivery count, Linux App Service Plan, required app settings on both web apps, SQL connection string. (c) **TestPropertyLevelParity** (10 tests) — cross-validates key properties between TF and ARM: HTTPS-only on web apps, storage TLS version, Key Vault RBAC + soft-delete retention, queue max delivery count + dead-lettering, SQL server version, blob retention days, app setting keys, search identity. (d) **TestArmParameterFileConsistency** (6 tests) — validates ARM parameter files: valid schema, same keys across dev/staging/prod, environment value matches filename, required params present, param file keys reference main template. (e) **TestTfvarsFileConsistency** (5 tests) — validates TF tfvars: same keys across envs, environment value matches, keys reference declared variables, SKU vars present, ARM/TF SKU defaults match. (f) **TestCliIncludesSecurityHardening** (2 tests) — CLI fails on security violations, passes when compliant. CI workflow (`infra-validate.yml`) runs all tests on every infra change.
- [x] T-007: Frontend test hardening.
  - Status: Complete. 253 frontend tests (157 existing + 96 new) all passing across 24 test files.
  - Completed: (a) **API client tests** (`client.test.ts`) — 40 tests covering all endpoint groups: apiFetch internals (auth header injection, token provider null/missing, non-ok error, unwrap isSuccess=false), session endpoints (create/list/delete/getMessages/sendMessage), escalation draft endpoints (create/export/delete), feedback endpoints (submit, getFeedback 404 null return, rethrow non-404), outcome endpoints, connector admin endpoints (list/create/enable/disable/testConnection/syncNow/delete/listSyncRuns), pattern governance endpoints (getGovernanceQueue query string building, getPatternDetail URL encoding, review/approve/deprecate), retrieval settings endpoints (get/update/reset), diagnostics endpoints (summary, dead letters query param, secrets status), getMe. (b) **ChatPage tests** (`ChatPage.test.tsx`) — 12 tests covering: header rendering with nav links, no-session prompt, session load on mount, session creation, error banner on create failure, session selection with message loading, message send with API call verification, error banner on send failure, session deletion, session indicator display, silent listSessions failure handling, feedback submission flow. (c) **ConnectorDetail tests** (`ConnectorDetail.test.tsx`) — 22 tests covering: connector info rendering (name, status, type, auth, secret configured/not set, source config, schedule), back navigation, test connection (success/failure/error/loading states), status toggle (enable/disable with correct API call), sync now/backfill triggers (correct isBackfill flag, disabled for Disabled connectors), edit mode (enter, save with API call, exit on save, error on save failure), delete flow (confirmation prompt, confirm delete, cancel confirmation, error on delete failure), sync run loading on mount. (d) **PatternGovernancePage tests** (`PatternGovernancePage.test.tsx`) — 10 tests covering: loading state, access denied for non-governance roles and empty roles, Admin/SupportLead role access, pattern list loading and display, error banner on load failure, pattern detail selection with API call, error on detail load failure, navigation links. (e) **AuthProvider tests** (`AuthProvider.test.tsx`) — 4 tests covering: children rendered directly when MSAL not configured (dev mode), MsalProvider wrapping when MSAL configured, sign-in UI for unauthenticated users, Smart KB heading in auth gate. (f) **useRoles hook tests** (`useRoles.test.ts`) — 7 tests covering: initial loading state with empty roles, roles set after successful fetch, empty roles on fetch failure, cleanup on unmount (cancelled flag prevents state update), hasAdminRole true/false/empty.
- [x] T-008: Backend service test gaps.
  - Completed: (a) **DeadLetterService** — `DeadLetterServiceTests` with 8 tests: DLQ path construction, empty peek, message mapping (messageId, correlationId, subject, deliveryCount, body), multiple messages, default maxMessages=20, DisposeAsync receiver cleanup, DeadLetterMessage record properties, DeadLetterListResponse record properties. Mock `ServiceBusClient` + `ServiceBusReceiver` with virtual method overrides. (b) **KeyVaultSecretProvider** — `KeyVaultSecretProviderTests` with 5 tests: GetSecretAsync returns value, GetSecretAsync propagates RequestFailedException for missing secret, SetSecretAsync stores value, DeleteSecretAsync removes secret, multiple secrets return correct values. Mock `SecretClient` with virtual method overrides + `SecretModelFactory`. (c) **SqlAnswerTraceWriter** — `SqlAnswerTraceWriterTests` with 5 tests: all fields persisted (including JSON chunk IDs), multiple traces, escalation recommended flag, empty chunk lists as JSON arrays, CreatedAt timestamp range check. (d) `AdoSyncJobProcessorIntegrationTests` naming cosmetic — not changed (accurate description: "ADO connector wired into SyncJobProcessor").

## Open Risks / Watch Items
- [ ] R-001: Connector API limits and webhook reliability variability across providers. Mitigated by polling fallback (P0-008A) and rate-limit metrics (P1-008). Residual: per-provider quota tuning needed in production.
- [ ] R-002: Retrieval quality drift as corpus grows; requires active eval + tuning. Mitigated by eval harness (P0-021), per-tenant tuning (P1-007), and scheduled eval CI (P3-006 complete). Residual: monitor nightly/weekly eval trends for early drift detection.
- [ ] R-003: Escalation over/under-triggering before enough outcome data accumulates. Mitigated by routing analytics (P1-009) and improvement recommendations. Residual: cold-start period.
- [ ] R-004: Tenant misconfiguration risk in early environments. Mitigated by validation endpoints. Consider P3-010 (guided scope selection) for further mitigation.
- [ ] R-005: Terraform/ARM drift risk if infra updates bypass IaC workflows. Mitigated by drift-detection.yml (weekly scheduled), infra-validate.yml, and IaC parity tests (P1-010, T-006).
- [x] R-006: ~~PII leakage into model context~~ — resolved (P0-014A baseline redaction + P2-001 per-tenant policy with 3 enforcement modes + custom patterns).
- [ ] R-007: Service Bus message ordering under high load; design for idempotency. Mitigated by idempotency keys on all webhook handlers and content hash dedup in SyncJobProcessor.
- [x] R-008: Embedding model and chunking parameters — resolved in P0-005C.
- [ ] R-009: Dual Terraform + ARM maintenance burden. Fixed BUG-002. Ongoing vigilance via T-006 parity tests.
- [x] R-010: ~~Confidence/escalation thresholds undefined~~ — resolved. Configurable via `ChatOrchestrationSettings` and `EscalationSettings`.
- [x] R-011: ~~Phase 1 escalation drafts — copy/export only~~ — resolved. P1-003 added external ticket creation (ADO/ClickUp) after human approval.
- [x] R-012: ACL metadata models differ per source — resolved. Each connector documents its ACL mapping.
- [x] R-013: ~~Session context may exceed token limits~~ — resolved (D-010).
- [x] R-014: ~~No Ingestion Worker resource in IaC~~ — resolved (TECH-001).
- [x] R-015: ~~Service Bus uses connection string not Managed Identity~~ — resolved (TECH-002).
- [x] R-016: ~~Feedback reason codes not enumerated~~ — resolved (P0-018).
- [x] R-017: ~~jtbd-06 has no numeric SLO thresholds~~ — resolved (P0-021).
- [x] R-018: ~~Entra ID config optional with silent fallback — misconfiguration risk in production.~~ **RESOLVED** (iteration 106). Added startup validation in `Program.cs`: throws `InvalidOperationException` when `ASPNETCORE_ENVIRONMENT=Production` and `AzureAd:ClientId` is missing or placeholder. Development/test environments retain silent fallback. 4 new tests in `EntraIdStartupValidationTests`.
- [x] R-019: jtbd-03 spec very thin (33 lines) — all detail in PRD. **Resolved**: SPEC-007 patched jtbd-03 from 33 to 73 lines with full pipeline stages, field schema, and telemetry.
- [x] R-020: ~~No .NET or frontend CI pipeline~~ — resolved (TECH-006).
- [x] R-021: Binary document text extraction — **RESOLVED** by P3-003 (iteration 74). SharePoint connector now downloads and extracts text from PDF/DOCX/PPTX/XLSX via PdfPig and Open XML SDK.
- [x] R-022: Pre-retrieval query classification. **Resolved by** P3-001 (iteration 69). Classification via gpt-4o-mini biases retrieval filters by product area, source type, and time horizon.
- [x] R-023: ~~No scheduled eval automation.~~ **RESOLVED** by P3-006 — nightly smoke and weekly full eval CI workflows with GitHub Actions annotations and regression-gated issue creation.
- [x] R-024: ~~Phase 2 admin features (routing, playbooks, cost, privacy) have no frontend UI.~~ **RESOLVED** by P3-008 (iteration 80). Four admin pages added: RoutingAnalyticsPage, PlaybooksPage, CostControlsPage, PrivacyAdminPage.
- [x] R-025: ~~ScheduleCron field on connectors is stored but never executed.~~ **RESOLVED** by P3-018 (iteration 87). Scheduled sync execution implemented via `ScheduledSyncService`.
- [x] R-026: ~~Eval report history not persisted.~~ **RESOLVED** by P3-021 (iteration 83). `EvalReportEntity` + 3 admin API endpoints for eval report persistence, listing, and detail retrieval.
- [x] R-027: ~~OAuth auth flow not implemented~~ — **Resolved by P3-019** (iteration 84). Full OAuth authorization code flow with token refresh for all 4 connector types.
- [x] R-028: ~~Confidence rationale absent from response contract.~~ **RESOLVED** by P3-024 (iteration 82). `ConfidenceRationale` field added to ChatResponse and MessageEntity.
- [x] R-029: ~~No in-app rendered Source Viewer for evidence.~~ **Resolved by** P3-025 — `SourceViewerPanel` renders full evidence content inline with "View content" drill-down from EvidenceDrawer citation cards.
- [x] R-030: Audit export is API-only. **Resolved by** P3-029 — AuditCompliancePage at `/audit` provides self-serve event browsing, filtering, and NDJSON export.
- [x] R-031: ~~Customer-managed keys (CMK) not evaluated.~~ **RESOLVED** by P3-030 (iteration 101). CMK IaC support added for Storage, SQL TDE, and Search. D-019 documents per-tenant CMK architecture (infrastructure-level, not application-level per-tenant keys). Service Bus CMK deferred (requires Premium SKU).
- [x] R-032: No CD/deploy workflow. **Resolved by** P3-032 — deploy.yml with environment-gated Terraform apply, backend publish, frontend SWA deploy, and post-deploy smoke test.
- [x] R-033: ~~Distilled patterns are not auto-indexed to Azure AI Search.~~ **RESOLVED** by P3-034 (iteration 73). Distillation auto-indexes; governance deletes from index on deprecation.
- [x] R-034: No frontend hosting resource in IaC. **Resolved by** P3-036 — Azure Static Web App provisioned in Terraform and ARM.
- [x] R-035: Multi-turn conversation quality is unevaluated. **RESOLVED** by P3-038 (iteration 79). 12 multi-turn eval cases added covering follow-up clarification, context accumulation, escalation after failure, topic switching, and session summary scenarios.
- [x] R-036: ~~ARM/Terraform availability threshold default discrepancy (99 vs 99.5).~~ **RESOLVED** by P3-037 (iteration 85). ARM default updated to 99.5 with float() conversion.

## Open Design Decisions (must resolve before dependent items)

- [x] D-001: Embedding model — resolved: text-embedding-3-large at 1536 dimensions.
- [x] D-002: Chunking strategy — resolved: 512 tokens/chunk, 64 overlap, structural boundaries.
- [x] D-003: Confidence scoring methodology — resolved: 0-1 float, blended = 0.6×modelSelfReport + 0.4×retrievalHeuristic (avgRrfScore × saturation). Categorical: High (>=0.7), Medium (0.4-0.7), Low (<0.4). Configurable via `ChatOrchestrationSettings`.
- [x] D-004: Escalation policy schema and thresholds — resolved: confidence < 0.4 AND severity >= P2. Per-tenant `EscalationRoutingRules` table (tenant_id, product_area, target_team, escalation_threshold, min_severity). Fallback to "Engineering". Configurable via `EscalationSettings`.
- [x] D-005: Top-k and RRF fusion weights — resolved: top-k=20, equal RRF weights (1.0/1.0), semantic reranker on merged top-20. Pattern Index deferred to P1-004. Configurable via `RetrievalSettings`.
- [x] D-006: OpenAI model version — resolved: `gpt-4o` (latest), configurable via `OpenAiSettings.Model`. Temperature 0.2 for grounded generation.
- [x] D-007: Gold dataset strategy — resolved: JSONL format in `eval/gold-dataset/`, 30 manually authored seed cases covering 10 categories, JSON schema validation, PR-based review, min 30 cases for gated release. Schema at `eval/gold-dataset/schema.json`.
- [x] D-008: Solved-ticket candidate criteria — resolved: Status in (Closed, Resolved) AND ResolvedWithoutEscalation AND positive_feedback >= 1. Configurable per tenant via `DistillationSettings` (MinPositiveFeedback, SolvedStatuses, BaseConfidence, PositiveFeedbackBoost, NegativeFeedbackPenalty).
- [x] D-009: Terraform remote state backend — resolved in P1-012. Azure Storage backend with blob lease locking via `use_azuread_auth`.
  - **Proposed**: Azure Storage with blob lease locking.
- [x] D-010: Session token budget — resolved: sliding window, hard cutoff at 80% of gpt-4o 128k (102,400 tokens). Oldest messages dropped first. Configurable via `ChatOrchestrationSettings.MaxTokenBudget`. Summarization deferred to Phase 2.
- [x] D-011: Audit export format — resolved: NDJSON with cursor-based pagination. `GET /api/audit/events/export` returns `application/x-ndjson` stream. Each line is a JSON-serialized `AuditEventResponse`. Final line is cursor metadata (`__cursor: true`, `afterTimestamp`, `afterId`, `hasMore`). Client paginates by passing `afterTimestamp` and `afterId` query params from cursor to next request.
- [x] D-012: No-evidence threshold — resolved: < 3 results above score 0.3 (both conditions, tunable via `RetrievalSettings.NoEvidenceScoreThreshold` and `NoEvidenceMinResults`).
- [x] D-013: Hallucination degradation — resolved: refuse + next-steps. Blended confidence < 0.3 overrides final_answer to next_steps_only with "I don't have enough information" + diagnostic steps + escalation to Engineering. No-evidence path (HasEvidence=false) returns next_steps_only immediately. Configurable via `ChatOrchestrationSettings.DegradationThreshold`.

### Phase 3 Design Decisions (open — must resolve before dependent P3 items)

- [x] D-014: Pre-retrieval classification model and schema — **Resolved**: gpt-4o-mini (lighter/faster/cheaper than gpt-4o for generation). Schema: 9 fields (issue_category, product_area, severity_hint enum P1-P4/Unknown, needs_customer_lookup, missing_info_suggestions array, classification_confidence float, escalation_likelihood float, source_type_preference array, time_horizon_days nullable int). Classification biases retrieval via `EnrichFiltersFromClassification`: applies product_area, source_types, time_horizon to `RetrievalFilter` when confidence >= 0.5 threshold and user hasn't set explicit filters. Implemented in P3-001.
  - **Proposed**: Use gpt-4o-mini for classification (lower latency + cost). Output schema: `{ issue_category, product_area, severity_hint, environment_hint, missing_info_suggestions[] }`. Apply product_area as OData filter; severity_hint as escalation input; missing_info as FR-TRIAGE-002 response.
- [x] D-015: Text extraction tooling for binary documents — Azure AI Document Intelligence (cloud, cost per page) vs. local libraries (PdfSharp, Open XML SDK — free, no external calls). **Resolved** (iteration 74).
  - **Decision**: Use Open XML SDK (`DocumentFormat.OpenXml 3.4.1`) for DOCX/PPTX/XLSX (free, deterministic, no external calls). Use PdfPig (`PdfPig 0.1.13`) for PDF (free, .NET native). Azure AI Document Intelligence reserved for scanned/image-heavy PDFs in a future phase. Extraction happens inline during SharePoint connector fetch — `DownloadAndExtractTextAsync` downloads file bytes, extracts text, and sets `TextContent` on the `CanonicalRecord`. Falls back to metadata-only on failure.
- [x] D-016: Session summarization model — same model as generation or a cheaper/faster model? Summary length and format? **Resolved** (iteration 72).
  - **Decision**: Use gpt-4o-mini (configurable via `SummarizationModel`). Structured JSON output with `key_issue`, `attempted_solutions[]`, `unresolved_questions[]`, `customer_context`. Max 256 tokens. Injected as system message before remaining session history. Summarization triggered only when ≥4 messages would be dropped (configurable via `SummarizationMinDroppedMessages`). 5s timeout with graceful degradation (drops without summary on failure).
- [x] D-017: Search index migration strategy — blue-green vs. in-place with compatible changes only. **Resolved** (iteration 76).
  - **Decision**: Blue-green approach. Create versioned index (e.g., `evidence-v2`), reindex from SQL chunks via batched queries, activate new version in SQL tracking table, retire old version. Schema change detection via SHA-256 hash of sorted field definitions. Rollback reactivates previous version. Note: embedding vectors are not stored in SQL so reindexed documents have null vectors until re-embedding — acceptable trade-off for schema migration safety.

- [x] D-019: Customer-managed key (CMK) encryption architecture — **Resolved** (iteration 101).
  - **Decision**: CMK is an infrastructure-level opt-in (per deployment, not per application-tenant). A single User Assigned Identity (`id-smartkb-cmk-{env}`) with Key Vault Crypto Officer role accesses the encryption key. CMK covers three services: (1) Storage Account — `customer_managed_key` block with Key Vault key URI + identity; (2) Azure SQL — Transparent Data Encryption (TDE) with `keyId` on server resource; (3) Azure AI Search — `encryptionWithCmk.enforcement=Enabled`. Service Bus CMK requires Premium SKU (not cost-effective for current Basic/Standard tiers) — deferred until Premium is justified by throughput needs. Key Vault purge protection is auto-enabled when CMK is active (required by Azure for CMK scenarios). Per-tenant key isolation (each tenant gets a separate key) is not feasible at infrastructure level — would require separate Azure resources per tenant. If true per-tenant CMK is needed, implement at the application layer using client-side encryption with per-tenant keys stored in Key Vault. All environments default to CMK disabled (`enable_cmk = false`); opt-in by setting `enable_cmk = true` and providing `cmk_key_vault_key_id`.

- [x] D-018: Model fine-tuning / distillation decision — **Resolved** (iteration 102).
  - **Decision**: **Defer model fine-tuning/distillation indefinitely.** Current architecture (gpt-4o generation + gpt-4o-mini classification/summarization + RAG retrieval with two-store evidence/pattern architecture) meets all latency SLOs (P95 ≤ 8s) and quality targets without custom model training. Fine-tuning should only be reconsidered when **all** of the following trigger criteria are met simultaneously:
    1. **Gold dataset maturity**: ≥ 500 curated gold cases (currently 62 baseline + admin-managed via GoldCaseService) with diverse coverage across all connector types, severity levels, and product areas.
    2. **Eval stability**: Scheduled eval CI (P3-006) running for ≥ 3 months with stable baselines (no blocking regressions in ≥ 6 consecutive weekly runs).
    3. **Demonstrated quality ceiling**: Prompt engineering, retrieval tuning (P1-007), synonym maps (P3-004), and query classification (P3-001) have been exhausted and quality metrics plateau below targets.
    4. **Cost justification**: Per-query LLM cost exceeds acceptable thresholds AND a smaller distilled model can demonstrably reduce cost while maintaining quality within 5% of gpt-4o baselines.
  - **Privacy requirements** (mandatory before any training data export):
    - All training data must pass through `PiiRedactionService` with enforcement mode `"redact"` (P2-001) — no exceptions.
    - Per-tenant PII policies (custom patterns + enabled types) must be applied during export.
    - Tenant consent must be obtained before including their data in any training set.
    - Training data must be exported via `GoldCaseService.ExportAsJsonlAsync` (P3-022) which enforces tenant isolation.
    - Exported data must be reviewed by a human (SupportLead or Admin role) before submission to any fine-tuning API.
    - Audit events must be logged for every training data export operation.
  - **Cost/benefit analysis**: Fine-tuning gpt-4o or distilling to a smaller model incurs (a) training compute cost ($25+ per fine-tuning job), (b) ongoing hosting cost for custom model endpoints, (c) model maintenance burden (re-train on corpus changes), (d) evaluation complexity (must maintain separate eval baselines for fine-tuned vs. base model). Benefits are marginal when RAG + prompt engineering already meet quality targets. The case-pattern distillation pipeline (P1-005) already captures institutional knowledge without model customization.
  - **Architecture note**: If fine-tuning is eventually pursued, the recommended approach is distillation of gpt-4o into gpt-4o-mini using the existing gold dataset + high-confidence answer traces (from `AnswerTraceEntity`). This preserves the RAG architecture while potentially reducing per-query latency and cost for the generation step only. Classification and summarization already use gpt-4o-mini.

## Spec Clarification Backlog
Items where specs are ambiguous, inconsistent, or missing detail. Patch before or during dependent implementation.
**Tracked collectively as P3-011. All items resolved.**

- [x] SPEC-001: jtbd-01 — Add webhook registration lifecycle. Resolved in P0-008A.
- [x] SPEC-002: jtbd-01 — Define polling fallback interval. Resolved in P0-008A.
- [x] SPEC-003: jtbd-01 — Define content-level dedup strategy using ContentHash. **Spec patched**: jtbd-01 lines 21-24 document SHA-256 content hash dedup at raw content, chunk, and sync-run layers.
- [x] SPEC-004: jtbd-02 — Define enrichment version scheme. **Spec patched**: jtbd-02 lines 17-21 document monotonic int versioning, ReprocessedAt, hash-includes-version, deterministic reprocessing.
- [x] SPEC-005: jtbd-02 — Define error token extraction. Resolved in P0-010.
- [x] SPEC-006: jtbd-02 — Standardize chunk ID format. Resolved: underscore format.
- [x] SPEC-007: jtbd-03 — Expand thin spec with PRD detail. **Spec patched**: jtbd-03 expanded from 33 to 73 lines with 4 pipeline stages, evidence index schema table, retrieval telemetry, and configurable parameters.
- [x] SPEC-008: jtbd-06 — Enumerate feedback reason codes. Resolved in P0-018.
- [x] SPEC-009: jtbd-06 — Define numeric SLO thresholds for eval gates. **Spec patched**: jtbd-06 lines 18-23 document all four thresholds with configurable `EvalSettings` and CI annotation behavior.
- [x] SPEC-010: jtbd-08 — Define routing rule precedence. **Spec patched**: jtbd-08 line 15 documents FirstOrDefault matching by ProductArea with FallbackTargetTeam.
- [x] SPEC-011: jtbd-08 — Define severity classification ownership. **Spec patched**: jtbd-08 lines 16-20 document 4-stage severity pipeline (ingestion → classification → LLM → agent override).
- [x] SPEC-012: jtbd-09 — Trust state mismatch resolved. **Spec patched**: jtbd-09 lines 15-20 document 4-state TrustLevel enum with transition rules and index behavior.
- [x] SPEC-013: jtbd-09 — Pattern usage/reuse metrics. **Spec patched**: jtbd-09 lines 28-32 document PatternMaintenanceService citation-based usage detection. Formal per-pattern analytics API tracked as P3-012.
- [x] SPEC-014: jtbd-10 — PII detection tooling. Resolved in P0-014A + P2-001.
- [x] SPEC-015: jtbd-10 — Default retention windows. **Spec patched**: jtbd-10 lines 23-27 document per-entity RetentionConfigEntity with dual-window policy and supported entity types.
- [x] SPEC-016: jtbd-10 — Cross-tenant detection beyond missing-tid. **Spec patched**: jtbd-10 lines 30-35 document 403 for missing tid, 404 for cross-tenant resource access, EF per-tenant filters, and audit trail.
- [x] SPEC-017: jtbd-11 — Add Ingestion Worker to IaC. Resolved in TECH-001.

## Plan Maintenance Checklist (run each iteration)
- [ ] Mark completed items with evidence (PR/test IDs).
- [ ] Reorder remaining items by risk and dependency.
- [ ] Add newly discovered bugs/tech debt with priority.
- [ ] Confirm Terraform and ARM templates remain synchronized after Azure resource changes.
- [ ] Remove stale completed details to keep plan concise.

## Audit Log
- **Iteration 66 (2026-03-18)**: Full spec-vs-code gap audit. All 11 specs and PRD FR-*/NFR-* requirements compared against implemented codebase. Phase 1+2 confirmed complete (1880 tests, 0 bugs blocking). 23 new Phase 3 backlog items added (P3-001 through P3-023). 4 new design decisions (D-014 through D-017). 7 new risks (R-021 through R-027). Open spec clarifications annotated with implementation status. Risk items R-006, R-010, R-011 marked resolved. Key gaps: pre-retrieval classification (FR-TRIAGE), binary text extraction (SharePoint), scheduled eval automation, Phase 2 admin frontend pages.
- **Iteration 67 (2026-03-18)**: Deep PRD cross-reference audit with requirement-level traceability. Cross-referenced all FR-CHAT, FR-TRIAGE, FR-ESC, FR-PATTERN, FR-FB, NFR-SEC, NFR-PERF, NFR-OPS, and ADM-* requirement IDs against P0–P3 backlog. 8 new Phase 3 items added (P3-024 through P3-031): confidence rationale (FR-CHAT-002), Source Viewer drill-down (FR-CHAT-003), routing tag mapping (ADM-004), connector preview retrieval test (ADM-006), stop-words/special tokens (ADM-103), audit export UI (compliance), CMK evaluation (data encryption), and model fine-tuning decision (Level 4 optional). 1 new design decision (D-018: fine-tuning). 4 new risks (R-028 through R-031). Test count updated to ~1949 (1627 backend + 253 frontend + 68 eval + 98 IaC - some overlap in counting). Phase 3 backlog now has 31 items ordered by priority.
- **Iteration 68 (2026-03-18)**: Full spec-vs-code reconciliation with codebase deep scan. All source code (API endpoints, DI registrations, EF entities, connectors, services, frontend pages/components, IaC resources, CI workflows, eval harness, gold dataset) compared against 11 specs + PRD + existing backlog. 7 new Phase 3 items added (P3-032 through P3-038): CD/deploy workflow (HIGH — no deployment automation exists), dead BaselineEnrichmentService cleanup, auto-index patterns after distillation (MEDIUM — patterns invisible to retrieval), embedding cache eviction worker, frontend hosting IaC (MEDIUM — no production hosting target), ARM threshold fix, multi-turn eval cases (MEDIUM — 0 of 30 gold cases exercise session_history). 5 new risks (R-032 through R-036). Confirmed: all Phase 1 + Phase 2 items complete, all test backlog items complete, all bugs/tech-debt resolved. Phase 3 backlog now has 38 items. Priority ordering validated: P3-001 (pre-retrieval classification), P3-003 (binary extraction), P3-006 (scheduled eval), P3-018 (scheduled sync), P3-032 (CD workflow) remain the top-5 HIGH-priority items.
- **Iteration 70 (2026-03-18)**: P3-006 — Scheduled evaluation CI workflows. New `SmartKb.Eval.Cli` console project (EvalCliRunner, HttpChatOrchestratorClient, GitHubActionsFormatter). Two GitHub Actions workflows: `eval-nightly.yml` (daily 02:00 UTC, smoke subset) and `eval-weekly.yml` (Sunday 04:00 UTC, full dataset, auto-creates GitHub issue on blocking regression). Gold dataset expanded 30 → 50 cases (HubSpot, ClickUp, pattern governance, cost controls, GDPR/privacy, playbooks, diagnostics, performance SLOs). CLI supports live (staging API) and offline (dataset validation + baseline check) modes. Stratified smoke sampling. GitHub Actions annotations for violations/regressions. 22 new tests (13 EvalCliRunnerTests + 9 GitHubActionsFormatterTests). R-023 resolved. Total: 1673 backend + 253 frontend = 1926 tests, all passing.
- **Iteration 72 (2026-03-18)**: P3-002 — Session context summarization for long conversations. New `ISessionSummarizationService` interface + `OpenAiSessionSummarizationService` implementation using gpt-4o-mini with structured JSON output (`key_issue`, `attempted_solutions[]`, `unresolved_questions[]`, `customer_context`). `ChatOrchestrator.AssembleMessages` refactored: new `ComputeDroppedMessages` method detects sliding-window drops; when ≥4 messages would be dropped and summarization is enabled, calls gpt-4o-mini to produce a ~200-token structured summary injected as a system message before remaining history. New `ComputeHistoryBudget` helper extracts shared budget calculation. Settings added to `ChatOrchestrationSettings`: `EnableSessionSummarization` (default: true), `SummarizationModel` (gpt-4o-mini), `SummarizationMaxTokens` (256), `SummarizationTimeoutMs` (5s), `SummarizationMinDroppedMessages` (4). OTel counter `smartkb.chat.session_summarizations_total`. DI registration in Program.cs. D-016 resolved. 22 new tests (5 AssembleMessages summary, 4 ComputeDroppedMessages, 7 SessionSummarizationService, 5 settings, 1 diagnostics). Total: 1714 backend + 253 frontend = 1967 tests, all passing.
- **Iteration 73 (2026-03-18)**: P3-034 — Automatic pattern indexing completion. Verified PatternDistillationService already auto-indexes patterns after distillation (line 401). Fixed PatternGovernanceService to delete deprecated patterns from search index (via `DeletePatternsAsync`) instead of re-indexing them with Deprecated flag. Non-deprecation transitions continue to re-index with updated trust level. 7 new tests: deprecation deletes from index (3 source states: Draft/Reviewed/Approved), review re-indexes, approve re-indexes, graceful failure handling for both deprecation and approval. R-033 resolved. Total: 1721 backend + 253 frontend = 1974 tests, all passing.
- **Iteration 76 (2026-03-19)**: P3-005 — Search schema versioning and rollback strategy. Blue-green index migration with SQL version tracking. `IndexSchemaVersionEntity` + EF migration `20260319100000_AddIndexSchemaVersions`. `IIndexMigrationService` (7 methods) + `IndexMigrationService` with SHA-256 schema hash change detection, batched SQL→Search reindexing, full lifecycle (create → reindex → activate → retire). DTOs: `IndexSchemaVersionInfo`, `MigrationPlan`, `MigrationResult`. 7 admin endpoints under `/api/admin/index-migrations` using `HttpContext.RequestServices` for graceful 503 when search not configured. `BuildIndexDefinition`/`ToSearchDocument` made public on both indexing services. Conditional DI registration in API and Ingestion. D-017 resolved. 31 new tests (22 unit + 9 integration). Total: 1806 backend + 258 frontend = 2064 tests, all passing.
- **Iteration 83 (2026-03-19)**: P3-021 — Eval report persistence and API. `EvalReportEntity` with MetricsJson, ViolationsJson, BaselineComparisonJson columns. EF migration `20260319140000_AddEvalReports` with indexes on (TenantId), (TenantId, CreatedAt), (TenantId, RunType). `IEvalReportService` + `EvalReportService` — persist, list (paginated, run-type filterable), get-by-id with tenant isolation. 8 API DTOs (EvalReportSummary, EvalReportDetail, EvalMetricsDto, EvalViolationDto, EvalBaselineComparisonDto, EvalRegressionDetailDto, EvalReportListResponse, PersistEvalReportRequest). 3 admin endpoints: GET/POST `/api/admin/eval/reports`, GET `/api/admin/eval/reports/{id}`. Audit event `eval.report_persisted`. R-026 resolved. 26 new tests (16 unit + 10 integration). Total: 1872 backend + 296 frontend = 2168 tests, all passing.
- **Iteration 92 (2026-03-19)**: P3-013 — Pattern version history for field-level change tracking. `PatternVersionHistoryEntity` + EF migration `20260319170000_AddPatternVersionHistory` (TenantId, PatternId, Version, ChangedFieldsJson, PreviousValuesJson, ChangeType, Summary, ChangedBy, ChangedAt; indexes on TenantId+PatternId and TenantId+ChangedAt). `CaptureGovernanceSnapshot` records previous governance fields before mutation. `TransitionAsync` writes history entry on every trust-level transition. `GetPatternHistoryAsync` retrieves tenant-isolated history ordered by ChangedAt descending. DTOs: `PatternVersionHistoryEntry`, `PatternVersionHistoryResponse`. `GET /api/patterns/{patternId}/history` endpoint (requires `pattern:approve`). Frontend: TypeScript interfaces, `getPatternHistory` API client function, Version History table in `PatternDetailView` (date/change/by/fields columns, loading/empty states). CSS for version-history-table. 16 new backend tests (11 governance service: review/approve/deprecate history, multiple transitions, not-found, tenant isolation, empty, snapshot capture, invalid transition + 5 endpoint: CRUD/auth/RBAC/tenant) + 4 new frontend tests (empty/populated/loading/error). Total: 2011 backend + 327 frontend = 2338 tests, all passing.
- **Iteration 94 (2026-03-23)**: P3-022 — Gold dataset management workflow. `GoldCaseEntity` + EF migration `20260319190000_AddGoldCases` (TenantId, CaseId unique per tenant, Query, ContextJson, ExpectedJson, TagsJson, SourceFeedbackId, CreatedBy/UpdatedBy). `IGoldCaseService` + `GoldCaseService` with CRUD, promote-from-feedback (loads feedback + user message, uses CorrectionText as query), JSONL export (eval-CLI-compatible format). Validation: CaseId `eval-NNNNN`, Query >= 5 chars, ResponseType enum, MinConfidence/MinCitations ranges, duplicate check. 7 API endpoints under `/api/admin/eval/gold-cases`: list (paginated + tag filter), get, create, update, delete, export NDJSON, promote from feedback. 4 audit event types. Frontend: `GoldDatasetPage` at `/gold-cases` with Cases/Create/Export tabs, 7 API client functions, AdminPage nav link. 26 new backend tests (20 unit + 13 integration) + 15 new frontend tests. Total: 2045 backend + 347 frontend = 2392 tests, all passing.
- **Iteration 105 (2026-03-23)**: Spec acceptance criteria sync. Checked all 55 acceptance criteria checkboxes across 11 spec files (jtbd-01 through jtbd-11) to match actual implementation status. No code changes; documentation-only.
- **Iteration 106 (2026-03-23)**: R-018 — Entra ID production startup validation. Added fail-fast check in `Program.cs`: when `ASPNETCORE_ENVIRONMENT=Production` and `AzureAd:ClientId` is missing/empty/placeholder, throws `InvalidOperationException` at startup instead of silently falling back to unauthenticated mode. Development/test environments retain silent fallback for local development. 4 new tests in `EntraIdStartupValidationTests` (3 Production failure cases: empty, null, placeholder; 1 Development success case). R-018 resolved. R-025 marked resolved (was stale — P3-018 already complete).
- **Iteration 101 (2026-03-23)**: P3-030 — Customer-managed key (CMK) encryption evaluation and IaC support. D-019 design decision documented: CMK is infrastructure-level opt-in (per deployment), covers Storage Account (customer_managed_key), SQL Server (TDE with keyId), Azure AI Search (encryptionWithCmk enforcement). Service Bus CMK deferred (requires Premium SKU). Terraform: `enable_cmk` bool + `cmk_key_vault_key_id` string variables with URI validation, conditional `customer_managed_key`/`transparent_data_encryption`/`customer_managed_key_enforcement_enabled` blocks, `azurerm_user_assigned_identity` + Key Vault Crypto Officer role in `cmk.tf`, identity blocks upgraded to `SystemAssigned, UserAssigned` when CMK active, purge protection auto-enabled. ARM: `enableCmk`/`cmkKeyVaultKeyId` parameters, conditional `ManagedIdentity/userAssignedIdentities` resource, conditional Crypto Officer role assignment, `createObject`-based conditional encryption/identity on Storage/SQL/Search, conditional purge protection. Parity: `check_cmk_parity()` validates enable_cmk variable ↔ enableCmk parameter alignment and cmk.tf existence. `TF_TO_ARM_TYPE` extended with `azurerm_user_assigned_identity` ↔ `Microsoft.ManagedIdentity/userAssignedIdentities`. All tfvars and ARM parameter files updated with `enable_cmk = false`. Infra version bumped to 1.7.0. R-031 resolved. 17 new IaC parity tests (7 unit `TestCheckCmkParity` + 10 project-level `TestProjectCmkParity`).
- **Iteration 116 (2026-03-23)**: Frontend quality improvements — 3 tech-debt items. **TECH-010**: Added `v7_startTransition` and `v7_relativeSplatPath` future flags to `BrowserRouter` (App.tsx) and all 11 `MemoryRouter` instances in page test files; zero React Router deprecation warnings remaining. **TECH-011**: Route-based code splitting via `React.lazy()` for 10 admin pages + `Suspense` fallback + Vite `manualChunks` (vendor 158KB, msal 265KB); monolithic 615KB bundle split into 15 chunks; zero build warnings. **TECH-012**: Replaced 3 `NotImplementedException` in `StubTokenUsageService` test class with proper zero-value returns; zero `NotImplementedException` remaining in codebase. 413 frontend tests passing, build clean.
- **Iteration 120 (2026-03-23)**: TECH-021 — Narrow remaining broad `catch (Exception ex)` blocks to propagate cancellation. 55 `catch (Exception ex)` blocks across 27 files narrowed with `when (ex is not OperationCanceledException)` guard. Covers all connector clients (ADO, SharePoint, HubSpot, ClickUp), ChatOrchestrator, SyncJobProcessor, NormalizationPipeline, webhook handlers/managers, ConnectorAdminService, PatternDistillationService, PatternGovernanceService, EscalationDraftService, SecretRotationService, OAuthTokenService, AzureSearchRetrievalService, TextExtractionService, IndexMigrationService, EvalReportService, DataSubjectDeletionService, EvalRunner, Program.cs. 5 catches correctly left as-is (preceded by explicit OCE catch or top-level CLI handler). 3 new cancellation propagation tests (embedding/retrieval/generation OCE propagation). Zero unguarded broad catches remaining in async production code. Total: ~2570 tests.
- **Iteration 135 (2026-03-24)**: TECH-036 — Log 12 silent catch blocks in connector clients, ConnectorAdminService, SyncJobProcessor. Added `_logger.LogWarning(ex, ...)` with structured parameters to: SharePointConnectorClient (TestConnection, token acquisition, delta query), HubSpotConnectorClient (TestConnection, 2× object mapping), ClickUpConnectorClient (TestConnection, task mapping), AzureDevOpsConnectorClient (TestConnection), ConnectorAdminService (OAuth resolution, secret retrieval), SyncJobProcessor (credential retrieval). Zero silent catch blocks remaining in production backend code.
- **Iteration 170 (2026-03-25)**: TECH-073 — Add `StringComparison.Ordinal` to 11 culture-dependent string operations. Fixed `.Contains`, `.IndexOf`, `.StartsWith`, `.Replace` calls on ASCII literal patterns in SynonymMapService, SharePointConnectorClient, PatternDistillationService, GoldCaseService, GoldDatasetLoader, ODataFilterBuilder, AzureSearchRetrievalService, FusedRetrievalService. 3 new tests (Unicode OData escaping, Unicode synonym mappings). Zero culture-dependent string operations remaining on ASCII literal patterns.
